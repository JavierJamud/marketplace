# Plantilla documentada. Vite carga .env.development automáticamente en
# modo dev (npm run dev) y .env.production en build de producción — no hace
# falta tocar código para eso, es built-in. Copiar a .env.development para
# desarrollo local.

VITE_API_URL=http://localhost:4000
