# Addendum ZeuDin — Marketplace Cuba (actualización del handoff)

> Este addendum reemplaza el contexto genérico "Apex Market" del README original.
> El diseño ahora está **rebrandeado a ZeuDin** y alineado 1:1 a tu
> `ARQUITECTURA_MARKETPLACE.md` (Cuba, CUP, WhatsApp, provincias, planes, KYC,
> restaurantes, admin). El README original sigue siendo válido para la **tabla de
> tokens de Tailwind y el patrón de traducción** (los colores/tipografías no
> cambiaron — el `tailwind.config.js` ya calza).

## Qué cambió respecto de la v1

- **Marca:** Apex Market → **ZeuDin** (logo "Z" naranja, wordmark Zeu**Din**).
- **Moneda:** `es-AR` / `$` → **CUP** (`n.toLocaleString('es-CU') + ' CUP'`).
- **Pedido:** carrito genérico → **pedido por WhatsApp** (`wa.me/<telefono>?text=...`)
  como canal primario, + contra entrega + transferencia CUP. **Sin Stripe en ventas.**
- **Ubicación:** filtrado global por **provincia + municipio** (16 provincias de Cuba);
  recomendación de **provincias adyacentes** cuando no hay stock local.
- **Planes:** Regular (gratis, 20 productos, solo WhatsApp) vs **Business** (badge,
  ilimitado, home destacada, IA, horarios). Onboarding con **KYC**.
- **Restaurantes:** mesas + **QR por mesa** + estado de cocina (Recibido → Preparando → Listo).
- **Admin:** panel completo (KYC, tiendas/bloqueo, clientes, campañas Resend,
  integraciones cifradas).

## Datos compartidos

- `zeudin-data.js` (en la raíz del proyecto de diseño) — datos que consumen todos
  los mockups. Es la **referencia del modelo de datos**.
- `cuba-seed-data.js` (en este handoff) — el **dataset de prueba de Cuba listo para
  tu `backend/prisma/seed.js`**: provincias, municipios (muestra), adyacencias,
  categorías, planes, vendedores (con owner/KYC), productos (CUP + código de barras),
  menú de restaurante con variantes, clientes, pedidos y verificaciones. Recorrelo y
  creá los registros con Prisma (hasheá passwords con bcrypt).

## Mapa completo: mockup → ruta real (`frontend/src/App.jsx`)

### Público (envuelto en `PublicLayout` — header navy + selector de ubicación)
| Mockup | Página repo | Ruta |
|---|---|---|
| `Home.dc.html` | `pages/public/Home.jsx` | `/` |
| `Shop.dc.html` | `pages/public/Shop.jsx` | `/catalogo` |
| `Product.dc.html` | `pages/public/Product.jsx` | `/producto/:vendorSlug/:productSlug` |
| `Store.dc.html` | `pages/public/Store.jsx` | `/tienda/:slug` |
| `Stores.dc.html` | `pages/public/Stores.jsx` | `/tiendas` |
| `Cart.dc.html` | `pages/public/Cart.jsx` | `/carrito` |
| `Checkout.dc.html` | `pages/public/Checkout.jsx` | `/checkout` |
| `Account.dc.html` | `pages/public/Account.jsx` | `/cuenta` |
| `VendorOnboarding.dc.html` | `pages/public/VendorOnboarding.jsx` | `/vender` |
| `TableOrder.dc.html` | `pages/public/TableOrder.jsx` | `/mesa/:qrToken` |
| `NotFound.dc.html` | `pages/public/NotFound.jsx` | `*` |

### Cliente
| `CustomerPanel.dc.html` | `pages/customer/CustomerPanel.jsx` | `/cuenta/panel` |

### Vendedor (envuelto en `VendorLayout` — sidebar navy `#0e1a28`)
| `VendorDashboard.dc.html` | `pages/vendor/VendorDashboard.jsx` | `/vendedor` (index) |
| `VendorProducts.dc.html` | `pages/vendor/VendorProducts.jsx` | `/vendedor/productos` |
| `VendorOrders.dc.html` | `pages/vendor/VendorOrders.jsx` | `/vendedor/pedidos` |
| `VendorTables.dc.html` | `pages/vendor/VendorTables.jsx` | `/vendedor/mesas` |
| `VendorVerification.dc.html` | `pages/vendor/VendorVerification.jsx` | `/vendedor/verificacion` |
| `VendorSettings.dc.html` | `pages/vendor/VendorSettings.jsx` | `/vendedor/configuracion` |

### Admin (envuelto en `AdminLayout` — sidebar teal oscuro `#001d1e`)
| `AdminDashboard.dc.html` | `pages/admin/AdminDashboard.jsx` | `/admin` (index) |
| `AdminVendors.dc.html` | `pages/admin/AdminVendors.jsx` | `/admin/tiendas` |
| `AdminVerifications.dc.html` | `pages/admin/AdminVerifications.jsx` | `/admin/verificaciones` |
| `AdminCustomers.dc.html` | `pages/admin/AdminCustomers.jsx` | `/admin/clientes` |
| `AdminCampaigns.dc.html` | `pages/admin/AdminCampaigns.jsx` | `/admin/campanas` |
| `AdminIntegrations.dc.html` | `pages/admin/AdminIntegrations.jsx` | `/admin/integraciones` |

## Notas de integración específicas de ZeuDin

- **Pedido WhatsApp:** el botón arma `wa.me/<vendor.whatsapp>?text=<detalle>` con
  el detalle del carrito. En tu repo tomá `vendor.whatsapp` real.
- **Carrito de un solo vendedor:** los mockups ya aplican la regla (si agregás de
  otra tienda, se reemplaza). En tu repo usá `CartContext` + `CartConflictModal`.
- **Layouts por sección:** hay 3 shells distintos — público (header navy),
  vendedor (sidebar `#0e1a28`), admin (sidebar `#001d1e`). Metelos en
  `PublicLayout.jsx`, `VendorLayout.jsx`, `AdminLayout.jsx` respectivamente; no
  repitas el chrome en cada página.
- **KYC / privacidad:** las pantallas de verificación tratan fotos como privadas
  y cifradas — respetá eso en el backend (nunca públicas).
- **Estados de cocina** (VendorOrders / TableOrder): `received | preparing | ready`
  → mapear a `table_orders.kitchen_status`.
- **Integraciones:** las claves se guardan cifradas y se activan desde el panel
  (no en `.env`), tal cual tu arquitectura.

## Archivos de este handoff (v2)

- `README.md` — guía base + tabla de tokens Tailwind (sigue vigente).
- `ADDENDUM_ZEUDIN.md` — este archivo (mapa completo de 25 pantallas + notas Cuba).
- `cuba-seed-data.js` — dataset de prueba para `prisma/seed.js`.
- `examples/PublicLayout.jsx`, `examples/Home.jsx` — patrón React (ajustar marca a ZeuDin).
- `design_references/` — mockups. Los ZeuDin actuales están en la **raíz del proyecto**
  (`*.dc.html`); son la fuente de verdad visual más reciente.

## Bloques aplicados

> Cada bloque fue un prompt de diseño separado que se implementó de forma
> incremental. No hay changelog ni los prompts originales guardados — esta
> lista es el resumen de una línea por bloque, para que cualquier sesión
> nueva (en esta PC o en otra) recupere contexto rápido sin releer todo el
> código. Agregá una línea nueva acá cada vez que termines un bloque nuevo.
> Bloques 1, 2, 3, 7, 8, 35 y 36 no dejaron rastro identificable en comentarios
> del código (probablemente setup inicial o trabajo absorbido por bloques
> posteriores) y se omiten a propósito, no son un error de esta lista.

- **Bloque 4:** El admin puede suspender la cuenta de un cliente desde AdminCustomers (el flag existía pero en ese momento aún no bloqueaba nada de verdad).
- **Bloque 5:** Corrección de un bug real en el envío de correos vía Resend (los errores de la API se detectan en vez de darse por enviados).
- **Bloque 6:** En el checkout, los datos de contacto y entrega del cliente pasan a ser obligatorios sin excepción.
- **Bloque 9:** Selector de país con teléfono normalizado a formato E.164, compartido entre el registro y el input de teléfono reutilizable.
- **Bloque 10:** Verificación KYC del vendedor con captura de documento solo por cámara en vivo (sin subir archivos de galería) más una validación automática básica de existencia/resolución del archivo.
- **Bloque 11:** Registro de cuenta y de tienda con todos los campos obligatorios, incluido el correo de contacto propio de la tienda.
- **Bloque 12:** Soft-delete y suspensión real de cuentas/tiendas, panel de "Uso de la plataforma" en el admin, edición básica de usuarios, buzón de sugerencias y confirmación genérica para acciones destructivas.
- **Bloque 13:** Único punto de generación de contenido con IA del proyecto — botón "Mejorar con IA" para textos de producto.
- **Bloque 14:** Se consolidan los 4 canales de pedido en 3 opciones claras de checkout y se agregan los métodos de pago aceptados (informativos) de cada tienda.
- **Bloque 15:** Analíticas de "clientes potenciales" para el vendedor más el chat interno vendedor↔admin.
- **Bloque 16:** La aprobación de verificación/cobro pasa a requerir SIEMPRE revisión humana; se suma la campanita de notificaciones del vendedor y la animación de confirmación (SuccessCheck).
- **Bloque 17:** Auditoría de seguridad del backend (sin Row Level Security), selector global de provincia/municipio, barra de progreso de navegación y slider de tiendas verificadas.
- **Bloque 18:** Catálogo de "tipo de negocio"/rubro administrable, obligatorio al registrar una tienda, con íconos y filtro propio en Tiendas.
- **Bloque 19:** Gestión de países de entrega y provincias de venta (multi) por tienda, más límites de plan (Regular/Business) expuestos al vendedor.
- **Bloque 20:** Rediseño de la pantalla /cuenta como vista propia de viewport completo, con drawer mobile que se cierra al navegar.
- **Bloque 21:** Chatbot con IA por tienda verificada (sin login del cliente), con documento privado de negocio que el vendedor puede subir para entrenarlo.
- **Bloque 22:** Favoritos de producto, sistema de reseñas/calificaciones con desglose 5→1 estrellas, moderación de comentarios por el admin y autocompletado de búsqueda.
- **Bloque 23:** Botón "Solicitar este producto" para productos agotados, tags de producto (hasta 5) y umbral de stock bajo.
- **Bloque 24:** El chatbot de tienda puede citar/recomendar productos concretos y muestra una burbuja proactiva a los 5s de entrar a la tienda.
- **Bloque 25:** Integración real de Stripe para el pago de verificación/suscripción (reemplaza el checkout simulado), con Groq como IA de respaldo de Gemini.
- **Bloque 26:** Las sesiones de chat vencen a las 24h desde el último mensaje y se corrige el logging de errores 5xx en Admin > Errores.
- **Bloque 27:** Ajuste de compatibilidad con la API de Groq (formato tipo OpenAI).
- **Bloque 28:** El bot de tienda puede agregar productos al carrito citándolos por número de catálogo, con tarjetas de producto compactas embebidas en el chat.
- **Bloque 29:** Flujo de pedidos Pendiente→Vendido con confirmación de venta manual (sin descuento automático de stock), generación de factura/garantía en PDF y edición/eliminación de pedidos pendientes.
- **Bloque 30:** Chatbot general del marketplace (Home), distinto del de cada tienda, con detección de "¿cómo va mi pedido?" y búsqueda de tiendas.
- **Bloque 31:** Consulta del estado del propio pedido dentro del chat, usando el email de sesión si el cliente está logueado.
- **Bloque 32:** Filtrado geográfico progresivo por provincia/municipio en las búsquedas del chat y transcripción de audio corto grabado por el cliente (Groq Whisper), reemplazando el reconocimiento de imágenes.
- **Bloque 33:** Registro centralizado de errores técnicos (ErrorLog) con su propio panel de Admin > Errores.
- **Bloque 34:** Sugerencias de seguimiento contextuales tras cada respuesta del bot, rediseñadas como lista vertical.
- **Bloque 37:** Serie de correcciones críticas de bugs reales en la búsqueda geográfica/por categoría del chatbot, más el panel de revisión de conversaciones con ejemplos curados (Groq no permite fine-tuning propio).
- **Bloque 38:** Sonido de notificación al recibir respuesta del bot, indicador de "escribiendo" (TypingDots) y se quita el avatar de los mensajes del bot.
- **Bloque 39:** Historial real de compras del cliente logueado dentro del chat, optimización de la búsqueda con muchos productos activos, y ondas de audio reales durante la grabación.
- **Bloque 40:** Corrección de bugs reales de búsqueda con acentos (se activa `unaccent` en Postgres) y del comando de vaciar el carrito por chat.
- **Bloque 41:** Tarjetas de tienda puntuales dentro del chat cuando el cliente pide ver una o varias tiendas.
- **Bloque 42:** Optimización del consumo de tokens en los prompts de IA y pruebas/ajustes con el modelo de NVIDIA.
- **Bloque 43:** El modelo de IA usado por cada proveedor pasa a ser configurable desde el admin, con cambio automático entre proveedores si uno falla.
- **Bloque 44:** El admin puede ver la lista real de modelos disponibles para la key guardada de cada proveedor de IA (evita typos de nombre de modelo).
- **Bloque 45:** Se retira Cerebras del sistema de IA (su cuenta gratuita dejó de funcionar) y se agrega NVIDIA como proveedor.
- **Bloque 46:** Suscripciones del Plan Business administrables (solo lectura + revocar) y anuncios/banners programados del marketplace.
- **Bloque 47:** Autenticación en dos pasos (2FA) opcional por código de correo en el login, monedas aceptadas por tienda, correo directo del admin a clientes/tiendas y barra de búsqueda + notificaciones en el panel admin.
- **Bloque 48:** Páginas legales/de ayuda editables desde el admin (Términos, FAQ, Ayuda, Contacto) y rediseño del botón flotante de chat.
- **Bloque 49:** Marca de la plataforma editable (nombre/logo), opción de pegar un link externo como imagen de producto, reordenar fotos de producto por drag & drop y plantillas de correo con nombre de la tienda.
- **Bloque 50:** Ofertas de vendedor sobre productos ya existentes, mostradas en el Home y en la tienda, con anuncio propio para vendedores verificados.
- **Bloque 51:** Ofertas ampliadas con contenido personalizado/HTML creadas por el admin, política de ofertas configurable (cooldown y duración), y subida de imágenes con recorte (ImageCropUploader).
- **Bloque 52:** Códigos de descuento del vendedor (% o monto fijo, con mínimo/máximo de compra, límite de usos y vigencia opcional) aplicables en el carrito; ofertas propias de cada tienda (distintas de las del Home) con imagen, descripción mejorable con IA y código de descuento asignado o creado al vuelo, incluyendo ofertas por tiempo limitado; y reseñas con hasta 4 fotos, solo en tiendas verificadas y solo para clientes logueados (con redirect a login y retorno automático). Follow-ups del mismo bloque: el ícono del carrito abre un mini-carrito flotante (drawer moderno, esquinas redondeadas, separado de los bordes) en vez de navegar a /carrito; corrección de imágenes de producto estiradas en mobile (alto fijo → aspect-ratio) y de la alineación del encabezado de tienda; scroll siempre reinicia arriba al cambiar de página.
- **Bloque 53:** La sección "Páginas" del admin quedó solo para Términos/Privacidad (HTML libre). Preguntas frecuentes pasó a tener su propia sección con CRUD real (antes era copy hardcodeado en React) — cada pregunta pertenece a un público (cliente o vendedor) vía el nuevo modelo `FaqItem`; el FAQ público es un acordeón de una sola apertura (abrir una pregunta cierra la anterior). Contacto y Centro de ayuda pasaron a tener su propia entrada de menú en el admin (seguían usando `StaticPage` por debajo, solo se reorganizó la UI).
- **Bloque 54:** Carrito persistente por cuenta — un cliente logueado que tiene productos en el carrito los recupera automáticamente al iniciar sesión en otro dispositivo (`CartSnapshot`, sincronizado en cada cambio mientras hay sesión). "Compartir carrito" genera un link público (`SharedCart`) que, al abrirlo, resuelve los productos contra el catálogo real (precio/stock vigentes, nunca los del momento en que se compartió) y los agrega al carrito de quien lo abre, con una animación de importación y pidiendo confirmación si ya tenía productos de otra tienda.
- **Bloque 55:** Precios por cantidad (mayoreo), 100% opcional por producto — el vendedor puede cargar, al crear o editar un producto, hasta 10 tramos de "a partir de N unidades, precio X" (cada tramo exige más unidades y un precio menor al anterior/base, validado en el backend). El precio real de cada línea del carrito y de cada ítem de un pedido siempre se resuelve en el servidor según la cantidad (nunca se confía en el precio mandado por el cliente); en la ficha de producto, carrito, mini-carrito y checkout se muestra el precio por unidad ya resuelto según la cantidad elegida junto con el ahorro respecto de pagar todo al precio de 1 sola unidad. Deliberadamente no se aplica a los pedidos de mesa/restaurante (`tables.controller.js`) — el pedido pidió explícitamente pensar en vendedores al mayoreo, no en menús a la carta.
- **Bloque 56:** Dos ajustes al alta de productos, ambos pedidos explícitos. (1) "Disponible siempre" (`Product.unlimitedStock`) — opcional, para productos sin stock acotado real (el vendedor no lleva o no quiere llevar la cuenta): el campo Stock deja de ser obligatorio, el producto se muestra siempre disponible en toda la plataforma (tienda, catálogo/búsqueda, ficha de producto, ambos bots de IA) y queda afuera de "stock bajo"/"sin stock" del dashboard del vendedor — se le avisa explícitamente en el formulario que no va a tener seguimiento. Mutuamente excluyente con tallas (cada talla sí necesita su propio stock real), validado también en el backend. (2) Foto obligatoria para salir a la venta — un producto recién creado SIEMPRE se guarda en pausa (`isActive: false`) porque todavía no puede tener fotos (necesita existir primero para subirlas); se reactiva solo en cuanto tiene al menos 1 foto (subida o por link) y vuelve a pausarse si se queda sin ninguna. El listado del vendedor distingue este caso ("Sin fotos (pausado)") de un pausado común.
- **Auditoría de seguridad (post-Bloque 54):** mapa completo de rutas frontend↔backend revisado sin encontrar endpoints huérfanos ni roles inconsistentes salvo 4 excepciones puntuales, ya corregidas: `GET/PATCH /vendors/me`, `/me/dashboard` y `/me/schedule` ahora exigen `requireRole("VENDOR")` explícito (antes solo `authenticate`, protegidos de facto por `resolveMyVendor` pero inconsistentes); `POST /auth/register` ahora tiene rate limit (era el único endpoint de auth sin ninguno); CORS admite múltiples orígenes separados por coma en `FRONTEND_URL` (antes solo uno). Se cerraron 3 huecos reales de supervisión de admin que no existían: códigos de descuento y ofertas de tienda de vendedores (Bloque 52) ahora tienen listado + activar/desactivar/eliminar desde `/admin/codigos-descuento` y `/admin/ofertas-tienda`; pedidos de mesa de cualquier vendedor ahora son visibles (solo lectura) desde el modal de estadísticas de `AdminVendors.jsx`. Login separado por rol: `/vendedor/ingresar` y `/admin/ingresar` son pantallas de entrada propias (mismo `POST /auth/login` de siempre, sin wizard de registro, rechazan con error genérico si el rol no corresponde a la URL) — `/cuenta` sigue igual para clientes.
- **Bloque 57 (bug real reportado en vivo):** en la ficha de producto, cambiar de talla a una con menos stock que la cantidad ya elegida dejaba el stepper mostrando un número mayor al disponible de verdad — ni "Agregar al carrito" ni "Pedir por WhatsApp" volvían a chequearlo. Se agregó un `useEffect` que reverifica la cantidad contra el stock real cada vez que cambia la selección, bajándola automáticamente si ya no alcanza.
- **Bloque 58:** el buscador (`SearchBar.jsx` → `/search` y `/search/autocomplete`) ignoraba mayúsculas/tildes (ya arreglado en el Bloque 52) pero un signo de puntuación de más o de menos entre la búsqueda y el nombre real rompía el match (ej. "Ron Santiago 750ml" no encontraba "Ron Santiago, 750ml"). Ahora se pliega todo signo/espacio no alfanumérico a un solo espacio de los dos lados de la comparación antes de compararlos. De paso se encontró y corrigió un bug real preexistente: la extensión `pg_trgm` (el fallback de "resultados más parecidos" para typos) estaba marcada como aplicada en el historial de migraciones pero nunca se había creado de verdad en la base — la búsqueda aproximada llevaba tiempo sin funcionar en silencio.
- **Bloque 59 (pedido explícito):** verificación de correo obligatoria antes de crear una cuenta (cliente o vendedor — el registro de vendedor es el mismo `POST /auth/register` + un `POST /vendors` después, así que gatearlo ahí alcanza para los dos). Se manda un código de 6 dígitos al correo (mismo patrón que el 2FA de login del Bloque 47) y la cuenta recién se crea al verificarlo — mientras tanto los datos del formulario viven en una tabla aparte (`PendingRegistration`), nunca en un usuario a medio crear, así que un intento abandonado no deja basura. Un correo que ya tiene cuenta nunca puede registrarse de nuevo — en vez de un error seco, se lo manda directo (con el código ya en camino) al flujo de restablecer contraseña. Si el cliente ya había comprado como invitado con ese mismo correo (sin cuenta), esos pedidos quedan vinculados de verdad (`customerId`) a la cuenta recién creada al verificar el código — antes ya aparecían en "Mis pedidos" por el email, pero no contaban para las estadísticas del vendedor que sí filtran por `customerId` real (clientes potenciales, etc.).
- **Bloque 60 (pedido explícito):** seguridad de sesión de punta a punta. (1) El código de login (antes opt-in vía un switch en el perfil de vendedor) pasa a ser **obligatorio para los 3 roles** — se salta únicamente si el navegador ya quedó marcado "de confianza" para esa cuenta en los últimos 30 días (`TrustedDevice`, ventana rodante, token propio en `localStorage`); un navegador nuevo siempre vuelve a pedirlo. (2) El refresh token deja de ser un JWT sin estado — pasa a ser un token opaco respaldado por una tabla `Session`, lo que permite revocar sesiones de verdad: `POST /auth/logout` (cierre normal) y el nuevo `POST /auth/logout-all` ("Cerrar sesión en todos los dispositivos", reemplaza el switch de 2FA en `VendorProfile.jsx`) tienen efecto **inmediato** porque `authenticate` ahora valida la sesión contra la DB en cada request, no solo la firma del JWT. (3) Una sola sesión activa por navegador — cada login nuevo revoca cualquier sesión previa de ese mismo navegador (`browserId` propio, independiente del "dispositivo de confianza"). (4) Cierre de sesión por inactividad: 30 minutos sin actividad muestran un aviso "¿sigues ahí?" con cuenta regresiva de 5 minutos (solo un clic explícito en "Sigo aquí" la reinicia); si nadie responde, se cierra la sesión de verdad. (5) Cierre consistente entre pestañas — si una pestaña cierra sesión (a mano, por inactividad, o por "todos los dispositivos"), las demás pestañas del mismo navegador se enteran vía el evento `storage` y se cierran igual, nunca queda una con la vista de adentro sin datos. (6) El destino tras cerrar sesión (a mano o por expiración) ahora depende de en qué panel se estaba — cliente vuelve al inicio del sitio, vendedor a `/vendedor/ingresar`, admin a `/admin/ingresar` (antes los 3 iban siempre a "/"). (7) Las 3 pantallas protegidas (panel de cliente, de vendedor, de admin) tenían cada una su propio guard manual escrito a mano — una de ellas (panel de cliente) ni siquiera chequeaba el rol. Se unificaron en un solo `<ProtectedRoute roles redirectTo>` reusado en las 3.
- **Bloque 61 (pedido explícito):** rediseño moderno del shell de correos (`emailShell()` en `_shared.js`) — cambia una sola vez y se propaga a las 11 plantillas. Header: con logo de marca sigue igual; sin logo, ya no muestra solo el nombre suelto — un cuadrado naranja con la inicial + nombre + subtítulo "Marketplace multivendedor de Cuba" (mismo criterio que `Footer.jsx`). Un correo enviado por una TIENDA (no la plataforma) muestra el logo/nombre de esa tienda con una pastilla "vía {siteName}" a la derecha si la tienda tiene `Vendor.logoUrl` cargado — sin logo propio, sigue mostrando la marca de la plataforma. Cuerpo: nueva pastilla de categoría/estado arriba del título (reusa `statusBadge()`, ahora es una posición fija del shell — las 3 plantillas que ya tenían su propia pastilla suelta en el cuerpo pasaron a usar esta posición) y título más grande (22px/800). Pie: fila de 4 íconos circulares bulletproof (WhatsApp/Instagram/Facebook/Sitio web, sin imágenes externas) que linkean a las redes reales cargadas en "Marca de la plataforma" (nuevos campos `whatsappUrl`/`instagramUrl`/`facebookUrl` en `SiteSettings`, editables desde `AdminBranding.jsx`) — un ícono sin link cargado simplemente no se muestra, nunca uno inventado; más una línea legal con links a Privacidad. **Pendiente:** no existe todavía una página `/preferencias` de opt-out de correos — el link de "Preferencias de correo" apunta a `/privacidad` mientras tanto.
- **Bloque 62 (pedido explícito):** ciclo de vida de inactividad de tiendas + re-enganche de clientes — primer cron real del proyecto (`node-cron`, `backend/src/jobs/vendorLifecycle.job.js`, diario 8:00am Cuba, arrancado desde `server.js`). `User.lastLoginAt` (nuevo) se actualiza en cada login/2FA/registro. Tres pasos diarios, en orden: (1) recordatorio por correo a los 7 días sin acceder al panel de vendedor, repetido cada 7 días mientras siga inactivo (`Vendor.lastInactivityEmailAt` throttlea el reenvío); (2) suspensión automática a los 90 días (`Vendor.status: SUSPENDED`, nuevo enum `VendorStatus`, distinto de `isBlocked` que sigue siendo el bloqueo manual del admin) — la tienda y sus productos desaparecen de **todo** punto público (barrido de ~20 sitios en 13 controllers que ya filtraban `isBlocked:false`, ahora también filtran `status:"ACTIVE"`; se encontró y cerró un hueco real preexistente en `tables.controller.js` — el QR de mesa no chequeaba `isBlocked`/`status` para nada); (3) re-enganche: cliente con exactamente 1 pedido no cancelado en una tienda, 30+ días de antigüedad, y la tienda publicó una `Offer` ACTIVE nueva desde esa compra → correo de "te puede interesar" (`WinbackEmailLog` evita reenviarlo). Reactivación **solo por un admin**, nunca automática ni por el propio vendedor — pantalla nueva `AdminSuspendedVendors.jsx` (`/admin/tiendas-suspendidas`), motivo obligatorio (mín. 5 caracteres), deja `VendorStatusLog` con `byAdminId` (a diferencia de la suspensión automática, que lo deja en `null`) y manda `vendorReactivatedEmail`. El vendedor suspendido que intenta entrar a su panel ve una pantalla propia explicando el motivo (`VendorLayout.jsx`, no un error genérico) en vez de quedar bloqueado a ciegas. Verificado en vivo de punta a punta: suspensión → desaparece de `/vendors`, `/vendors/:slug`, autocomplete y QR de mesa → reactivación real vía el controller → vuelve a aparecer en todos lados, con `EmailLog` confirmando el envío.
- **Bloque 63 (bug real reportado en vivo):** login del admin que se cerraba solo apenas entrando al panel, justo después de que una sesión anterior hubiera expirado por inactividad ("recargar la página lo arregla, pero no debería pasar esto — revisa si es un error de tokens"). Causa raíz: `clearLocalSession()` (`AuthContext.jsx`) borraba `accessToken`/`refreshToken` pero nunca `zeudin_last_activity` — el siguiente login heredaba esa marca de tiempo vieja (ya con 35+ minutos de "antigüedad"), y el bootstrap del efecto de inactividad solo escribe una marca nueva si la clave está *completamente ausente*, no si simplemente está vieja. El primer tick del intervalo (≈1 segundo después de entrar al panel) la leía como "ya pasaron 35 minutos sin actividad" y disparaba el cierre de sesión real casi al instante. Fix de dos partes: `clearLocalSession()` ahora también borra `zeudin_last_activity`, y `login`/`verifyTwoFactor`/`verifyRegistration` sellan explícitamente "actividad ahora" en el momento exacto en que arranca la sesión nueva, sin depender de que esa clave esté ausente.
- **Bloque 64 (bug real reportado en vivo + pedido explícito):** el badge de verificación aparecía aunque el panel dijera "Sin verificar todavía". Confirmado en la propia base de datos: 5 de 12 tiendas sembradas tenían `Vendor.isVerified=true` sin verificación real completa — artefacto de los datos de siembra, no un bug de código en vivo (los 3 únicos lugares que escribían `isVerified=true` ya lo hacían siempre junto con el estado real, en la misma transacción), pero el diseño en sí — un booleano guardado APARTE del estado real — hacía posible esa desincronía. Fuente única de verdad nueva: `Vendor.verificationStatus` (enum `NOT_STARTED|PENDING_DOCS|IN_REVIEW|PENDING_PAYMENT|VERIFIED|PAYMENT_FAILED|SUSPENDED|REJECTED`, reemplaza el viejo `VerificationRequest.status` de 4 valores + el booleano `Vendor.isVerified`, eliminado de la tabla de verdad). Servicio central `vendorVerification.service.js`: `transitionVendorVerification()` es el ÚNICO punto que cambia el estado (transacción + `VerificationStatusLog` de auditoría con quién/cuándo/por qué + notificación), reemplaza los ~6 lugares que antes tocaban el campo directo; `withComputedVendorFields()` calcula `isVerified` fresco en cada respuesta de la API desde `verificationStatus` — nunca guardado aparte, así que los ~23 componentes de frontend que ya leían `vendor.isVerified` siguen funcionando igual, pero es estructuralmente imposible que vuelva a desincronizarse. Barrido de los 13 controllers backend que filtraban/leían `isVerified` directo. Cobro recurrente real: Stripe pasa de `mode:"payment"` (cobro único, nunca vencía — decisión "Opción A" del Bloque 46, reemplazada a propósito) a `mode:"subscription"`, con 3 webhooks nuevos (`invoice.payment_succeeded`, `invoice.payment_failed` → `PAYMENT_FAILED`, `customer.subscription.deleted` → `SUSPENDED`); CUP también pasa a recurrente (`nextPaymentDueDate`, 30 días), con cron nuevo (`verificationPayment.job.js`, segundo cron del proyecto) que recuerda 7 días antes y vence a `PAYMENT_FAILED` si nadie renueva — datos bancarios (cuenta/monto/instrucciones) ahora editables desde "Suscripciones Business" en vez de hardcodeados en el frontend. Nueva regla de visibilidad, **independiente** de la verificación: una tienda con 0 productos publicados no aparece en home/búsqueda/categorías (verificada o no, Business o Regular) — acceder por URL directa muestra "Tienda aún no disponible" en vez del perfil completo o un catálogo vacío. Admin: `AdminVerifications.jsx` reorganizado en 5 pestañas por estado (agrupa PENDING_DOCS+IN_REVIEW y PAYMENT_FAILED+SUSPENDED), nueva acción "Abrir para revisar" (marca qué admin está mirando cada solicitud), botón de aprobar deshabilitado si faltan documentos. Migración de datos real: las 5 tiendas sembradas con la desincronía se corrigieron a su estado real (perdieron badge/Business); las 3 verificadas de verdad migraron con 7 días de plazo para el nuevo cobro recurrente. Verificado en vivo de punta a punta (fixture propio): ciclo completo NOT_STARTED→PENDING_DOCS→IN_REVIEW→PENDING_PAYMENT→VERIFIED→PAYMENT_FAILED (cron)→VERIFIED (renovación)→PAYMENT_FAILED/VERIFIED/SUSPENDED (webhooks de Stripe simulados a mano), confirmando que el badge aparece/desaparece de inmediato en cada transición y que la regla de visibilidad oculta tiendas sin catálogo. **Pendiente:** el admin de Stripe todavía tiene que agregar `invoice.payment_succeeded`/`invoice.payment_failed`/`customer.subscription.deleted` a los eventos suscritos del webhook en su dashboard (antes solo hacía falta `checkout.session.completed`); un pago con tarjeta que falla (`PAYMENT_FAILED`, no `SUSPENDED`) no tiene todavía un botón de autoservicio para actualizar el método de pago — Stripe reintenta solo, y si el vendedor necesita actualizar la tarjeta antes de que la suscripción se cancele, por ahora tiene que contactar al equipo (no se integró el Billing Portal de Stripe en este bloque).
- **Bloque 65 (pedido explícito):** moneda operativa ÚNICA por tienda — antes cada PRODUCTO elegía su propia moneda libremente (`Product.currency`, CUP/USD/EUR, Bloque 52), y ya había un caso real mezclado en la base de datos (Sabor Criollo: 4 productos en CUP, 1 en USD). Se agrega `MXN` al enum (reusado, `Vendor.currency` y `Product.currency` comparten el mismo tipo) y `Vendor.currency @default(CUP)` — moneda única elegida obligatoriamente en el registro (`VendorOnboarding.jsx`, mismo patrón que "tipo de negocio"), editable después desde `VendorSettings.jsx` con un aviso explícito de que cambiarla **no convierte** los precios ya cargados, solo la etiqueta. `SiteSettings.availableCurrencies` (nuevo, default las 4) — de qué conjunto puede elegir una tienda, administrable sin redeploy desde una tarjeta nueva en "Marca de la plataforma" (mismo patrón que "Métodos de pago de producto"), nunca puede quedar vacío. El producto deja de tener moneda propia elegible — `createProduct`/`updateProduct`/`updateAdminProduct` la fuerzan siempre a `vendor.currency` server-side (nunca un valor mandado por el cliente, ni el propio selector por producto sigue existiendo en `VendorProducts.jsx`/`AdminProducts.jsx` — ahora es un texto fijo junto al precio). Esto además re-normaliza solo, sin ninguna migración de datos: un producto viejo con una moneda distinta a la de su tienda se corrige la próxima vez que alguien lo edite y guarde. Distinto a propósito de `Vendor.acceptedCurrencies` (Bloque 47, informal/múltiple, "qué coordinás con el cliente") — conviven con etiquetas claramente distintas ("Moneda oficial de tu tienda" vs. "Monedas que aceptas"). Migración de datos: los 12 vendedores existentes quedaron en CUP (11 ya lo eran; Sabor Criollo también, era su mayoría) — su producto en USD **no se normalizó en silencio** (decisión confirmada con el usuario): recibió un mensaje real por el chat admin↔vendedor (Bloque 15) explicando que ese producto quedó con otra moneda y que lo revise. Verificado en vivo de punta a punta: registro sin moneda rechaza, moneda fuera de `availableCurrencies` rechaza, producto nuevo hereda la moneda de la tienda sin importar qué mande el cliente, cambiar la moneda no toca productos viejos pero sí el próximo que se edite, y el admin no puede desactivar todas las monedas ni dejar operar con una ya desactivada.
- **Bloque 66 (batch grande, 12 pedidos explícitos reportados en vivo):** **(1) Carrito con tallas** — `AddToCartControl.jsx` (tarjetas de producto) redirigía SIEMPRE a la ficha del producto para un producto con tallas (`sizes.length > 0`) y además nunca detectaba que ya estaba en el carrito (`items.find` hardcodeaba `!i.size`). Ahora la talla se elige con pills compactas directo en la tarjeta (mismo look que `Product.jsx`, sin redirigir), y el control de cantidad/agregar queda correctamente keyeado por `productId+size` — `CartContext.jsx` ya soportaba esto desde el Bloque 52, no necesitó cambios. **(2) Catálogo de etiquetas de producto** — `Product.badge` pasa de texto libre a una selección de un catálogo administrable (`SiteSettings.availableProductBadges`, tarjeta nueva "Etiquetas de producto" en `AdminBranding.jsx`, mismo patrón que métodos de pago/monedas). "Nuevo" es un valor fijo del sistema (nunca vive en el catálogo, no se puede desactivar): se fuerza siempre al crear un producto y se vence solo — expiración perezosa (`expireNewBadges()`, mismo criterio sin-cron que `expireStaleStoreOffers`) contada desde `Product.activatedAt` (nuevo — se setea recién con la 1ª foto real, no en `createdAt`) más `SiteSettings.newBadgeDurationDays` (default 14). Se suma "Más vendido" como indicador COMPUTADO aparte (`attachBestSellerFlag()`, top 1 producto por unidades vendidas en 30 días **por tienda**, nunca limitado a lo que esté en pantalla) — se muestra en `ProductCard.jsx` sin tocar `badge`. **(3) Validaciones de producto** — `categoryId` pasa a obligatorio (antes opcional); el modal de `VendorProducts.jsx` tenía un bug real: nunca se cerraba solo tras editar y guardar con éxito (`onClose()` faltante en el `onSuccess` de `save`) — ahora se cierra solo si es una edición y el producto ya tiene ≥1 foto; para un producto nuevo, el botón pasa a decir "Listo" y avisa en vez de cerrar si todavía no subió ninguna foto. **(4) Ofertas de tienda solo para verificadas** — `createStoreOffer` no tenía ningún gate de verificación (a diferencia de `createOffer`, que sí); ahora exige `verificationStatus:"VERIFIED"`, con el mismo tratamiento de UI (botón deshabilitado + EmptyState) que ya usaba `VendorOffers.jsx`. **(5) Fuga de seguridad real cerrada:** `getVendorBySlug` incluía `tables` (con `qrToken` real) y `Store.jsx` armaba un link directo "Ver menú →" a `/mesa/:qrToken` — cualquier visitante llegaba al pedido de mesa sin escanear ningún QR físico. Se quitó el include y el bloque de UI enteros; el menú de mesa solo se alcanza vía QR real o desde `VendorTables.jsx` del propio vendedor. **(6) Verificación reforzada** — nuevo enum `IdDocumentType` (carné/pasaporte/carné de extranjería, elegible en el formulario); datos legales nuevos y PRIVADOS (`Vendor.companyTaxId`, `registrationCountryId`, `legalProvinceId`, `legalMunicipalityId` — nunca expuestos en `getVendorBySlug`, mismo criterio que `ownerIdNumber`/`companyAddress`), exigidos desde el ARRANQUE del trámite junto con los documentos (decisión confirmada con el usuario, no en una fase posterior): el gate de `submitVerification` pide ID fiscal de la empresa O el ID del propietario (alcanza con uno), país/provincia de registro, y que la descripción pública de la tienda ya tenga contenido real (≥30 caracteres). Se reordenó `VendorVerification.jsx` — "Al verificarte obtienes" + comparación de planes bajó de estar al final (después de los formularios de pago) a quedar inmediatamente debajo de "Tu plan incluye". Bug real de la tarjeta de admin corregido: `PENDING_DOCS` mostraba documentos Y los botones Aprobar/Rechazar exactamente igual que `IN_REVIEW`, así que "Abrir para revisar" no parecía hacer nada — ahora `PENDING_DOCS` muestra SOLO ese botón, documentos/acciones recién aparecen al pasar a `IN_REVIEW`. Confirmado con el usuario y sin cambios de código: la selfie ya fuerza cámara frontal con una animación real de escaneo (`CameraCapture.jsx`), y el enfoque actual de Stripe (Checkout real + webhooks firmados) ya es la única forma correcta de confirmar un pago real sin confiar en "el cliente dice que pagó" — no hacía falta simplificarlo. **(7) Cobro CUP simplificado + avisos al admin** — el comprobante de pago deja de ser obligatorio (decisión confirmada): el vendedor reclama el pago con el botón "Ya pagué" (`VerificationRequest.paymentClaimedAt`, nuevo) con o sin archivo adjunto; el admin ya no ve el botón de confirmar hasta que `paymentClaimedAt` esté seteado. Nuevo: el admin recibe un correo inmediato (`sendAdminDirectEmail`, reusando la infraestructura del Bloque 47) en los 2 puntos que antes no avisaban a nadie — documentos nuevos enviados (`submitVerification`) y pago CUP reclamado (`uploadMyPaymentProof`). **(8) Cancelación diferida de suscripción** — cancelar ya NO revoca el acceso de inmediato: `Vendor.cancelAtPeriodEnd` (nuevo) registra la intención, Stripe recibe `cancel_at_period_end:true` (nativo, reemplaza la cancelación inmediata solo para tiendas ya `VERIFIED` — una solicitud todavía en documentos/pago sin llegar a verificarse sigue cancelándose de inmediato, no hay nada que "diferir") y sigue facturando/vigente hasta el final del ciclo ya pagado; recién ahí `customer.subscription.deleted` revoca de verdad. El cron `verificationPayment.job.js` ganó un 3er paso (corre primero, antes del vencimiento por falta de pago) para el caso CUP: vendedor con `cancelAtPeriodEnd:true` y `nextPaymentDueDate` ya vencida → transiciona a `NOT_STARTED` recién ahí. **(9) Limpieza:** se quitó la sección "Monedas que aceptas" de `VendorSettings.jsx` (redundante desde el Bloque 65 — la moneda operativa de la tienda ya es una sola) — la columna `Vendor.acceptedCurrencies` queda intacta en la base, solo dejó de editarse desde ahí. Verificado de punta a punta con smoke tests (Node, contra la DB real): `categoryId` obligatorio, catálogo de badges, expiración perezosa de "Nuevo", "Más vendido" computado, `getVendorBySlug` sin `tables` ni campos privados, `createStoreOffer` rechazando tienda no verificada, y `submitVerification` rechazando sin ID fiscal/descripción — más `vite build` limpio del frontend completo.
- **Bloque 67 (pedido explícito):** "Mejorar con IA" en Campañas (Admin) + personalización real de campañas de correo. El botón de IA ahora también vive en el campo "Mensaje" de `AdminCampaigns.jsx` — nuevo `kind:"campaign"` en `generateDescription()` (`aiPrompts.js`), sin `vendorName`/`productName` de por medio (a diferencia de product/store/offer, lo pide un ADMIN sin tienda propia — `generateProductOrStoreDescription` ahora salta `resolveMyVendor` para este kind puntual, que hubiera tirado 404 "No tienes una tienda registrada" antes de este cambio). De paso, pedido explícito de que "los resúmenes de la IA no sean tan cortos": los prompts de product/store pasan de 2-4 a 3-5 oraciones y el de offer de 1-2 a 2-3, los tres con instrucción explícita de tono "moderno y descriptivo" en vez de telegráfico. Personalización de campañas: `Campaign.imageUrl`/`ctaLabel`/`ctaUrl` (nuevos) — banner opcional arriba del mensaje (archivo subido a `/uploads/site` o link externo, mismo criterio de `assertImageLinkAllowed`/`allowProductImageLinks` que store-offers) + botón de llamado a la acción opcional (label+url van siempre juntos, nunca uno solo). Gestión completa del historial — antes `AdminCampaigns.jsx` solo podía crear y listar, sin ninguna forma de tocar una campaña ya existente: se agregan `PATCH /admin/campaigns/:id` (edita asunto/mensaje/segmento/imagen/botón sin enviar nada), `POST /admin/campaigns/:id/resend` (reenvío real — crea un envío NUEVO con su propio `sentAt`/`sentCount`, la campaña original queda intacta en el historial, mismo criterio que una herramienta de email marketing real) y `DELETE /admin/campaigns/:id` (borra el registro del historial — nunca "desenvía" un correo ya entregado, eso es imposible). Reenviar/eliminar piden confirmación (`ConfirmModal`, mismo patrón que el resto del admin) antes de ejecutar, por ser acciones reales/difíciles de revertir. Verificado con smoke tests (Node, contra la DB real) cuidando de NO disparar ningún envío real de prueba (la integración de Resend está activa en esta base): se probaron las rutas de validación de `createCampaign` (rechaza botón CTA incompleto y segmento inválido ANTES de llegar a Resend) y el ciclo completo de `updateCampaign`/`deleteCampaign` contra una fila de prueba insertada aparte — más `vite build` limpio del frontend. **Pendiente (bug real reportado en vivo, no de este bloque — preexistente en TODO correo con imágenes/logo, recién visible al abrir un correo real en un teléfono):** las imágenes salen rotas/vacías en la bandeja de entrada real (confirmado con una campaña real enviada por Resend y abierta en un teléfono). Causa raíz: `backend/.env` no tiene `BACKEND_URL` seteada, así que `resolveAssetUrl()` (`templates/_shared.js`) arma cualquier imagen relativa (logo de la plataforma, banner de campaña, fotos de producto embebidas, etc.) con el default `http://localhost:${PORT}` (`config/env.js`) — una URL que solo existe en la máquina de desarrollo, inalcanzable desde cualquier bandeja de entrada real. Decisión confirmada con el usuario: se deja pendiente a propósito hasta que el backend tenga un despliegue público real (no se arma un túnel temporal tipo ngrok solo para probar) — cuando eso pase, hay que setear `BACKEND_URL` en el `.env` de producción al dominio público real del backend, y ese mismo día conviene reenviar/probar de nuevo una campaña con imagen para confirmar que carga. (El aviso de "Cargar imágenes externas" que también se ve en el correo es aparte y normal — un bloqueo de privacidad del cliente de correo, no un bug, pasa con cualquier remitente.)
- **Bloque 68 (pedido explícito):** ofertas de tienda reubicadas y restyleadas, más rediseño completo del flujo de "a dónde llega el pedido". **(1) Ofertas de tienda** — la sección "Ofertas" de `Store.jsx` (StoreOffer, distinta de la del Home) se movió de estar debajo de reseñas/países de entrega a quedar inmediatamente ARRIBA de "Productos", justo después del banner. La tarjeta (`StoreOfferCard`) se restyleó para usar el mismo lenguaje visual que `OfferCard` del Home (`OffersSlider.jsx`): imagen a sangre completa con degradado `from-black/85` desde abajo y título/descripción superpuestos en blanco, en vez de un panel blanco separado debajo de la imagen — el botón de copiar código queda aparte (necesita su propio área táctil, no puede ir superpuesto). **(2) Destino de pedidos — 3ra opción real:** `Vendor.orderDestination` (enum `WHATSAPP|PANEL`) gana un tercer valor, `BOTH` (WhatsApp + panel a la vez) — pero el cambio de fondo es más profundo que agregar un valor: antes, para una tienda `WHATSAPP`, el catálogo entero (`Product.jsx`, `Cart.jsx`, `CartDrawer.jsx`) ofrecía un atajo que abría `wa.me` directo, saltándose el carrito/checkout por completo — el cliente podía pedir SIN escribir su nombre, teléfono ni dirección, y ese pedido nunca creaba un `Order` real (nunca llegaba al panel del vendedor). Pedido explícito del usuario: el cliente SIEMPRE debe completar sus datos, sin importar el destino elegido. Se eliminaron los 3 atajos que saltaban el formulario; ahora todo pedido pasa por `Checkout.jsx` (que YA exigía el formulario completo — eso no cambió) y crea SIEMPRE un `Order` real, sin importar `orderDestination`. Ese campo pasa a decidir únicamente qué le ofrece la pantalla de CONFIRMACIÓN al cliente después de pedir: `WHATSAPP` → solo el botón de enviarlo por WhatsApp; `PANEL` → solo el aviso de que quedó en el panel de la tienda; `BOTH` → los dos bloques a la vez (ya no son mutuamente excluyentes). El botón del carrito (`Cart.jsx`/`CartDrawer.jsx`) también refleja la elección — verde estilo WhatsApp con ícono para `WHATSAPP`/`BOTH`, color neutro para `PANEL` — pero los 3 casos llevan al mismo `/checkout`, nunca a un atajo. **(3) Vendor.orderDestination por fin editable** — sorpresa encontrada durante la investigación: el campo ya existía en el backend/DB desde hace tiempo (fijo según `isRestaurant` al registrarse) pero `VendorSettings.jsx` nunca tuvo ningún control para cambiarlo — se agrega la tarjeta "Cómo quieres recibir tus pedidos" con las 3 opciones como tarjetas seleccionables, usando el mismo `form`/mutación ya existente (el campo ya viajaba en el payload de guardado, solo le faltaba UI). **(4) Mensaje de WhatsApp del pedido, rediseñado** — antes era una sola línea sin datos del cliente (`"¡Hola! Acabo de hacer el pedido X en Y."`); ahora `buildOrderWhatsAppText()` (`Checkout.jsx`) arma un mensaje organizado por secciones (productos, total, datos del cliente, método de pago) separadas con líneas de guiones repetidos — nunca una tabla de ancho fijo, para que se lea bien en cualquier ancho de pantalla dentro de WhatsApp — usando los datos REALES que el cliente acaba de completar en el formulario (nombre, teléfono, dirección), nunca inventados. **Dos bugs reales preexistentes encontrados y corregidos de paso** (no eran el pedido, pero estaban en el mismo código): la pantalla de confirmación de `Checkout.jsx` linkeaba "Volver a la tienda" a `/v/:slug` (ruta que nunca existió — la tienda vive en `/tienda/:slug`) y además usaba `vendorSlug` del contexto del carrito, que queda `undefined` justo después de `clearCart()`; y el estado de "carrito vacío" linkeaba a `/stores` (tampoco existe — es `/tiendas`). Los dos se corrigieron capturando `vendorSlug` en `confirmedInfo` ANTES de limpiar el carrito y apuntando a las rutas reales. Verificado con smoke tests (Node, contra la DB real): el enum `OrderDestination` tiene los 3 valores en la base de Postgres de verdad, y un vendor sembrado puede guardar y leer `BOTH` de punta a punta (restaurado a su valor original al terminar) — más `vite build` limpio del frontend completo.
- **Bloque 69 (pedido explícito):** reglas de moderación de reseñas + reseñas de producto (antes solo de tienda, de solo lectura). **(1) Rate limit** — un mismo usuario logueado ya no puede dejar más de 1 comentario por día en la MISMA tienda (`REVIEW_DEDUP_HOURS = 24` en `reviews.controller.js`, mismo idioma que `PRODUCT_REQUEST_DEDUP_HOURS` de products.controller.js) — se cuenta por `vendorId+userId`, sin importar si el comentario es general o sobre un producto puntual de esa tienda. **(2) Reseñas de PRODUCTO, de punta a punta** — `Product.jsx` mostraba `product.reviews` de solo lectura (nunca hubo formulario para publicar una) pese a que el modelo `Review.productId` ya existía desde el Bloque 22; se agregó el mismo composer que `Store.jsx` (estrella opcional, fotos solo en tiendas verificadas, gate de login con retorno a `#resenas`), mandando `productId` además de `vendorId`. **Bug real encontrado y corregido de paso:** `getProductBySlug` no filtraba `isHidden:false` ni en el include de `reviews` ni en el `ratingAgg` — un comentario oculto por el admin (o recién oculto por un reporte, ver más abajo) seguía apareciendo en la ficha del producto; `getVendorBySlug` sí lo filtraba correctamente desde el Bloque 22, a este endpoint se le había pasado por alto. **(3) Reportar un comentario** — nuevo `POST /reviews/:id/report`, un solo endpoint usado desde 2 lugares: el panel de vendedor (`VendorReviews.jsx`, sobre reseñas de su propia tienda) y la cuenta de cualquier cliente logueado (botón "Reportar" agregado directo en `ReviewsMarquee.jsx`, el componente compartido entre `Store.jsx`/`Product.jsx` — nunca sobre tu propio comentario). Reportar oculta la reseña de inmediato (`isHidden:true`, `reportStatus:"REPORTED"`) y notifica al admin por correo al instante (`notifyAdminActionNeeded`, ver el punto 5). Nuevo enum `ReviewReportStatus` (`REPORTED|KEPT|SUSPENDED`) + campos `reportedById`/`reportReason`/`reportedAt`/`reportResolvedById`/`reportResolvedAt` en `Review`. **(4) Resolución del admin** — nuevo `PATCH /admin/reviews/:id/resolve-report` (`AdminReviews.jsx`, botones "Mantener"/"Suspender" que solo aparecen mientras `reportStatus:"REPORTED"`, con el motivo y quién reportó a la vista): "Mantener" vuelve a mostrar el comentario y lo SELLA contra nuevos reportes (`reportStatus:"KEPT"` — `reportReview` rechaza cualquier intento futuro con 409, evita que un vendedor reintente hasta lograr esconder algo que el admin ya revisó); "Suspender" lo deja oculto para siempre, y desde ahí la única acción posible es `deleteReview` (ya existente, exclusivo del admin — el vendedor nunca recupera la capacidad de tocarlo). `listMyReviews` (vendor) dejó de filtrar `isHidden:false` — el vendedor necesita ver el estado real de lo que reportó (oculto esperando revisión, mantenido de nuevo visible, o suspendido), la UI lo distingue con opacidad + una pastilla de estado. **(5) Notificación al admin, extraída a un lib compartido** — `notifyAdminActionNeeded` (antes vivía duplicado localmente dentro de `verification.controller.js`, Bloque 66) se movió a `backend/src/lib/adminNotify.js`; `reviews.controller.js` es el segundo consumidor real, evitando una tercera copia futura. Verificado con smoke tests (Node, contra la DB real, con filas de prueba creadas y borradas por el propio script): rate limit de 1x/día, no-auto-reporte, reporte real disparando el correo al admin (de verdad, bajo riesgo — un solo email a la bandeja del propio admin, no una campaña masiva), rechazo de re-reportar mientras está `REPORTED` o después de `KEPT`, el ciclo completo `keep`/`suspend`, y confirmación de que `getProductBySlug` ya no devuelve reseñas ocultas — más `vite build` limpio del frontend completo (incluyó corregir un error de compilación preexistente y no relacionado en `VendorProducts.jsx`, una declaración duplicada de `siteSettings` que rompía el build entero).
- **Bloque 70 (bugs reales reportados en vivo + pedido explícito):** tres bugs de conexión rota + historial de actividad nuevo. **(1) Correo de cuenta eliminada bloqueaba reutilización** — `deleteVendor`/`deleteCustomer` (soft-delete, nunca borran la fila — necesaria para pedidos/reseñas históricas) nunca tocaban `User.email`, así que ese correo quedaba tomado PARA SIEMPRE por una cuenta "eliminada" invisible en toda la UI — el admin no podía reutilizarlo ni dándose cuenta de por qué. Fix: ambas funciones ahora renombran el email a `deleted+<userId>@zeudin.invalid` en el mismo momento del soft-delete, liberando la dirección real; se corrió además un script único (ejecutado y borrado en esta sesión) que liberó la cuenta ya eliminada que tenía atascado el correo del propio admin ahora mismo. **(2) Mensajes del panel de vendedor bloqueados aunque la tienda esté verificada** — `VendorChat.jsx` chequeaba `vendor.verification.status === "APPROVED"`, un campo que ya no existe (el estado real vive en `Vendor.verificationStatus` desde el Bloque 64) y un valor que tampoco es parte del enum — la condición daba `false` siempre, para cualquier tienda, verificada o no. Fix de una línea: pasa a leer `vendor.isVerified`, el mismo campo que ya usan correctamente los otros ~6 puntos del panel que gatean por verificación (confirmado con un barrido completo de `frontend/src/pages/vendor/*.jsx` y `admin/*.jsx` — este era el único roto). **(3) Campanita de notificaciones del admin, ampliada** — `listAdminNotifications` solo agregaba 4 señales (verificaciones pendientes, sugerencias nuevas, errores sin resolver, mensajes de vendedor sin leer); un pago CUP reclamado ("Ya pagué") y un comentario recién reportado ya disparaban un correo (Bloques 66/69) pero eran invisibles en la campanita del panel — se agregaron ambos como ítems reales con su link directo. **(4) Historial de actividad, nuevo** — pedido explícito de "una sección dedicada a todas las acciones de vendedores y de clientes... para calcular quién más usa la plataforma por día/semana/mes". Nuevo modelo `ActivityLog` (`actorId`, `actorRole`, `vendorId` opcional, `action` de texto libre — nunca un enum, para no necesitar una migración cada vez que se registre un tipo de acción nuevo — `description` ya en español listo para mostrar, `meta` JSON) + helper `logActivity()` (`backend/src/lib/activityLog.js`, best-effort, mismo criterio que `notifyAdminActionNeeded` — nunca rompe la acción real si falla). Instrumentado en 13 acciones ya existentes: crear producto, hacer un pedido, publicar reseña, responder reseña, reportar reseña, crear oferta/oferta de tienda/código de descuento/mesa, enviar verificación, reclamar pago CUP, actualizar configuración de tienda, y enviar una sugerencia (deliberadamente NO se instrumentó cada edición/toggle menor — el pedido era un registro de acciones significativas, no un log de cada click). Nueva sección `/admin/actividad` (`AdminActivityLog.jsx`): selector Hoy/Semana/Mes que alimenta dos gráficos de barra (mismo estilo sin librería que "Tiendas por provincia" del dashboard — `pct` precalculado server-side) de tiendas y clientes con más actividad, más el historial crudo filtrable por rol debajo. Endpoints nuevos `GET /admin/activity-log` y `GET /admin/activity-log/stats` (`groupBy` + `_count` sobre `ActivityLog`, mismo criterio de agregación que ya usa `getDashboard`). Verificado con smoke tests (Node, contra la DB real, filas de prueba creadas y borradas por el propio script): `logActivity` escribe filas reales para vendedor y cliente, el historial filtra por rol correctamente, las estadísticas reflejan la actividad de prueba en el período "día", y la campanita de notificaciones sigue respondiendo bien con los 2 nuevos orígenes agregados — más `vite build` limpio del frontend completo.
- **Bloque 71 (pedido explícito):** cambiar el correo de una cuenta (tienda, cliente o admin) deja de ser directo — ahora exige un código de verificación (letras+números) mandado al correo VIEJO antes de aplicar el cambio, con sus reglas y tokens de sesión correctos. Nuevo flujo de 2 pasos en `auth.controller.js` (genérico para los 3 roles, mismo criterio que el reset de contraseña): `POST /auth/me/email/request-code` (valida la contraseña actual, genera un código de 8 caracteres alfanumérico — alfabeto sin `0/O/1/I` para evitar ambigüedad al transcribir a mano — lo hashea, guarda `User.pendingEmail` + `emailChangeCodeHash` + `emailChangeCodeExpiresAt`, y lo manda por correo al email ACTUAL de la cuenta, nunca al nuevo) y `POST /auth/me/email/confirm` (valida el código, recién ahí escribe `email = pendingEmail`). Al confirmar, "el usuario viejo debe dejar de aparecer en el sistema para evitar que siga teniendo acceso": se revoca TODA sesión activa y se borran todos los dispositivos de confianza de esa cuenta (mismo mecanismo que "Cerrar sesión en todos los dispositivos", Bloque 60) — como `authenticate` valida la sesión contra la DB en cada request, el efecto es inmediato, incluida la propia pestaña que acaba de confirmar el cambio (queda forzada a loguearse de nuevo, ya con el correo nuevo). Nuevo componente compartido `ChangeEmailModal.jsx` (2 pasos: pedir código → confirmar, con opción de reenviar) reemplaza el modal de 1 paso que antes vivía duplicado en `AdminProfile.jsx`/`VendorProfile.jsx`. **Hueco real de seguridad cerrado de paso:** `CustomerPanel.jsx` dejaba cambiar el correo del cliente junto con el resto del perfil (`PATCH /customers/me`) sin pedir contraseña ni verificación de ningún tipo — se le quitó `email` a ese endpoint por completo y se le agregó al cliente la misma tarjeta "Correo de la cuenta" + `ChangeEmailModal` que ya tenían tienda/admin. Verificado con smoke tests (Node, contra la DB real, con una cuenta de prueba creada y borrada por el propio script): contraseña incorrecta rechaza, el código se genera y guarda bien, un código incorrecto rechaza, el código correcto aplica el cambio Y revoca sesión/dispositivo de confianza reales, el código ya usado no se puede reusar, y `customers.controller.updateMe` ignora silenciosamente un `email` en el body — más `vite build` limpio del frontend completo.
- **Bloque 72 (pedido explícito):** archivo permanente de la documentación que llevó a cada tienda a verificarse — hora, fecha, datos personales y del negocio, fotos del responsable y de la identificación, todo archivado y consultable/descargable después. Nuevo modelo `VerificationArchive`: una fila nueva ("rama") por CADA verificación exitosa, creada sola por `transitionVendorVerification()` (`vendorVerification.service.js`) — nunca a mano, nunca se reemplaza ni se pisa. La señal exacta de "cuándo archivar" fue la parte no trivial: de los 3 lugares donde una tienda puede llegar a `VERIFIED`, solo uno representa documentación recién aprobada de verdad (`fromStatus === "PENDING_PAYMENT"`, el estado que sigue justo después de que un admin aprueba documentos recién enviados) — las otras 2 transiciones a VERIFIED son renovaciones que NO reenvían documentos (`SUSPENDED -> VERIFIED` al pagar de nuevo sin rehacer KYC, `PAYMENT_FAILED -> VERIFIED` al recuperarse un cobro de Stripe) y correctamente NO generan una rama duplicada de la misma documentación ya archivada — así, cuando una tienda pierde la verificación del todo y más adelante paga y reenvía TODA la documentación de nuevo (el otro camino, vía `submitVerification`), esa segunda verificación exitosa sí crea una rama nueva, y la vieja queda intacta para siempre, exactamente como se pidió. Cada rama es un snapshot independiente — guarda nombres de país/provincia/municipio ya resueltos (no FKs) para seguir siendo legible aunque esas tablas de referencia cambien más adelante. Reglas de edición/borrado: editable (corrige datos mal cargados, nunca las fotos — esas son la evidencia real), pero solo eliminable a mano por un admin y SOLO si la tienda YA NO está verificada hoy (`deleteVerificationArchive` chequea el estado ACTUAL de la tienda, no el de la rama) — nunca un borrado automático, no hay ningún cron que toque esta tabla. Nueva UI en `AdminVerifications.jsx`: un botón de archivo (ícono) en cada tarjeta de cualquier pestaña abre un modal con todas las ramas de esa tienda, cada una editable inline y con sus 2-3 fotos (selfie, documento frente, documento dorso si existe) vía un endpoint de archivo propio (`GET /admin/verification-archive/:id/file/:type`, mismo mecanismo de blob autenticado que ya usaba `PrivateDocument.jsx` para la verificación en curso, generalizado con un prop `endpoint` para poder apuntar a una rama archivada en vez de a la `VerificationRequest` vigente) y un botón real de descarga (no solo ver). Verificado con smoke tests (Node, contra la DB real, con una tienda/vendedor de prueba creados y borrados por el propio script): la transición `PENDING_PAYMENT -> VERIFIED` archiva una rama con el snapshot correcto; simular una renovación (`PAYMENT_FAILED -> VERIFIED`) NO crea una rama duplicada; listar, editar y el gate de borrado (rechaza mientras sigue verificada, permite una vez que deja de estarlo) funcionan de punta a punta — más `vite build` limpio del frontend completo.
- **Bloque 73 (pedido explícito):** se quitó el efecto de zoom al pasar el cursor por encima de una imagen (`group-hover:scale-105`/`transition-transform`) en los 4 lugares donde existía — tarjeta de tienda compacta (`CompactStoreCard.jsx`), ofertas/promociones del Home (`OffersSlider.jsx`), fotos adjuntas a una reseña (`ReviewsMarquee.jsx`) y fotos de producto en la tienda (`Store.jsx`). Las imágenes se quedan estáticas al pasar el cursor por encima en todo el sitio.
- **Bloque 74 (bug real reportado en vivo + pedido explícito):** el carrusel infinito de reseñas/comentarios (`ReviewsMarquee.jsx`, compartido por `Store.jsx`/`Product.jsx`) "volvía al inicio" con un salto visible en vez de reciclar el contenido sin cortes. Causa raíz: con menos de 4 reseñas el contenido se triplicaba (`[...reviews, ...reviews, ...reviews]`), pero el keyframe CSS `marquee` (`tailwind.config.js`) siempre anima `translateX(0) -> translateX(-50%)` — matemática pensada para EXACTAMENTE 2 copias (con 2 copias, -50% cae justo en el borde entre la 1ª y la 2ª, así que el reinicio del keyframe es invisible); con 3 copias, -50% cae a mitad de la 2ª copia, un frame visualmente distinto al del inicio → salto real en cada ciclo. Fix: se duplica SIEMPRE exactamente una vez (2 copias totales), sin importar cuántas reseñas haya — así el loop es matemáticamente continuo siempre, sin excepción. De paso, tamaño responsivo pedido explícitamente: la tarjeta de reseña pasa de un ancho fijo de 300px (casi la pantalla completa en un celular chico) a `210px` en mobile, `260px` en `sm:` y `300px` en `md:` en adelante, con el avatar, la foto adjunta y los espaciados internos escalando junto con ella — las fotos adjuntas a un comentario se siguen mostrando siempre que la reseña tenga una, en cualquier tamaño de pantalla, sin ningún cambio de visibilidad. **Ajuste posterior (mismo bloque, pedido explícito de rediseño):** la foto adjunta vivía como una franja angosta pegada al costado del texto (fácil de subestimar, casi invisible en mobile) — la tarjeta pasó de un layout horizontal (texto a la izquierda, foto en una tira vertical a la derecha) a uno vertical apilado por secciones (cabecera con avatar/nombre/fecha/estrellas → insignia de compra verificada → comentario → foto, si tiene, como una miniatura ANCHA de `h-24`/`h-28` a todo el ancho de la tarjeta → respuesta del vendedor, si tiene) — más moderno, cada elemento con su propio espacio, y la foto queda imposible de pasar por alto en cualquier tamaño de pantalla. De paso se verificó que `index.css` compila limpio (`node --check` no aplica a CSS; se corrió directo por el CLI de Tailwind, `npx tailwindcss -i src/index.css -o ...`, sin errores) — los "errores" que se ven en el editor son falsos positivos del linter de CSS plano de VS Code marcando `@tailwind`/`@apply`/`theme()` como desconocidos (no reconoce sintaxis de Tailwind sin la extensión "Tailwind CSS IntelliSense"), no un problema real del archivo.
- **Bloque 75 (bug real reportado en vivo + pedido explícito):** una tienda bloqueada a mano por un admin (`Vendor.isBlocked`) no tenía NINGÚN aviso en su panel de vendedor — el dueño seguía viendo todo con normalidad, sin enterarse ni poder actuar. Causa raíz: el bloqueo sincronizaba `User.isSuspended` (para impedir logins nuevos), pero eso no hace nada contra una sesión YA abierta, y el panel nunca chequeaba `isBlocked` para nada. Se dejó de tocar `isSuspended` al bloquear/desbloquear (eso ahora es responsabilidad exclusiva del soft-delete permanente) y se unificó el bloqueo manual con el mismo mecanismo que ya usaba la suspensión automática por inactividad: `VendorLayout.jsx` detiene al vendedor con una pantalla real (`VendorAccessBlockedGate`, generalizada a partir de la vieja `VendorSuspendedGate`) que explica el motivo y no deja tocar ninguna sección, sin importar si es bloqueo manual o suspensión automática. Nuevo `Vendor.blockReason` — obligatorio al bloquear desde `AdminVendors.jsx` (antes no se pedía ningún motivo). Nuevo botón "Contactar soporte" con WhatsApp real: arma un mensaje prellenado (nombre de la tienda + motivo) hacia el número que el admin cargue en la tarjeta nueva "Número de soporte" (`Admin → Mi perfil → Marca de la plataforma`, campo `SiteSettings.supportWhatsapp`, formato E.164, distinto del `whatsappUrl` que ya existía para el ícono del pie de los correos) — cambiar el número ahí actualiza el botón en todos lados sin tocar código; sin número cargado, cae de respaldo a `/contacto`. También se agregó el mismo llamado a la acción "Mejora tu plan" (glass, ícono de rayo, botón "Ver planes" hacia `/vendedor/verificacion`) en las 3 secciones del panel de vendedor que ya se bloqueaban para tiendas no verificadas (Ofertas, Ofertas de tienda, Mensajes) — antes solo avisaban que la función no estaba disponible, sin ningún llamado a la acción. Verificado con smoke tests (Node, contra la DB real, con tienda/vendedor de prueba creados y borrados por el propio script): bloquear sin motivo rechaza, bloquear con motivo lo guarda sin tocar `isSuspended`, desbloquear limpia el motivo, guardar un número de soporte válido funciona y uno con formato inválido rechaza — más `vite build` limpio del frontend completo.

- **Bloque 76 (4 pedidos explícitos + 1 bug real reportado en vivo):** **(1) Login cruzado entre paneles, bug real** — una cuenta ADMIN podía arrancar el login desde `/vendedor/ingresar` (o viceversa con una cuenta VENDOR desde `/admin/ingresar`): el backend nunca chequeaba el rol, mandaba un código de 2FA real por correo, y el rechazo ("Credenciales inválidas") recién pasaba en el FRONTEND después de completar el código entero — mandaba un correo innecesario y llegaba a emitir tokens reales por un instante para un rol que nunca debía poder entrar por ahí. Fix: nuevo campo `context` ("admin"/"vendor", ausente = `/cuenta` acepta cualquier rol como siempre) que viaja desde `Account.jsx` en cada intento de login/2FA — el backend corta con el mismo mensaje genérico de contraseña incorrecta ANTES de mandar ningún código si el rol no coincide con el formulario usado, así una cuenta con el rol equivocado es indistinguible de una cuenta que no existe. **(2) Tiendas bloqueadas/suspendidas unificadas en una sola sección** — antes "Tiendas suspendidas" (`AdminSuspendedVendors.jsx`) solo mostraba las suspendidas automáticamente por inactividad; las bloqueadas a mano seguían apareciendo en "Tiendas" con un botón "Desbloquear" ahí mismo. Ahora `listVendors` excluye por completo cualquier tienda con `isBlocked:true` o `status:"SUSPENDED"`, y `listSuspendedVendors` junta ambos casos — cada fila muestra su propio tipo/motivo/fecha (nuevo `Vendor.blockedAt`, mismo criterio que `suspendedAt`) y la acción correcta (Desbloquear, sin motivo, vs. Reactivar, con motivo obligatorio y auditoría real vía `VendorStatusLog`) — una tienda solo vuelve a "Tiendas" cuando se resuelve. **(3) "Marca de la plataforma" pasó a vivir dentro de "Mi perfil"** — como pestaña nueva ("Mi cuenta" / "Marca de la plataforma") en vez de una entrada propia del menú lateral; `AdminBranding.jsx` no se tocó por dentro, solo cambió desde dónde se llega a él. **(4) Botón de salir del panel de admin** — mismo tratamiento que ya se le había dado al panel de vendedor: se movió de la parte de abajo del sidebar a la barra superior, al lado de la campanita de notificaciones. Verificado con smoke tests (Node, contra la DB real, con cuentas/tienda de prueba creadas y borradas por el propio script): login cruzado rechaza en ambas direcciones sin mandar 2FA, login normal sigue pidiendo 2FA para admin y para vendedor sin contexto, bloquear una tienda la saca de `listVendors` y la mete en `listSuspendedVendors` con `blockedAt` seteado, y desbloquear la devuelve a `listVendors` — más `vite build` limpio del frontend completo.

- **Bloque 77 (2 pedidos explícitos):** **(1) Tiendas privadas** — nuevo `Vendor.isPrivate` (toggle "Pública"/"Privada" en `VendorSettings.jsx`, viaja con el resto del form/botón "Guardar cambios" de siempre). Una tienda privada sigue funcionando exactamente igual por su link directo — mismo `/tienda/:slug` de siempre, checkout, reseñas, chat, todo — lo único que cambia es que desaparece de TODO punto de descubrimiento público: se auditaron y parchearon los 9 lugares reales que listan tiendas/productos para clientes (`listVendors` — Home Y el catálogo `/tiendas` comparten este mismo endpoint —, los 4 puntos de `search.controller.js` — 2 búsquedas SQL crudas por nombre/trigram de tienda + el `where` de productos del buscador estructurado + el del autocompletado en vivo —, el conteo de `businessCategories.controller.js`, y los 4 puntos de `assistant.controller.js` que usa el chatbot de IA para recomendar productos/tiendas), mientras que `getVendorBySlug` (la página de la tienda en sí) y todo lo que pasa DESPUÉS de encontrarla (pedidos, favoritos, chat, mesas) se dejó sin tocar a propósito — la privacidad es sobre descubrimiento, nunca sobre función. Debajo del toggle, un enlace copiable + un QR REAL y escaneable (librería nueva `qrcode.react` — se encontró de paso que el único "QR" que existía en el proyecto, en `VendorTables.jsx`, era decorativo, un patrón visual sin ninguna codificación real detrás, nunca un QR de verdad) apuntando siempre al mismo `/tienda/:slug` — el texto de arriba ("privada"/"pública") es lo único que cambia según el estado. **(2) Bloqueo de zoom** — se auditó lo que ya existía (Bloque 23): la meta `viewport` (`user-scalable=no, maximum-scale=1.0, minimum-scale=1.0`) y `touch-action: manipulation` en `body` (`index.css`, `@layer base`, global — una sola SPA con un solo `index.html`/`index.css` para admin/vendedor/cliente/público por igual) ya son el máximo bloqueo de zoom táctil/móvil técnicamente posible en la web — no había nada más que agregar ahí, y no se tocó nada para no romper lo que ya andaba bien. **Límite real, no arreglable:** el zoom de ESCRITORIO (Ctrl+scroll, Ctrl+Plus/Minus) es control exclusivo del navegador — ninguna página web, en ningún browser moderno, puede desactivarlo ni interceptarlo (por diseño, es una función de accesibilidad que los navegadores protegen a propósito); cualquier intento de bloquearlo vía JavaScript (`preventDefault` sobre `wheel`+`ctrlKey`, etc.) es un anti-patrón que además rompe la accesibilidad real para gente que sí necesita agrandar la página, así que no se implementó. Verificado con smoke tests (Node, contra la DB real, con tienda/producto de prueba creados y borrados por el propio script): una tienda nueva aparece en `listVendors` por default, marcarla privada la saca de ahí pero `getVendorBySlug` la sigue sirviendo intacta, y volver a marcarla pública la devuelve — más `vite build` limpio del frontend completo (incluida la instalación de `qrcode.react`).
- **Bloque 78 (2 bugs reales reportados en vivo, sobre el zoom del Bloque 77):** **(1) Zoom automático al escribir en un input, mobile** — Safari/Chrome mobile hacen zoom SOLOS apenas se enfoca un `input`/`textarea`/`select` con `font-size` menor a 16px, sin importar `user-scalable=no` ni `touch-action` (ya aplicados) — es un comportamiento propio del focus para legibilidad, no el gesto de zoom, así que esos 2 no lo tocan. Casi todo el sitio usa texto compacto (12-14px) por diseño, en decenas de archivos — en vez de tocar cada input uno por uno, se agregó una media query global (`index.css`, `@media (max-width: 640px)`) que fuerza `font-size: 16px !important` en los 3 selectores (`input`/`textarea`/`select`) SOLO en pantallas chicas, dejando el tamaño compacto intacto en tablet/desktop. **Límite real, ya explicado en el Bloque 77 pero confirmado de nuevo acá:** el zoom por PELLIZCO (pinch) en iOS Safari específicamente ignora `user-scalable=no` desde iOS 10 (decisión de Apple, no arreglable desde el código) — Android sí lo suele respetar. **(2) Salto de scroll al cambiar de página** — el reseteo de scroll a "arriba del todo" en cada cambio de ruta (`RouteLoader.jsx`, Bloque 52) usaba `window.scrollTo(0, 0)` sin más, pero `html` tiene `scroll-smooth` global (Bloque 23) — sin especificar lo contrario, ese `scrollTo` quedaba sujeto al scroll-behavior:smooth del CSS y ANIMABA desde la posición vieja en vez de saltar directo, así que la página nueva se veía "un poco desplazada hacia arriba" en vez de arrancar ya arriba del todo (exactamente el bug reportado). Fix: `window.scrollTo({ top: 0, left: 0, behavior: "instant" })` — `behavior:"instant"` ignora el smooth del CSS puntualmente para este reseteo, sin tocar `scroll-smooth` global (sigue andando bien para anclas dentro de la misma página, ej. `#resenas` en Store.jsx). Verificado con `vite build` limpio del frontend completo.
- **Bloque 79 (bug real reportado en vivo, con captura de un celular real):** el sidebar fijo del panel de vendedor (y, por el mismo motivo, el del admin) se veía cortado abajo en mobile — la tarjeta "Mejora tu plan" quedaba a medio mostrar, como deslizándose fuera de la pantalla. Causa raíz: el `<aside>` usaba `fixed inset-y-0 ... h-screen` — `h-screen` es `100vh`, y en mobile el `vh` de CSS mide MÁS que el área realmente visible (no descuenta la barra de direcciones del navegador, que se superpone/reduce el viewport real) — el sidebar terminaba más alto que la pantalla de verdad, y esa diferencia quedaba literalmente detrás de la interfaz del navegador, invisible. `inset-y-0` por sí solo ya lo hubiera resuelto (fija top:0/bottom:0 al viewport real), pero el `h-screen` explícito pisaba eso. Fix: `h-screen`/`min-h-screen` → `h-dvh`/`min-h-dvh` (unidad de viewport DINÁMICO, sí descuenta la barra del navegador) en los 2 sidebars fijos (`VendorLayout.jsx`, `AdminLayout.jsx`) y, por consistencia, en el resto de los usos de `min-h-screen` del sitio (`PublicLayout.jsx`, `ProtectedRoute.jsx`, `NotFound.jsx`, `Account.jsx`) — nunca hacía falta tocar `CartDrawer.jsx` ni otros overlays, esos ya usaban `inset-y-*`/`inset-0` puro sin ningún `h-screen` de por medio, inmunes al bug por construcción. Se confirmó en el CSS compilado que Tailwind 3.4 sí genera `height:100dvh`/`min-height:100dvh` de verdad (no una clase silenciosamente ignorada). Verificado con `vite build` limpio del frontend completo — solo el menú de secciones (Resumen a Mi perfil) queda con scroll interno, el resto del sidebar (logo, datos de la tienda, tarjeta de abajo) ya no se corta en ningún tamaño de pantalla.
- **Bloque 80 (bug real reportado en vivo, con capturas de un celular real — el `h-dvh` del Bloque 79 no alcanzaba):** el `h-dvh` del bloque anterior arregló el corte, pero el sidebar se veía "romperse"/reacomodar a mitad de gesto al deslizar el contenido de la página, y la tarjeta "Mejora tu plan" quedaba pegada sin ningún margen contra el último ítem del menú en algunas capturas. Causa raíz real: `dvh` no es un valor fijo — es DINÁMICO, se RECALCULA en vivo mientras el navegador oculta/muestra su barra de direcciones durante el scroll (justo lo que hace un navegador mobile al bajar la página). Un `<aside fixed>` con altura en `h-dvh` repintaba/reflowaba en cada paso de esa animación de la barra, dando el efecto de "se separa"/parpadea. Fix real: en mobile (`fixed`) se saca la altura explícita del todo — `inset-y-0` (ya presente) alcanza solo para calzar el sidebar contra el viewport real, sin ningún valor de longitud que recalcular nunca (geometría nativa del navegador vía compositing, no CSS reactivo) — inmune al bug por construcción, no solo mitigado. En desktop (`lg:sticky`, sin barra de navegador que aparezca/desaparezca) se mantiene `lg:h-dvh` explícito porque ahí sí hace falta — `sticky` no deriva altura de `top`/`bottom` como sí hace `fixed`. De paso, se le agregó `mt-3`+`flex-shrink-0` a la tarjeta "Mejora tu plan" (no tenía NINGÚN margen contra el menú de arriba, por eso a veces se veía pegada) para que siempre tenga el mismo respiro visual, sin depender de cuánto ocupe el menú en cada momento. Verificado con `vite build` limpio del frontend completo.
- **Bloque 81 (bug real reportado en vivo, con captura de escritorio — reproducido en vivo con Playwright):** con el sidebar del Bloque 80 ya verificado en un viewport alto (390×844) sin problema, el usuario reportó el mismo tipo de corte pero en una ventana de Chrome de escritorio angosta y de menos alto disponible (probablemente por el escalado de pantalla de Windows) — el menú se veía desbordado, "Mi perfil" desaparecía de la lista y la tarjeta "Mejora tu plan" quedaba cortada en el borde inferior, sin scrollbar real que la alcance. Causa raíz, distinta a la de los Bloques 79/80 (esos eran del `<aside>` contra el viewport; esta es del `<nav>` interno contra el `<aside>`): el `<nav>` (`flex flex-1 flex-col ... overflow-y-auto`, pensado para scrollear internamente y dejar fijos el logo/tarjeta de tienda arriba y "Mejora tu plan" abajo, ver Bloque 79) le faltaba `min-h-0`. Por default un hijo flex tiene `min-height:auto`, lo que le impide encogerse por debajo de la altura de su propio contenido — así que cuando el alto real disponible es menor que el contenido del menú (11 links en vendedor, 22 en admin), `flex-1` no alcanza para forzar el scroll: el `nav` simplemente crece más allá del `<aside>` y arrastra todo lo de abajo fuera de la pantalla, sin nada que hacer scroll de verdad (por eso no había ninguna barra de scroll funcional). Reproducido en vivo con Playwright MCP (login real como Casa Verde, `/vendedor/perfil`, viewport 390×600 — deliberadamente más bajo que el contenido del menú, ~723px, para forzar el caso real) antes y después del fix, confirmando con `nav.scrollHeight` (472px) > `nav.clientHeight` (380px) que el scroll interno ahora sí es real y que la tarjeta queda con `getBoundingClientRect().bottom` dentro del viewport. Fix: `min-h-0` agregado al `<nav>` de `VendorLayout.jsx` y de `AdminLayout.jsx` (el mismo patrón exacto en los dos, el admin con más riesgo por tener el doble de ítems de menú). Verificado con `vite build` limpio del frontend completo + reproducción en vivo con Playwright (antes: overflow real, sin scroll; después: `nav` con scroll interno funcional y "Mejora tu plan" siempre visible dentro del viewport, sin importar cuán bajo sea el alto disponible). **Segundo bug real, mismo bloque, reportado en vivo minutos después con 2 capturas de un celular real (efecto secundario directo del fix anterior, solo en mobile):** al deslizar la sección abierta con el menú lateral desplegado, se abría un hueco vacío entre "Mi perfil" y la tarjeta "Mejora tu plan" — pero solo tras deslizar un poco, nunca al principio, y nunca en un navegador de escritorio. Causa: `flex-1` no solo permite ENCOGERSE (lo necesario, arreglado arriba) sino también CRECER más allá del contenido propio cuando sobra espacio — y en iOS Safari, al hacer scroll la barra de direcciones se colapsa y el viewport visual crece en vivo, estirando el `<aside>` (`inset-y-0` puro, Bloque 80) y con él a este `nav` de `flex-1`, que se agrandaba de más dejando ese espacio de sobra como un hueco vacío ANTES de la tarjeta (los `NavLink` se quedan apilados arriba por `justify-content` default, así que el sobrante cae al final del nav, justo antes de la tarjeta). Fix: se saca `flex-1` del todo (un hijo flex sin esa clase ya es `flex:0 1 auto` por default — no crece, si acaso se encoge), dejando solo `min-h-0` para permitir el encogimiento cuando sí hace falta. Reproducido en vivo con Playwright simulando el crecimiento de viewport (938px, bastante más que los ~723px de contenido real): confirmado que `nav.scrollHeight === nav.clientHeight` (472px, ya no se estira) y que el espacio entre el fondo del nav y el techo de la tarjeta quedó en ~12px (exactamente el `mt-3` de diseño, no un hueco extra) — el sobrante de espacio ahora cae, sin verse, por debajo de la tarjeta, al fondo del `<aside>`. Mismo fix aplicado en los dos archivos. **Tercer ajuste, mismo bloque, pedido explícito de "buscar una manera inteligente y moderna" tras confirmar que el fix anterior solo movió el hueco:** sin `flex-1`, el nav ya no crecía de más, pero el espacio de sobra quedaba SUELTO debajo de la tarjeta (entre "Ver planes" y el borde inferior del `<aside>`) — se veía igual de "roto", solo que del otro lado. Solución con el patrón estándar de sidebar profesional (header fijo + lista scrollable + tarjeta anclada abajo): la tarjeta pasa de `mt-3` a `mt-auto` — el margen automático absorbe TODO el espacio de sobra de la columna flex y ancla la tarjeta al borde inferior real del sidebar siempre, sin importar cuánto contenido tenga el nav ni cuánto viewport sobre; con espacio realmente escaso (nav ya scrolleando) el margen automático se reduce solo a 0, la tarjeta queda pegada al nav sin hueco extra, nunca cortada. Verificado en vivo con Playwright en los 2 extremos: viewport alto (938px, mucho sobrante) — la tarjeta queda anclada al fondo real del `<aside>` (a 22px del borde, el padding de diseño `py-[22px]`, no un bug) con el sobrante ahora concentrado de forma prolija arriba de la tarjeta; viewport bajo (600px, sin sobrante) — el nav scrollea con su propia barra y la tarjeta queda pegada justo debajo, sin overflow ni corte. Verificado con `vite build` limpio del frontend completo. **Cuarto y último ajuste, mismo bloque, con 3 capturas reales (2 celular + 1 escritorio) mostrando que `mt-auto` seguía sin ser suficiente:** en iOS Safari, con la barra de direcciones expandida (el estado normal recién cargada la página, sin haber scrolleado nada todavía) el alto real disponible resultó ser más ajustado de lo esperado, y ahí el margen automático se reducía a 0 — la tarjeta volvía a quedar pegada directo contra "Mi perfil", el mismo síntoma original del Bloque 80 ("se pega"), solo que ahora la causa era el cálculo de `margin:auto` colapsando en vez de la barra del navegador en sí. Se investigaron referencias de sidebars profesionales (patrón "header fijo + lista scrollable + tarjeta anclada abajo" usado en dashboards tipo Notion/Linear/shadcn-ui) y se rediseñó la estructura separando 3 responsabilidades que antes estaban mezcladas en una sola clase: un wrapper nuevo (`min-h-0 flex-1 overflow-y-auto`) es el ÚNICO elemento que se encoge/scrollea; el `nav` volvió a ser simple (sin su propio scroll ni crecimiento); y un separador invisible nuevo entre el nav y la tarjeta (`flex-1 min-h-3`) absorbe todo el espacio de sobra cuando alcanza, pero con un piso de `min-h-3` (12px) que ningún cálculo de flexbox puede reducir más allá — a diferencia de un margen automático, un `min-height` explícito nunca colapsa a 0 aunque el espacio disponible sea exactamente igual al contenido. Verificado en vivo con Playwright en 3 puntos exactos: viewport muy alto (938px) — tarjeta anclada al fondo real, sobrante arriba de ella; viewport en el borde justo de necesitar scroll (650px, medido con `wrapper.scrollHeight > wrapper.clientHeight`) — el gap se mantuvo exactamente en 12px (nunca 0), confirmado con `getBoundingClientRect()`, y el wrapper pasó a scrollable revelando el resto del menú + la tarjeta completa al deslizar, sin cortar nada; viewport intermedio (750px) — sin problema. `vite build` limpio del frontend completo en cada paso. **Quinto y definitivo ajuste, mismo bloque, con capturas reales (celular + escritorio) mostrando que la tarjeta quedaba directamente OCULTA fuera del área visible del navegador, sin forma de alcanzarla ni con scroll — un salto de severidad respecto a los 4 ajustes anteriores.** Causa raíz distinta a todas las anteriores: el wrapper interno del ajuste #4 metía un SEGUNDO contenedor con `overflow-y-auto` propio ANIDADO dentro del `<aside>`, que a su vez ya es `sticky`/`fixed` con altura en `dvh` — dos niveles de contenedor con scroll combinados con `sticky`+`dvh` resultó frágil en navegadores reales (Chrome de escritorio real y Safari/Chrome mobile reales), aunque en las pruebas automatizadas con Playwright (mismo motor Chromium, pero con un viewport fijado directo por API en vez del cálculo real del navegador) siempre midiera bien. Solución definitiva, más simple que todos los intentos anteriores: se elimina el wrapper interno por completo — el scroll vive en un solo lugar, el propio `<aside>` (`overflow-y-auto` agregado directo a su `className`), sin ningún contenedor de scroll anidado dentro de otro. El nav, el separador (`flex-1 min-h-3`, intacto) y la tarjeta pasan a ser hijos directos del `<aside>` — si todo el contenido no entra, el `<aside>` entero scrollea como una sola unidad (logo y datos de tienda incluidos, ya no quedan pegados arriba en el caso más apretado) en vez de intentar que solo el menú scrollee por dentro mientras el resto queda fijo. Mismo cambio aplicado en los dos archivos (`VendorLayout.jsx`, `AdminLayout.jsx` — este último sin tarjeta pero con el mismo riesgo de anidamiento en su nav de 22 links). Verificado en vivo con Playwright en el escenario más parecido al reportado (ancho de escritorio angosto — 1100px, ARRIBA del breakpoint `lg:` para confirmar que el modo `sticky` de escritorio también se probó, no solo el `fixed` de mobile — con alto ajustado de 650px): antes de scrollear la tarjeta queda fuera del viewport (750px > 722px de alto real), después de `aside.scrollTop = aside.scrollHeight` la tarjeta entra completa (700px ≤ 722px) con "Ver planes" visible — repetido igual en mobile (390×650). `vite build` limpio del frontend completo. **Sexto ajuste, mismo bloque — el 5to resultó ser un falso positivo, revertido, con el problema real identificado al fin.** El usuario probó en vivo la MISMA ventana de Chrome visible que usa Playwright para las pruebas de este bloque (Chrome for Testing, no un navegador aparte) y reportó dos cosas nuevas con capturas: el zoom de Chrome no cambiaba nada (confirmado, no era la causa), y ahora el logo/nombre de tienda y la tarjeta "Mejora tu plan" ya NO quedaban fijos — todo el sidebar (incluido el header de arriba) se deslizaba junto al hacer scroll. Investigando la causa del ajuste #5 (el que decía que "un wrapper interno anidado con `overflow-y-auto` dentro de un `<aside>` sticky/dvh es frágil en navegadores reales"), se encontró que esa conclusión fue un FALSO POSITIVO: la "prueba" que la respaldaba en realidad estaba midiendo esta misma ventana de Chrome dejada en un viewport de prueba angosto (390px, tamaño de celular) por un resize de Playwright anterior que nunca se revirtió — nunca hubo un bug real de "contenido oculto detrás del navegador" en el `<aside>` sticky de escritorio; siempre fue el navegador de pruebas visible en un tamaño equivocado. Con eso descartado, se revirtió el ajuste #5 (scroll en el `<aside>` entero) de vuelta al patrón de "header fijo + wrapper interno con su propio scroll" del ajuste #4 (que SÍ es correcto) — en `VendorLayout.jsx`, el logo y los datos de tienda vuelven a quedar fuera de cualquier `overflow-y-auto`, con el wrapper (nav + separador + tarjeta) como único contenedor scrolleable; en `AdminLayout.jsx`, sin tarjeta al final, el nav vuelve a llevar `flex-1 min-h-0 overflow-y-auto` directo (sin necesitar wrapper, ya que no hay nada después compitiendo por el espacio). Verificado en vivo, en la MISMA ventana de Chrome real (no solo con `scrollTop` programático sino con `page.mouse.wheel()`, un scroll de rueda de mouse genuino) en ambos breakpoints: desktop (1280×660) — el logo y "Casa Verde" quedan fijos arriba mientras el nav se desliza con su propia barra de scroll interna, confirmado con `aside.scrollTop === 0` mientras el wrapper interno sí avanza; mobile (390×650) — mismo resultado, el wrapper llega a su tope de scroll (`scrollHeight - clientHeight`) y la tarjeta completa entra en el viewport. `vite build` limpio del frontend completo. **Séptimo y último ajuste, mismo bloque: el usuario insistió en que la visualización real seguía sin hacerse — y tenía razón.** Con captura de la MISMA ventana de Chrome recién cargada (sin ningún scroll manual todavía) a 1280×660, se confirmó en vivo: "Ver planes" quedaba cortado por el borde inferior de la ventana mientras el menú mostraba sus 11 links completos — el nav estaba "ganando" el espacio disponible y la tarjeta perdiendo. Causa: el wrapper interno (ajuste #6) trataba nav+separador+tarjeta como un solo bloque scrolleable sin ninguna prioridad entre ellos — cuando el total no entraba, lo que quedaba cortado era lo último en el flujo (la tarjeta), no lo primero (el nav). Diagnóstico correcto: el ORDEN de prioridad estaba invertido — la tarjeta (el CTA de upsell, todo el propósito del bloque) nunca debe cortarse; si algo cede espacio, debe ser el menú (una lista larga, con scroll ya esperable por naturaleza). Fix: se quita el wrapper por completo. El logo y los datos de tienda pasan a `flex-shrink-0` explícito (nunca ceden). El scroll vuelve a vivir en el propio `nav` (`min-h-0 overflow-y-auto`, sin `flex-1` — no crece de más, mismo cuidado del bug #2) — es el ÚNICO elemento sin `flex-shrink-0`, así que absorbe toda la reducción cuando falta espacio, con su propia barra de scroll interna revelando el resto de sus links. El separador (`flex-1 min-h-3`) y la tarjeta (`flex-shrink-0`) nunca ceden ni un píxel. Verificado en vivo, en la misma ventana real (1280×660), con captura de pantalla ANTES de cualquier scroll manual: "Mejora tu plan" se ve completo con el botón "Ver planes" desde la primera carga, mientras el nav aparece cortado después de "Configuración" con su propia barra de scroll visible — y con un scroll real de rueda de mouse sobre el nav, "Mi perfil" se revela y la tarjeta se mantiene visible en todo momento (`isVisible()` de Playwright confirmando ambos). `vite build` limpio del frontend completo. `AdminLayout.jsx` no necesitó este ajuste — su nav es el último elemento del sidebar (sin tarjeta después compitiendo por espacio), así que su `flex-1 min-h-0 overflow-y-auto` de siempre ya prioriza correctamente sin necesitar cambios.
- **Bloque 82 (bug real reportado en vivo, con captura):** un producto agotado (`stock:0`, sin `unlimitedStock`) aparecía mezclado con los disponibles en "Destacados" del Home, y su botón de "Agregar al carrito" seguía funcionando de verdad — la captura mostró el toast "Agregado al carrito ✓" sobre una tarjeta marcada "Sin stock". Causa raíz de 2 partes: (1) `AddToCartControl.jsx` nunca chequeaba `product.stock` para el camino SIN tallas — el selector de tallas sí deshabilitaba cada talla agotada (`stockForSize === 0`), pero un producto simple (sin `sizes`) iba directo al botón de agregar sin ningún guard, a diferencia de lo que su propio nombre sugiere. (2) Home y el catálogo (`/catalogo`, Shop.jsx) comparten el mismo endpoint `GET /search` (`structuredSearch`), que nunca filtraba por stock — cualquier producto activo aparecía ahí sin importar si tenía unidades o no. La sección "Próximamente disponibles" de la propia tienda (`Store.jsx`, ya existente desde el Bloque 23, con botón "Solicitar" en vez de "Agregar al carrito") ya hacía esto bien — el pedido fue que ESA sea la ÚNICA superficie donde un producto agotado se muestre, en ningún otro lado. Fix: `AddToCartControl.jsx` calcula `outOfStock = !hasSizes && !product.unlimitedStock && product.stock === 0` y en ese caso renderiza un botón deshabilitado ("Sin stock", sin `onClick`) en vez del de agregar — nunca deja que la cantidad en el carrito pase de 0 para un producto así. `structuredSearch` (`search.controller.js`) suma `OR: [{ unlimitedStock: true }, { stock: { gt: 0 } }]` a su `baseWhere` — cubre Home Y el catálogo de un solo cambio, al ser el mismo endpoint. Mismo filtro sumado a `autocompleteSearch` (dropdown de búsqueda en vivo) y a las 3 consultas de `searchCandidateProducts` en `assistant.controller.js` (pool de recomendaciones del asistente de IA) — un producto agotado tampoco debe ser sugerido por ninguna de esas otras 2 vías de descubrimiento. Deliberadamente SIN tocar: `getVendorBySlug` (Store.jsx necesita ambos — disponibles Y agotados — para armar sus 2 secciones), `Product.jsx` (la ficha de producto ya maneja bien el caso agotado, con el botón deshabilitado y "Sin stock disponible", alcanzable solo desde el link de "Próximamente disponibles"), y la consulta de productos de mensajes de chat viejos en `assistant.controller.js` (reconstruye lo que YA se recomendó en su momento, no una recomendación nueva). Verificado con un smoke test (Node, contra la DB real, sin modificar datos): `structuredSearch` sin query no devuelve ningún producto agotado de los 26 activos; buscando por el nombre exacto de un producto agotado sembrado ("Ropa vieja con congrí") no aparece ni en `structuredSearch` ni en `autocompleteSearch` — más una verificación en vivo con Playwright contra el Home real, confirmando que los 3 productos agotados de la captura original ("Ropa vieja con congrí", "Cerdo asado", "Reparación de pantalla de celular") ya no aparecen — más `vite build` limpio del frontend completo.
- **Bloque 82, follow-up (2 bugs reales reportados en vivo minutos después, con captura, sobre la ficha de un producto agotado):** **(1) La cantidad mostraba "1" en vez de "0".** `Product.jsx` mostraba SIEMPRE el stepper de cantidad (-/número/+), sin importar el stock — y el botón "-" tenía un piso de `Math.max(1, q - 1)` que nunca lo dejaba bajar de 1, así que un producto sin stock quedaba pegado en "1" para siempre. Cuando no hay stock, el botón de abajo ya cambiaba a "Solicitar este producto" (`RequestProductButton`, Bloque 23) — que NO usa ninguna cantidad (solo registra interés por `productId`), así que el stepper de arriba no tiene ningún sentido ahí. Fix: se oculta el stepper entero cuando `remainingStock === 0` (sin stock real disponible, ya sea porque el producto no tiene ninguno o porque el cliente ya tiene en el carrito todo lo que había) — en vez de mostrar un número que no representa nada real, no se muestra ninguno. **(2) Una pastilla con el texto crudo "table" aparecía junto al precio, donde deberían ir los métodos de pago.** Causa: `"table"` es un valor válido del enum `paymentMethods` (`products.controller.js`, `z.enum(["whatsapp","cod","prepaid","table"])`) que quedó de datos de siembra viejos — NO es algo que un vendedor pueda elegir hoy desde `VendorProducts.jsx` (el pedido por mesa/QR es un campo totalmente aparte, `availableForTableMenu`, con su propio checkbox), así que ni `PAY_LABELS` (`Product.jsx`) ni `PAYMENT_METHOD_LABEL` (`VendorProducts.jsx`) nunca tuvieron una traducción para él — el `?? m` de respaldo mostraba el valor crudo del enum tal cual. Pedido explícito: en vez de traducirlo (no correspondía mostrarlo como método de pago), se filtra directo (`.filter((m) => m !== "table")`) y, en su lugar — mismo espacio visual — un producto sin stock real muestra ahí una pastilla roja "Agotado" (mismo criterio de color que el badge "Sin stock" de `ProductCard.jsx`). Verificado en vivo con Playwright contra el producto real de la captura ("Tostones", Sabor Criollo): el stepper ya no aparece, la pastilla dice "Agotado" en vez de "table", y "Solicitar este producto" sigue funcionando igual que antes. `vite build` limpio del frontend completo.

- **Bloque 82, segundo follow-up (pedido explícito, con captura):** el estado vacío de reseñas ("Todavía no hay reseñas.") era un párrafo de texto plano sin ningún contenedor, flotando directo sobre el fondo de la página — se veía descuidado comparado con el resto del sitio. `ReviewsMarquee.jsx` (compartido por `Store.jsx` y `Product.jsx`) pasa a usar el mismo `EmptyState` (ícono + título + descripción dentro de una tarjeta con borde punteado) que ya usan las listas vacías del panel de vendedor/admin — nunca se había reusado en las páginas públicas hasta ahora. Verificado en vivo con Playwright contra el mismo producto sin reseñas ("Tostones"). `vite build` limpio del frontend completo.
- **Bloque 83 (pedido explícito — "usar la IA más rápida primero", medido en vivo, no adivinado):** el asistente de compras tardaba mucho en responder. Se hizo un benchmark real contra los 3 proveedores de IA activos de esta cuenta (Gemini, Groq, NVIDIA NIM) — nunca se asumió cuál era "el más rápido" de oídas. Resultado real: Gemini falló en 2.4s (503, sobrecarga transitoria del lado de Google), Groq falló en 513ms (404), y **NVIDIA NIM tardó 108.9 SEGUNDOS** en responder. Como el orden de siempre era Gemini → Groq → NVIDIA (último recurso), cualquier momento en que Gemini fallara (pasa seguido) el cliente terminaba esperando esos ~109s de NVIDIA antes de ver una respuesta — la causa real de la lentitud reportada. Investigando por qué Groq fallaba, se encontró la causa raíz real: el modelo default `llama-3.3-70b-versatile` (`groq.js`) ya NO EXISTE MÁS para esta cuenta — Groq lo dio de baja (la nota del código ya avisaba "ya pasó dos veces", esta es la tercera). Se llamó en vivo al endpoint real de modelos de Groq (`listGroqModels`) y se probó cada candidato contra la API real hasta encontrar uno que respondiera 200: `openai/gpt-oss-120b` (402ms, el mismo modelo que un bloque anterior había querido usar pero estaba bloqueado a nivel de proyecto — ese bloqueo ya no está) — cargado como `SiteSettings.aiModelGroq` (dato, no código, el mecanismo ya estaba pensado para esto). Con Groq arreglado y siendo real y consistentemente el más rápido (~400-1500ms medido en vivo, varias corridas), se reordenó `PROVIDER_NAMES` en `ai.js` de `["gemini","groq","nvidia"]` a `["groq","gemini","nvidia"]` — NVIDIA sigue de último recurso (sigue siendo real, solo que lento) pero ya no es lo primero que se prueba tras un fallo de Gemini. Se descubrió de paso que `chatWithGroq` con el modelo nuevo (una familia "razonadora", gasta tokens pensando antes del JSON final — mismo fenómeno ya documentado para Gemini con `thinkingBudget`) fallaba con `json_validate_failed`/"max completion tokens reached" en el techo viejo de 260 tokens — subido a 500 para darle margen real de terminar el JSON completo. **Además, ningún proveedor tenía NINGÚN timeout** — el cuelgue de 109s de NVIDIA no tenía techo, podía ser peor. Se agregó `AbortSignal.timeout(20_000)` a las 6 llamadas fetch de los 3 proveedores (`nvidia.js`, `groq.js`, `gemini.js`) — un proveedor colgado ahora falla en máximo 20s (tratado como un fallo más de ESE proveedor, la cadena de respaldo sigue funcionando igual) en vez de bloquear al cliente varios minutos. Optimización menor de paso: `chat.controller.js` corría `lookupOrderContext` y `buildPurchaseHistoryContext` en secuencia (dos consultas a la DB totalmente independientes entre sí) — pasan a `Promise.all`. Verificado en vivo de punta a punta con `chatWithStoreAssistant` real (mismo código que usa el chat de tienda): tras el fix, respondió en 1.5s por Groq en el primer intento, sin necesitar ningún fallback — antes de este bloque, ese mismo camino podía terminar esperando minutos si Gemini fallaba primero. `node --check` limpio en los 5 archivos backend tocados.
- **Bloque 84 (bug real reportado en vivo, con captura):** al intentar agregar un producto de una tienda distinta a la que ya tenía el carrito activo, salía el toast "Agregado al carrito ✓" — pero `CartContext.addItem` en ese caso NO agrega nada de verdad, solo abre `CartConflictModal` (pidiendo vaciar el carrito primero) y devuelve `{conflict:true}` sin tocar `items`/`vendor`. El toast mentía: nada se había agregado todavía. Encontrada la causa: `AddToCartControl.jsx` y `Product.jsx` (`handleAddToCart`) llamaban a `addItem(...)` y mostraban el toast de éxito SIEMPRE, sin fijarse en el valor de retorno — a diferencia de `StoreChatWidget.jsx`, que ya hacía esto bien desde antes (chequea `result.conflict` antes de confirmar el agregado), patrón que simplemente no se había replicado en los otros 2 lugares. Fix: los dos ahora guardan `const result = addItem(...)` y solo muestran el toast (y, en `AddToCartControl.jsx`, solo registran el tracking de "cliente potencial") si `!result.conflict`. De paso, un segundo hueco real: cuando el cliente SÍ confirmaba "Vaciar y continuar" en el modal (el momento en que el producto se agrega DE VERDAD — ver `resolveConflict(true)` en `CartContext.jsx`), no había NINGÚN toast que lo confirmara — silencio total en el único momento en que algo realmente pasaba. `CartConflictModal.jsx` ahora guarda el nombre del producto ANTES de resolver (`resolveConflict(true)` limpia `pendingConflict` de inmediato) y muestra `Carrito vaciado — "{nombre}" agregado ✓` justo después — un solo toast combinado en vez de apilar dos, más limpio que el "toast doble" que se había sugerido como boceto inicial. Verificado en vivo con Playwright de punta a punta: agregar un producto de otra tienda con el carrito ya con contenido → NO sale ningún toast, solo se abre el modal (confirmado con captura); clic en "Vaciar y continuar" → el carrito pasa de 43 ítems de la tienda vieja a 1 solo ítem de la tienda nueva (confirmado con el badge del header y el contenido real del carrito), con el toast combinado apareciendo en ese momento. `vite build` limpio del frontend completo.
- **Bloque 85 (pedido explícito):** verificación automática diaria de que los proveedores de IA activos siguen funcionando, con cambio automático de modelo si uno fue dado de baja, y aviso por correo al admin si un proveedor entero deja de responder — sobre la base ya arreglada en el Bloque 83 (orden Groq→Gemini→NVIDIA, timeouts de 20s, modelo de Groq corregido). **Zona horaria configurable, nueva:** `SiteSettings.timezone` (default `America/Havana`), editable desde una tarjeta nueva "Zona horaria" en Admin → Mi perfil → Marca de la plataforma, con un reloj en vivo (actualiza cada segundo) que confirma la hora/fecha real de la zona elegida antes de guardarla — validada server-side intentando construir un `Intl.DateTimeFormat` con el valor (un string que no sea una zona IANA real rechaza, en vez de guardarse silenciosamente y romper el cálculo de "son las 3am" más adelante). **Tercer cron del proyecto** (`aiHealthCheck.job.js`, después de `vendorLifecycle.job.js` y `verificationPayment.job.js`), pero con una diferencia real de diseño respecto de esos dos: aquellos corren siempre a una hora FIJA de Cuba (`cron.schedule(..., {timezone:"America/Havana"})`, capturada una sola vez al arrancar el servidor) — como acá la zona horaria es editable por el admin en caliente, ese patrón se hubiera desincronizado hasta el próximo reinicio del backend cada vez que el admin la cambiara. En su lugar, este cron corre CADA HORA en punto y se autochequea por dentro: lee `SiteSettings.timezone` en cada tick (nunca una fija de arranque), calcula la hora actual en esa zona con `Intl.DateTimeFormat`, y solo actúa si son las 3am — `SiteSettings.lastAiHealthCheckAt` evita un doble disparo el mismo día calendario (en esa zona) si el tick de las 3am coincide más de una vez. Por cada proveedor ACTIVO (Gemini/Groq/NVIDIA — un proveedor apagado se salta entero, mismo criterio que `getActiveProviders` de `ai.js`): prueba el modelo configurado (`SiteSettings.aiModelX` o el default del archivo) con un prompt trivial, reusando las mismas funciones `generateWithX` que ya usa el chat real (mismo timeout de 20s del Bloque 83 — este chequeo mide la ruta de código real, nunca un mock aparte). Si responde bien, no hace nada. Si falla, busca un reemplazo real: lista los modelos que esa key puede usar hoy (`listGeminiModels`/`listGroqModels`/`listNvidiaModels`, el mismo endpoint que ya usa el selector de `AdminIntegrations.jsx`, nunca una lista vieja hardcodeada), descarta candidatos obviamente no aptos (audio/voz, moderación, embeddings — regex compartida) y prueba cada uno midiendo su tiempo de respuesta hasta un techo de 6 candidatos (probar los 20-30 que a veces devuelve la lista completa gastaría cuota de sobra en un chequeo diario). Si algún candidato respondió, guarda el más rápido como el nuevo `SiteSettings.aiModelX` (mismo mecanismo de override ya existente, sin tocar código) y le manda un correo detallado al admin (`notifyAdminActionNeeded`, reusa el mismo helper de Bloque 66/69) explicando qué modelo falló, por qué, y a cuál se cambió solo. Si NINGÚN candidato respondió, el proveedor entero se marca "caído" — correo distinto avisando que ninguna key/modelo de ese proveedor funciona, sugiriendo revisar la integración — y se deja registro en `ErrorLog` con un origen nuevo (`AI_HEALTH_CHECK`, había que agregarlo al enum `ErrorOrigin`, era un enum cerrado — Postgres `ALTER TYPE ... ADD VALUE`, migración aparte). `AdminIntegrations.jsx` gana un segundo banner explicando esto (por qué un modelo puede aparecer cambiado solo de un día para otro) y sus 3 tarjetas de IA se reordenan/re-etiquetan para reflejar el nuevo orden real (Groq primero, "el más rápido medido"). Verificado en vivo de punta a punta contra los proveedores reales de esta cuenta (sin mocks): Gemini devolvió 403 PERMISSION_DENIED y NVIDIA no conectó — ambos correctamente detectados y marcados "down", con sus correos y sus `ErrorLog` escritos sin errores (confirmado el fix del enum, que había fallado en la primera corrida); Groq respondió bien (618-711ms) y no disparó ningún cambio, como corresponde. Se encontraron y arreglaron 2 problemas reales del entorno de trabajo en el camino (no del código de este bloque): el backend estaba caído (se había matado para aplicar la migración y nunca se reinició) y el frontend de Vite había dejado de recoger cambios de archivo a mitad de esta sesión larga (confirmado comparando el código fuente contra lo que el propio servidor de Vite devolvía por fetch — no era caché del navegador) — reiniciar ambos procesos lo resolvió. La tarjeta de "Zona horaria" se probó en vivo cambiando a Ciudad de México y guardando (confirmado en la base), y devolviendo a La Habana. Migraciones aplicadas (`timezone`+`lastAiHealthCheckAt` en `SiteSettings`, `AI_HEALTH_CHECK` en `ErrorOrigin`) con backup de la base tomado después de cada una. `node --check` limpio en el job nuevo y los archivos backend tocados, `vite build` limpio del frontend completo.
- **Bloque 86 (pedido explícito, sobre el hallazgo del Bloque 85 — "la key de Resend es correcta, no sé por qué falló"):** se agregó un botón "Probar — mandar correo de prueba" en la tarjeta de Resend de `AdminIntegrations.jsx`, mismo criterio que "Actualizar lista" que ya tenían los 3 proveedores de IA (ejercitar la API real, nunca un chequeo de formato) — Resend no tiene "modelos" que listar, así que la prueba real posible es mandar un correo de verdad. Nuevo `POST /admin/integrations/resend/test` (`testResendIntegration`, `integrations.controller.js`) — exige la clave ya guardada (mismo guard que `listProviderModels`), manda SIEMPRE a la cuenta del propio admin (nunca a un cliente/vendedor ni a una dirección escrita a mano — mismo destinatario "seguro" que `notifyAdminActionNeeded`), reusando `sendAdminDirectEmail` tal cual (mismo template, mismo registro en `EmailLog`) en vez de armar un envío aparte — y responde con el error EXACTO que devolvió Resend si rechaza el envío, nunca un "no se pudo" genérico, para poder diagnosticar de verdad. Verificado en vivo contra la cuenta real de Resend de este proyecto (sin mocks, dos veces — llamando la función directo y golpeando el endpoint HTTP real con el token de sesión): Resend devuelve **"API key is invalid"**, su propio mensaje de rechazo — no un timeout, no un error de red, no un bug de este flujo. Se investigó si el problema podía estar del lado de ESTE código (cifrado/guardado corrompiendo la clave) antes de asumir que la clave está mal: se descifró la clave guardada y se inspeccionó su forma (sin exponerla completa) — 36 caracteres, empieza con `re_` (prefijo real de Resend), sin espacios ni caracteres no imprimibles — o sea, estructuralmente una key de Resend normal, no corrupta. Conclusión: el guardado/cifrado/envío de este proyecto funciona correctamente de punta a punta: es la cuenta de Resend la que está rechazando esta clave puntual como inválida (revocada, regenerada desde el dashboard de Resend después de guardarla acá, o un typo real al copiarla en su momento que no se nota a simple vista) — hace falta ir a resend.com/api-keys, generar una key nueva, y pegarla acá con "Rotar" para confirmar con el mismo botón "Probar" que ya quedó funcionando.
- **Bloque 87 (pedido explícito — "verifica todos los cambios de GitHub, no sé por qué me sigue dando error el compartir tienda"):** el usuario reportó (con captura) el error "No se pudo copiar el enlace." al compartir una tienda, pese a asegurar que ya lo había arreglado con Claude Code en GitHub. **Diagnóstico de sincronización real:** `git status` decía "up to date with origin/main", pero eso solo refleja el ÚLTIMO fetch — un `git fetch --all` reveló que `origin/main` había avanzado de `b26fcb7` a `2cb95fb` con 2 commits nuevos nunca traídos a este clon (`2cb95fb` "Tarjeta de producto: clic en cualquier parte abre el producto" y `7559e82` "Responsividad de modales, reporte solo para clientes, venta rápida al nivel de un producto" — exactamente los 2 primeros bloques que el usuario dijo haber arreglado), más 7 ramas remotas nuevas nunca fetcheadas. Como había cambios locales sin commitear (el trabajo de esta sesión, Bloques 83-86) que tocaban 2 de los mismos archivos que esos commits (`Product.jsx`, `AdminIntegrations.jsx`), se hizo `git stash push -u` → `git merge --ff-only origin/main` (limpio, sin necesidad de resolver conflictos de merge — local era ancestro directo) → `git stash pop`, que Git auto-mergeó sin un solo conflicto real; se verificó con `git diff origin/main -- <archivo>` en ambos archivos que el resultado final tiene AMBOS juegos de cambios intactos (nada se pisó ni se perdió). **La causa real del bug reportado, encontrada después de traer el código:** `Store.jsx` seguía mostrando el error incluso con el fix de `lib/clipboard.js` (nuevo, con fallback a `execCommand`) ya aplicado — el motivo real es OTRO bug, preexistente, nunca relacionado con el portapapeles: `showLinkCopiedToast()` (el callback de ÉXITO de `handleShare`) llamaba a `toast.custom(...)`, un método de la librería react-hot-toast que el wrapper de este proyecto (`lib/toast.jsx`) JAMÁS expuso (confirmado importando el módulo real y listando sus keys: `success/error/warning/info/loading/dismiss/remove/promise`, sin `custom`) — cada copiado EXITOSO tiraba `TypeError: toast.custom is not a function`, capturado por el `.catch()` de la MISMA cadena de promesas, mostrando "No se pudo copiar el enlace." — un mensaje falso: la copia sí había funcionado, lo que fallaba era el toast de confirmación. Fix: se reemplaza el `toast.custom(...)` (con su JSX armado a mano — ícono, título, texto, botón de cerrar) por `toast.success(title, description, {icon:"link"})`, la API real y ya documentada del wrapper (mismo patrón que usa el resto del sitio), con el mismo título/texto/ícono visual de siempre (`components/ui/Toast.jsx` sí soporta un ícono `"link"`) — se quita también el import de `Check` de lucide-react, que solo se usaba en el JSX roto. **Además, se hizo la revisión puntual que el usuario pidió para cada bloque:** (1) `ProductCard.jsx` — un solo `<Link>` envolviendo toda la tarjeta, `AddToCartControl` aislado con `stopPropagation` en `onClick` Y `onMouseDown`, sin ningún `<a>` anidado dentro de otro. (2) El gate `user?.role === "CUSTOMER"` del botón "Reportar estafa" en `Store.jsx` está bien — oculta el botón para vendedor/admin a propósito (cambio de comportamiento pedido explícitamente, no un bug); la galería de venta rápida en `CustomerPanel.jsx` sí llama a `DELETE /customer-listings/:id/images` con `{url}`, y el endpoint (`removeListingImage`, ya existía en el backend) resuelve dueño/imagen correctamente y borra el archivo del disco. (3) El heurístico `isRawZodDefault()` de `errorHandler.js` — se buscó en todo `backend/src` algún mensaje custom real que pudiera coincidir por casualidad con alguno de sus 10 patrones (`"Required"`, `"Invalid email"`, etc.) — ninguno coincide, cero riesgo de que un mensaje en español se pise por error; la concordancia de género quedó resuelta de forma genérica y consistente (siempre "{label}: campo obligatorio.", nunca depende del género del campo). (4) `npx prisma migrate status` confirmó "Database schema is up to date!" (70 migraciones, incluidas las de `CustomerListing`/`Report` de Feature A/B) y `npx prisma generate` corrió limpio (con el mismo EPERM de Windows de siempre — el backend tenía el motor de Prisma bloqueado, se mató el proceso con `taskkill` y se reinició después, mismo procedimiento ya establecido en este proyecto). Verificado en vivo con Playwright: `toast.custom` confirmado `undefined` en el módulo real; la nueva llamada `toast.success(...)` con los mismos argumentos NO tira ningún error (antes sí). No se pudo confirmar visualmente el toast renderizado dentro del navegador de pruebas automatizado — Chromium bajo Playwright no tiene a nadie para resolver el diálogo nativo de permiso de portapapeles que dispara un clic real, así que `navigator.clipboard.writeText()` queda pendiente para siempre en ese contexto puntual (una limitación conocida de la automatización, no del código: se probó por separado que `copyToClipboard()` sí resuelve `true` cuando se invoca fuera de un gesto de clic "de confianza"). `vite build` limpio del frontend completo, backend arrancando sin errores tras el `prisma generate`.
- **Bloque 88 (pedido explícito, con trade-off de seguridad discutido primero):** el usuario reportó (con captura, probando `admin@zeudin.cu`/`Zeudin2026!` — correo y contraseña reales mal tipeados a propósito) que el toast de login solo decía "Correo o contraseña incorrectos." sin más detalle, y pidió que dijera exactamente cuál era el problema. Se le explicó que ese mensaje genérico era intencional (`auth.controller.js`, comentario original: "un admin probando el formulario de vendedor... nunca debe poder distinguir 'no existe esta cuenta' de 'existe pero no con ese rol'") — es la defensa estándar contra enumeración de usuarios (con un mensaje específico, cualquiera puede usar el formulario de login para descubrir qué correos están registrados en el sistema, probando uno por uno). Se le presentaron 3 opciones (genérico siempre / detalle solo en dev / detalle siempre) y eligió explícitamente "detalle específico siempre", aceptando el trade-off. Cambios en `login()` y `assertRoleMatchesContext()` (`auth.controller.js`): correo no encontrado → "No existe ninguna cuenta con ese correo."; contraseña incorrecta → "La contraseña es incorrecta."; rol no corresponde al formulario usado (admin/vendedor) → "Esta cuenta no tiene permisos de administrador." / "Esta cuenta no es de vendedor." (antes los 3 casos compartían el mismo mensaje genérico). Verificado en vivo con `curl` contra el backend real: `admin@zeudin.cu` (dominio mal) → "No existe ninguna cuenta con ese correo."; `admin@zeudin.com` con contraseña mal → "La contraseña es incorrecta." El chequeo redundante de rol en el frontend (`Account.jsx`, `roleAllowedForMode`) queda sin tocar — es código defensivo que en la práctica nunca se dispara porque el backend ya corta el login por rol ANTES del 2FA quien manda el `context` correcto desde `handleLogin`; no es parte de lo pedido y tocarlo no cambiaba nada observable.
- **Bloque 89 (pedido explícito, con 2 cambios menores de texto de por medio):** "Te mandamos un código a tu correo." pasó a "Te enviamos un código a tu correo." (`Account.jsx`, único call site). Aparte: la barra de progreso del toast (`Toast.jsx`) era una animación CSS pura, ajena al hover — react-hot-toast SÍ pausa el cierre real al pasar el mouse por encima (pausa nativa de la librería, `startPause`/`endPause` en el wrapper que envuelve TODO toast, confirmado en el bundle instalado), pero la barra visual seguía llenándose (y "terminando") igual, sin importar el mouse — quedaba visualmente llena mucho antes de que el toast realmente desapareciera. Fix: se agrega estado `isHovered` (`onMouseEnter`/`onMouseLeave` en el div raíz del toast) que controla `animationPlayState` de la barra ("paused"/"running"), declarado DESPUÉS del shorthand `animation` en el objeto de estilo (la declaración posterior gana, mismo criterio de cascada que en CSS normal) — como ambos hovers (el de la librería y el nuevo de la barra) se disparan sobre la misma área visual al mismo tiempo, quedan sincronizados: la barra se pausa exactamente donde iba (CSS conserva el progreso al pausar, nunca lo resetea) y sigue desde ahí al sacar el mouse, terminando de llenarse justo cuando el toast real desaparece. `vite build` limpio.
- **Bloque 90 (pedido explícito, con captura de 2 correos reales de "la integración de X no responde" la misma noche — Gemini y NVIDIA):** se agregó un botón "Probar conexión" a cada tarjeta de proveedor de IA en `AdminIntegrations.jsx` (Gemini/Groq/NVIDIA), mismo criterio que "Probar" de Resend (Bloque 86) — ejercita la API real con el modelo configurado ahora mismo, sin esperar al chequeo de las 3am. Nuevo `POST /admin/integrations/:name/test-ai` (`testAiProviderIntegration`, `integrations.controller.js`) — reusa exactamente `generateWithX` y el modelo configurado (`SiteSettings.aiModelX` o el default), misma ruta de código que el chat real y que `aiHealthCheck.job.js`. La ruta literal `/integrations/resend/test` sigue registrada ANTES que la nueva `/integrations/:name/test-ai` en `admin.routes.js` (Express prueba en orden de registro, así que la de Resend nunca cae en el patrón genérico). **Investigación de los 2 correos reales:** se corrió `runAiHealthCheckJob()` a mano contra las keys reales de esta cuenta — Gemini y NVIDIA volvieron a marcar "down" de nuevo, con el mismo motivo que traían los correos. Investigando por qué, se encontró un bug real en el camino: los 6 `catch` de fetch en `gemini.js`/`groq.js`/`nvidia.js` tiraban SIEMPRE el mismo texto genérico ("No se pudo conectar con el servicio de IA...") sin importar la causa real — perdiendo la única pista útil que el health check/botón nuevo necesitan para diagnosticar. Se agregó `describeFetchFailure(err)` en los 3 archivos (distingue `TimeoutError`/`AbortError` de cualquier otro error de red) y se pasa como `details.detail` en los 10 `throw` afectados — mismo mensaje visible para el usuario final, pero ahora con el motivo real disponible para quien lo necesite (health check, botón de prueba, logs). Con esto ya en pie, se confirmó el motivo real: **"Se agotó el tiempo de espera (20s) sin respuesta"** para ambos, NO un error de key/auth. Se probó a mano con timeouts más largos, saltándose el límite de 20s: NVIDIA respondió 200 real (`"Listo"`) pero tardó **41 segundos** — la clave sigue funcionando, es real y consistentemente lenta (mismo comportamiento ya documentado en el Bloque 83, ahí llegó a medirse en 109s una vez). Gemini con "gemini-flash-latest" siguió sin responder incluso a los 60s — se investigó más: la key es válida (confirmado listando modelos reales, `GET` responde en <400ms) y el alias SÍ existe en la cuenta, pero probando modelos puntuales se encontró que Google retiró varios modelos que este código conocía ("gemini-2.5-flash ya no está disponible para usuarios nuevos, usa gemini-3.6-flash") — probando `gemini-3.6-flash` (lo que Google recomienda como reemplazo) directo, SÍ respondió, pero tardó 18.3s, peligrosamente cerca del techo de 20s — indicando que ahora mismo, esta noche, el modelo detrás de "flash-latest" está genuinamente lento del lado de Google (~10x más lento que el ~2s de referencia del Bloque 83), no roto. **Conclusión: ninguna de las 2 keys está vencida ni revocada — es una degradación de latencia real y transitoria del lado de ambos proveedores, coincidiendo la misma noche por casualidad** (confirmado también con `curl` directo a los 3 hosts: los 3 responden rápido a un GET simple, así que tampoco es un problema de red/DNS de este servidor). Como el orden de respaldo real de producción es Groq→Gemini→NVIDIA y Groq sigue sano (573-625ms medido en las mismas corridas), ningún usuario real se vio afectado — el sistema ya se está auto-protegiendo como está diseñado. Aprovechando el diagnóstico, se encontró y arregló una mejora real en `findFastestWorkingModel` (`aiHealthCheck.job.js`): probaba los candidatos de reemplazo EN SECUENCIA (uno por uno, cada uno agotando sus 20s completos antes de pasar al siguiente — hasta 2 minutos para 6 candidatos) — se cambió a `Promise.allSettled` en paralelo, así la espera total pasa a ser la del candidato más lento, no la suma de los 6; esto no cambió el resultado de esta noche puntual (todos los candidatos probados de Gemini/NVIDIA también estaban lentos), pero sí mejora las chances reales de encontrar un reemplazo rápido en un incidente futuro donde solo ALGUNOS modelos estén afectados. `node --check` limpio en los 5 archivos backend tocados, `vite build` limpio del frontend.
- **Bloque 92 (reportado en vivo — "el localhost no me carga la base de datos con todos los productos"):** al ejecutar frontend+backend en local, la Home cargaba vacía — se investigó pensando primero en la base de datos (conteo real vía Prisma: 36 productos, 6 tiendas, 13 usuarios, 12 pedidos, intacta) y en el endpoint público (`GET /search`, el que usa `Home.jsx`) — ambos funcionando bien golpeándolos directo. La causa real: el router de casa le había reasignado la IP local de esta máquina, de `192.168.1.78` a `192.168.1.79` (confirmado con `ipconfig`), pero `frontend/.env` y `frontend/.env.development` (`VITE_API_URL`) y `backend/.env` (`FRONTEND_URL`, para CORS) tenían la `.78` vieja hardcodeada — el frontend le pedía los datos a una IP que ya no existe en la red, y por separado el backend hubiera rechazado por CORS cualquier pedido que sí llegara desde la IP nueva del frontend. Se actualizaron las 3 referencias a `.79` (confirmado que no quedó ninguna otra referencia a `.78` en todo el repo) y se reiniciaron ambos procesos — ni Vite ni `node --watch` recargan variables de entorno solos ante un cambio de `.env`, hace falta reiniciar el proceso. Verificado en vivo: `curl` a la IP vieja no responde, a la nueva sí, y el log del backend ya mostraba pedidos reales (`/search`, `/vendors`, `/offers`) devolviendo datos tras el reinicio. Nota dejada para el usuario: si el router no tiene la IP de esta PC reservada por DHCP, esto puede volver a pasar en cualquier reinicio del router.
- **Bloque 93 (pedido explícito):** en Admin → Dashboard, la sección "Imagen principal del sitio" ahora dice el tamaño recomendado para subir: **1200×800px o más, relación 3:2** — calculado a partir del recuadro real donde se muestra en `Home.jsx` (columna de la imagen del grid del hero, ~578×400px en escritorio a 1280px de ancho de contenido, relación ~1.45:1 ≈ 3:2 — pedido al doble de esa resolución para verse nítida en pantallas retina/alta densidad). La imagen del hero YA se ajustaba sola al tamaño del contenedor (`h-full w-full object-cover` en `Home.jsx` — cubre el recuadro entero sin deformarse, recortando el sobrante si la relación de aspecto no calza exacto); no se tocó ese código porque ya hacía lo pedido. El texto nuevo solo evita que ese recorte automático se coma partes importantes de una foto subida con una relación de aspecto muy distinta a la del recuadro real. Sin cambios en ningún otro archivo. `vite build` limpio.
- **Bloque 94 (pedido explícito, follow-up del Bloque 93):** el hero de `Home.jsx` usaba `object-cover` — llenaba el recuadro entero recortando lo que sobrara, así que una foto más alta que ancha (o con cualquier relación de aspecto lejos de la del recuadro) perdía partes reales de la imagen. Se cambia a `object-contain`: la imagen se escala completa DENTRO del recuadro sin deformarse ni recortarse nunca — se agrega un div interno nuevo (`bg-white/10`, mismo tono que el placeholder de "sin imagen" de antes) que actúa de marco fijo, así el espacio sobrante (cuando la foto no calza exacto en la proporción 3:2 del recuadro) queda relleno en vez de dejar una franja en blanco suelta. Cuidado real durante el cambio: el `overflow-hidden` necesario para que el marco recorte prolijo sus esquinas NO podía ir en el `<div relative>` de afuera — las 2 tarjetitas flotantes ("Pide por WhatsApp"/"Filtrado por tu zona") son `absolute` respecto a ESE div y a propósito se salen del recuadro en desktop (`lg:-right-3.5`/`lg:-left-3.5`); ponerle `overflow-hidden` ahí se las hubiera recortado. Se resolvió metiendo el `overflow-hidden`+`rounded-xl`+`bg-white/10` en un div NUEVO, interno, que envuelve solo la imagen — las tarjetitas siguen siendo hermanas del div de afuera, sin recorte. Mismo cambio (`object-cover` → `object-contain`) en la miniatura de vista previa de Admin → Dashboard, para que lo que ve el admin ahí sea fiel a cómo se va a ver en la Home real; se ajustó también el texto de esa sección para explicar el nuevo comportamiento (centrada con sobrante relleno, nunca estirada). `vite build` limpio.
- **Bloque 95 (pedido explícito, follow-up del Bloque 94, con captura):** en el hero de `Home.jsx`, el marco de la imagen pasa de `bg-white/10` a transparente del todo (se ve el degradado de la sección por detrás cuando la imagen no cubre el recuadro completo) y se eliminan las 2 tarjetitas flotantes que quedaban superpuestas ("Pide por WhatsApp — Respuesta directa" y "Filtrado por tu zona — Provincia y municipio"). `overflow-hidden` + `rounded-xl` se mantienen en el marco interno (para que la imagen nunca se salga de sus esquinas redondeadas), pero como ya no quedan elementos `absolute` que se salieran a propósito del recuadro, la estructura se simplifica a un solo div. Se limpia el import de `MapPin` de lucide-react (quedó sin ningún uso en el archivo tras quitar la tarjetita que lo usaba; `MessageCircle` se deja porque sigue usándose en el bloque de texto de la izquierda). `vite build` limpio.
- **Bloque 96 (pedido explícito):** "Imagen principal del sitio" pasa de una sola imagen fija a un slider de varias, con fundido cruzado automático y puntos de posición cuando hay más de una. **Backend:** `SiteSettings.heroImageUrl` (guardaba solo el nombre de archivo) se reemplaza por `SiteSettings.heroImages String[]` (guarda rutas relativas completas, mismo criterio que `Product.images`) — migración nueva que agrega la columna, copia la imagen existente como primer elemento del array (para no perderla) y recién ahí borra la columna vieja; verificado en vivo que la imagen que ya estaba cargada sobrevivió la migración intacta. `updateHeroImage` (reemplazaba entero) se reemplaza por `addHeroImages` (agrega al array, `siteUpload.array("images", 6)`, mismo patrón que `addProductImages`) y `removeHeroImage` (`DELETE /admin/settings/hero-images` con `{url}` en el body, filtra el array y borra el archivo físico best-effort, mismo patrón que `removeProductImage`). **Frontend:** `HeroImageSlider.jsx` (componente nuevo) recibe la lista de URLs y maneja todo internamente — fundido cruzado (crossfade con opacity, todas las imágenes apiladas con `position:absolute`, solo la activa en `opacity-100`, `transition-opacity duration-700`) que avanza solo cada 5s, y una fila de punticos debajo (`w-6` el activo, `w-2` el resto) que SOLO aparece si hay más de 1 imagen — clic en un punto salta directo a esa imagen. Con 0 imágenes reserva el mismo espacio vacío de siempre (transparente, Bloque 95) para no desarmar el grid de 2 columnas del hero. `Home.jsx` ahora solo le pasa `heroImages` (mapeado con el baseURL) al componente. `AdminDashboard.jsx`: la sección de admin pasa de 1 preview + "Reemplazar imagen" a una galería de miniaturas (una por imagen, botón ✕ al pasar el mouse) + un botón "Agregar imagen(es)" con `<input type="file" multiple>` que sube todas juntas en una sola petición; borrar una pide confirmación con `ConfirmModal` (mismo componente/patrón que la galería de fotos de producto en `VendorProducts.jsx`) antes de ejecutar el `DELETE`. Sin reordenar ni "marcar portada" — no fue parte de lo pedido, el orden de subida es el orden del slider. Verificado en vivo de punta a punta contra el backend real (con un JWT de admin real generado a mano, mismo mecanismo que `createSession` — no un mock): subir 2 imágenes juntas a la que ya había (quedaron las 3, en orden), borrar una puntual (el array se actualizó Y el archivo físico se borró del disco, confirmado con `ls`), y se limpiaron los 2 archivos de prueba y la sesión de prueba al terminar, dejando la imagen original intacta como estaba antes de probar. `node --check` limpio en los 2 archivos backend tocados, `vite build` limpio del frontend.
- **Bloque 97 (bug real reportado en vivo, con captura — "no se muestran las imágenes en la página principal"):** tras el Bloque 96, los punticos SÍ aparecían en la Home pero el recuadro de la imagen se veía vacío (transparente) — la captura mostraba 3 puntos (confirmando que `heroImages` traía datos y el componente los recibía bien), solo que ninguna imagen se dibujaba. Causa: en `HeroImageSlider.jsx`, el div del marco tenía `h-[320px] flex-1 ... lg:h-[400px]` dentro de un padre `flex flex-col` sin altura propia — `flex-1` fija `flex-basis: 0%`, que en flexbox tiene prioridad sobre la altura explícita para calcular el tamaño base del ítem; como el padre no tenía altura fija de la que "crecer" (todo su contenido real son las imágenes, que al ser `position:absolute` no cuentan para la altura intrínseca), el marco terminaba colapsando a 0px de alto real — las imágenes SÍ estaban ahí, solo que dentro de una caja de altura cero con `overflow-hidden`. Se quita el `flex-1` (nunca hacía falta: el marco no necesita "crecer" dentro del `flex-col`, solo mantener su alto fijo) — con eso la altura explícita vuelve a ser la que manda. `vite build` limpio; el frontend en dev (Vite con HMR) ya estaba corriendo así que el fix aplica solo con refrescar, sin reiniciar nada.
- **Bloque 98 (pedido explícito, alcance completo elegido tras presentar el trade-off):** "Destacados en toda Cuba" pasa de `orderBy: [isFeatured, createdAt]` a un algoritmo de ranking real. El usuario pidió explícitamente clics, tiempo de permanencia, demanda de búsqueda, ventas y completitud del contenido — ninguna señal de tráfico existía todavía en el sistema (no había tracking de vistas/clics), así que se construyó desde cero antes de poder rankear con eso. **Nuevas columnas en `Product`** (contadores agregados, no una tabla de eventos aparte — lo único que hace falta para rankear es el total acumulado, nunca el historial evento por evento, y escala mucho mejor en cada carga de Home/catálogo): `viewCount`, `clickCount`, `searchClickCount`, `totalDwellMs`, `salesCount`. Nota real de la implementación: `totalDwellMs` se diseñó primero como `BigInt` (técnicamente más "correcto" para una suma que crece sin techo) — se detectó ANTES de desplegar que `JSON.stringify`/`res.json()` no sabe serializar `BigInt` (hubiera roto con un 500 silencioso TODOS los endpoints que devuelven un `Product`, no solo los de este bloque), así que se pasó a `Int` con un techo de 30 min por vista (ver `MAX_DWELL_MS`) que de sobra alcanza para la escala real de este proyecto (más de 71 mil vistas de un mismo producto antes de acercarse al límite de Int). **3 endpoints públicos nuevos** (`products.routes.js`/`products.controller.js`, sin `authenticate` — cualquier visitante genera estas señales con solo mirar): `POST /products/:id/track-view` (se abrió la ficha — interés real), `POST /products/:id/track-click` (clic en una tarjeta desde cualquier listado, con `source` opcional que distingue si vino de una búsqueda puntual), `POST /products/:id/track-dwell` (ms acumulados en la ficha, con techo de 30 min contra un valor corrupto de un solo golpe). **`salesCount`** se incrementa en `confirmOrderSale` (`orders.controller.js`), no en `createOrder` — es el momento real donde el vendedor confirma que la venta pasó de verdad (mismo momento que ya descuenta stock), cuenta incluso para productos `unlimitedStock` (se venden igual, solo no se les sigue el stock), y vive DENTRO de la misma transacción atómica que el descuento — si algo más falla, se revierte junto con el resto. **El algoritmo** (`lib/productRanking.js`, nuevo): normaliza cada señal 0..1 contra el máximo real del lote consultado (min-max, se adapta solo a medida que crece el catálogo, sin percentiles globales que mantener aparte) y arma un score compuesto (ventas 30%, clics 15%, clics-desde-búsqueda 15%, tiempo promedio en página 10%, completitud del contenido 15%, calificación con media bayesiana contra un prior neutro 15% — 1 sola reseña de 5★ no pesa igual que 50). `isFeatured` (palanca manual del admin) sigue siendo más fuerte que cualquier combinación automática, nunca lo pisa el algoritmo. **Piso de calidad (pedido explícito — "sin cometer errores de mostrar un producto que no mantenga buena calidad, sobre todo con los que no son de tienda verificada")**: un producto con poco contenido (completitud <40 si la tienda es verificada, <65 si no) o con calificación promedio <3.0 (si tiene reseñas) NUNCA se promueve como destacado sin importar cuántas ventas/clics tenga — cae a un score fijo bajo, nunca se excluye del catálogo, solo nunca se "empuja". Arranque en frío: un producto activado hace <14 días recibe un empujón chico y temporal (no permanente) para que tenga oportunidad real de acumular sus propias señales antes de competir 100% por mérito contra productos con historial. **Integración en `search.controller.js`**: el pool candidato del camino sin texto de búsqueda sube de 60 a 300 (el algoritmo necesita ver el catálogo completo disponible para no perderse un producto viejo con buen desempeño real solo por no estar entre los 60 más recientes) — se recorta a 60 DESPUÉS de rankear, no antes. El bloque final de "relevancia" (antes solo "tiendas verificadas primero") ahora llama a `rankFeaturedProducts` con un `groupBy` de reseñas agregado aparte; aplica tanto al feed sin texto (alimenta "Destacados" de la Home) como a resultados de búsqueda con texto en modo relevancia (no a los sorts explícitos precio/calificación, esos siguen intactos). **Frontend**: `ProductCard.jsx` gana un prop `trackSource` (dispara `track-click` al hacer clic, antes de navegar) — `Home.jsx` pasa `"home"`, `Shop.jsx` pasa `"search"` o `"catalog"` según si hay texto de búsqueda activo. `Product.jsx` gana un `useEffect` con `data?.product?.id` como dependencia (no un mount vacío — "También te puede interesar" navega a otro producto SIN desmontar este componente, mismo patrón que el `useEffect` ya existente de reseteo de imagen/talla): dispara `track-view` una vez por producto real (dedupeado con `sessionStorage`, recargar la misma ficha en la misma pestaña no infla el contador) y acumula tiempo de permanencia real, flusheado con `navigator.sendBeacon` (sobrevive el cierre de pestaña/navegación, a diferencia de un fetch normal) en `visibilitychange`→hidden y en `pagehide`/desmontaje. **Verificado en vivo contra la base real** (sin mocks): se le dieron a un producto con contenido real (3 fotos, descripción, 1 tag) 15 ventas y 40 clics simulados — saltó al puesto #1 de 27, por encima de todos los `isFeatured` manuales. Al mismo tiempo, a un producto con CERO contenido se le dieron exactamente los mismos números (15 ventas, 40 clics) — se quedó en el puesto 25 de 27, el piso de calidad lo bloqueó igual pese a los números idénticos. Los 3 endpoints de tracking probados uno por uno contra el backend real (`curl`), confirmando que cada contador sube exactamente lo esperado. Todos los datos de prueba se revirtieron a su estado original al terminar. `node --check` limpio en los 5 archivos backend tocados/nuevos, `vite build` limpio del frontend.
- **Bloque 99 (pedido explícito, alcance completo — "que cuando un modelo se desconecte no se muestre al usuario, se detecte y busque el reemplazo solo, y use siempre el más rápido"):** antes de construir, se le explicó al usuario qué ya existía (cadena de respaldo en vivo por pedido + chequeo nocturno de las 3am que ya auto-reemplaza modelos rotos y avisa solo si un proveedor queda sin ninguno que funcione) contra lo que realmente faltaba: (1) detección/reparación solo una vez al día, no en el momento — un modelo roto a las 4am tarda hasta 24h en arreglarse solo; (2) el orden groq→gemini→nvidia era una medición fija de una sola vez (Bloque 83), nunca se re-medía con tráfico real. Se construyeron las 2 mejoras confirmadas. **Refactor previo necesario**: `probeModel`/`findFastestWorkingModel`/`PROVIDERS`/`EXCLUDE_MODEL_PATTERN` vivían solo dentro de `aiHealthCheck.job.js` — se extraen a un archivo nuevo compartido, `lib/aiModelRepair.js`, para que `ai.js` (el camino EN VIVO) y el job nocturno (el camino PROACTIVO) usen la misma lógica de "buscar reemplazo" en vez de mantener dos copias que podrían divergir; el job nocturno queda con el mismo comportamiento exacto de siempre, solo importando de otro lado. **Reparación reactiva en vivo** (`repairProviderModel`, nuevo en `aiModelRepair.js`): `callWithFallbackChain` (`ai.js`) dispara esto en segundo plano (sin bloquear ni esperar) apenas un proveedor falla en un pedido real — nunca espera a las 3am. Antes de tocar nada, vuelve a probar el MISMO modelo una segunda vez (evita que un timeout de red puntual dispare un cambio de modelo innecesario por ruido pasajero); si sigue fallando de verdad, busca un reemplazo real igual que el job nocturno. Si encuentra uno, lo carga solo y queda registrado en `ErrorLog` — SIN correo (pedido explícito: una reparación que ya se resolvió sola no necesita la atención del admin). Si no encuentra ninguno (proveedor entero caído), ahí sí manda el correo de inmediato (pedido explícito: "en casos de error persistente se notifica al administrador inmediatamente") — mismo texto/criterio que ya usaba el job nocturno para ese caso. Cooldown de 5 minutos por proveedor (`REPAIR_COOLDOWN_MS`) para que una ráfaga de pedidos fallando a la vez contra el mismo modelo roto no dispare docenas de búsquedas en paralelo — el resto de esos pedidos ya está cubierto por el fallback normal de todos modos. **Orden dinámico por velocidad real** (`ai.js`): `PROVIDER_NAMES` deja de ser el orden de prioridad (pasa a ser solo la lista de proveedores conocidos) — `getActiveProviders()` ahora reordena los activos por latencia medida en vivo (`sortByMeasuredSpeed`), con una media móvil exponencial (`EMA_ALPHA=0.3`, en memoria del proceso, se reaprende solo con tráfico real tras un reinicio) actualizada en cada pedido que responde bien (`recordLatency`, agregado a `callWithFallbackChain`). Las referencias de Groq~600ms/Gemini~2s/NVIDIA~15s del Bloque 83 quedan como semilla inicial (`DEFAULT_LATENCY_MS`) solo para cuando el servidor recién arrancó y no hay ninguna medición propia todavía — en cuanto hay tráfico real, la medición real manda. **Verificado en vivo de punta a punta, sin mocks**: se rompió a mano `SiteSettings.aiModelGroq` con un nombre de modelo inexistente y se llamó a `generateDescription` real — el pedido del cliente igual respondió bien (cayó a Gemini y de ahí a NVIDIA, ambos con problemas reales y transitorios esa noche — 503 de sobrecarga de Google y timeout de NVIDIA, nada relacionado con este bloque), y en menos de 1 segundo, en segundo plano, el sistema detectó que Groq seguía roto, encontró "openai/gpt-oss-20b" como reemplazo real que sí funciona (562ms) y actualizó `SiteSettings.aiModelGroq` solo — confirmado leyendo la base directo, sin esperar nada. Una llamada siguiente ya respondió por Groq directo en 1.5s, sin necesitar ningún fallback — el sistema se había reparado solo de verdad. Se restauró `aiModelGroq` a su valor real de producción (`openai/gpt-oss-120b`) al terminar la prueba (la rotura fue a propósito, para probar). De paso se notó que `aiModelNvidia` ya venía como `moonshotai/kimi-k3` (distinto del `meta/llama-3.3-70b-instruct` de hace días) — confirmado que es el chequeo nocturno YA EXISTENTE funcionando solo de verdad en algún 3am real de estos días de sesión larga, no algo tocado por este bloque; se dejó tal cual. `node --check` limpio en los 3 archivos backend tocados/nuevos.
- **Bloque 100 (bug real reportado en vivo, con captura de Google AI Studio mostrando un modelo nuevo):** el usuario reportó 2 síntomas relacionados — "Actualizar lista" de Gemini no mostraba un modelo nuevo real que sí existía en su cuenta, y Gemini seguía agotando el timeout de 20s. Se encontraron y arreglaron 3 bugs reales, ninguno relacionado con la clave del usuario. **(1) `listGeminiModels` sin paginar**: confirmado en vivo que la cuenta de este proyecto ya tiene más de 50 modelos (`nextPageToken` presente en la primera página) — se traía solo la página 1 y el resto se perdía en silencio; se agrega un loop que sigue `nextPageToken` hasta agotarlo, con `pageSize=200` para minimizar vueltas. **(2) `listGeminiModels`/`listGroqModels`/`listNvidiaModels` sin ningún timeout** (a diferencia de `generateWithX`/`chatWithX`, que sí lo tienen desde el Bloque 83) — un cuelgue de red dejaba el botón "Actualizar lista" girando para siempre sin ningún error visible, fácil de confundir con "no se actualiza"; se agrega `AbortSignal.timeout(REQUEST_TIMEOUT_MS)` a los 3. **(3) El bug de fondo real, encontrado investigando por qué el sistema de reparación del Bloque 99 no arreglaba Gemini solo**: `findFastestWorkingModel` prueba candidatos en el orden alfabético que devuelve la lista, cortado a los primeros `MAX_CANDIDATES_TO_PROBE` — probado en vivo, esos primeros 6 alfabéticos de la cuenta de Gemini resultaron ser puros agentes especializados (`antigravity-preview-05-2026`, 3 variantes de `deep-research-*`, `gemini-2.5-computer-use-preview`) y un modelo ya deprecado (`gemini-2.5-flash`, confirmado 404 en el Bloque 90) — ni uno solo era un modelo de chat/texto simple, así que la búsqueda SIEMPRE devolvía `null` (confirmado: <1 segundo, cero candidatos reales intentados) por más veces que se corriera. `EXCLUDE_MODEL_PATTERN` se amplía bastante (de solo audio/moderación a también agentes de investigación/cómputo, generadores de imagen, transcripción, música, "omni", "live") y `MAX_CANDIDATES_TO_PROBE` sube de 6 a 10 (gratis en tiempo real, ya se prueban en paralelo desde el Bloque 90 — más candidatos no implica más espera). Con el filtro nuevo, la misma búsqueda contra la misma cuenta encontró "gemini-3.1-flash-lite" como reemplazo real que sí funciona. **Se aplicó de inmediato** (no hizo falta esperar a que el tráfico real disparara la reparación reactiva del Bloque 99): `SiteSettings.aiModelGemini` actualizado a mano a "gemini-3.1-flash-lite", verificado en vivo con 3 pruebas independientes — llamada directa a `generateWithGemini` (731ms), y el mismo endpoint real de "Probar conexión" que el usuario venía usando (987ms, antes fallaba a los 20s exactos). `node --check` limpio en los 4 archivos backend tocados.
- **Bloque 101 (pedido explícito, con imagen pegada en el chat):** el usuario pidió insertar una imagen (mockup de teléfono con notificación de venta) en la sección "Para dueños de negocio" de la Home, "por encima" y responsiva. Dos cosas se resolvieron antes de tocar código: (1) no hay forma de extraer los bytes exactos de una imagen pegada en el chat — no hay ninguna herramienta de guardado de imagen ni un archivo temporal accesible en disco (se buscó, no apareció nada reciente) — se le pidió al usuario que la guardara él mismo en el proyecto; (2) la imagen mostraba la marca "Shopify" bien visible (ícono + nombre + notificación "[Modern Mallard] ¡Hiciste una venta!") — se le avisó explícitamente que ZeuDin no usa Shopify (los vendedores reciben pedidos por WhatsApp) y que ponerla tal cual mostraría la marca de otra plataforma en el propio sitio; se le dieron 2 opciones (editarla para sacar el texto, o usarla tal cual) y confirmó usarla tal cual. El usuario guardó `frontend/src/assets/images/visualizacion_telefono.png` (1254×1254px, ~1MB — nota aparte no pedida: es un PNG bastante pesado para web, si en algún momento se nota lento el primer carga de la Home vale la pena comprimirlo/convertir a WebP, pero no se tocó porque no fue lo pedido). Insertada en `Home.jsx` como `<img>` `absolute` dentro de la tarjeta oscura de esa sección (que pasa a `relative`) — `pointer-events-none` (puramente decorativa, nunca debe robarle el clic a los botones de al lado) y tamaño+desplazamiento responsivos por breakpoint (`w-20 -top-10` en mobile → `sm:w-28 sm:-top-14` → `md:w-36 md:-top-20`) para que sobresalga por encima del borde superior de la tarjeta en todos los tamaños sin salirse nunca del ancho de la tarjeta (evita cualquier riesgo de scroll horizontal) ni taparse con el texto/botones. Verificado en vivo con Playwright en 2 anchos reales (390px mobile, 1280px desktop): en mobile, `document.documentElement.scrollWidth` (375px) nunca superó el viewport (390px) — sin overflow horizontal — y la imagen quedó muy por encima de los botones (que en mobile se apilan mucho más abajo, sin ningún riesgo de choque); en desktop, se midió con `boundingBox()` que la imagen NO se solapaba con ningún botón (se detectó y corrigió un solape real de ~40px contra "Ver tiendas en ZeuDin" en el primer intento, subiendo el offset vertical) y quedaba dentro del borde derecho de la tarjeta con margen de sobra. Confirmado también visualmente con capturas de pantalla reales en ambos anchos. `vite build` limpio.
- **Bloque 102 (pedido explícito, con boceto de referencia — "así no es el modelo y la posición... para pantallas grandes... y para móviles debe quedar responsivo también"):** el usuario mostró un boceto donde la imagen es mucho más grande, sobresale tanto por arriba como por abajo de la tarjeta, y los botones quedan apilados en columna (no en fila). Se agrandó la imagen bastante en `md`/`lg` (de `w-36` a `md:w-64`/`lg:w-80`), se centró verticalmente (`top-1/2 -translate-y-1/2`) para que sobresaliera simétricamente arriba y abajo, se cambiaron los botones de `flex-wrap` (fila) a `flex-col` (columna, como en el boceto), y se le reservó espacio real a la derecha de la tarjeta (`md:pr-72`/`lg:pr-96`) para que el texto/botones nunca quedaran debajo de la imagen grande. Verificado en vivo con Playwright (`boundingBox()` real, no a ojo) en 3 anchos — se encontró y corrigió un solape real de ~32px entre el botón "Ver tiendas" y la imagen en el primer intento (el padding reservado no incluía el `right-*` extra de separación del borde) — y se encontró y corrigió un bug propio real: se había agregado `overflow-hidden` a la tarjeta pensando que hacía falta para desktop, pero eso recortaba la versión chica de mobile (que sobresale por arriba a propósito desde el Bloque 101) — se sacó, la tarjeta nunca lleva overflow-hidden en ningún breakpoint.
- **Bloque 103 (pedido explícito, corrección inmediata al Bloque 102 — "la imagen debe estar pegada a la línea inferior del contenedor... en móviles centrada y pegada a la línea del contenedor como base"):** el centrado vertical del Bloque 102 no era lo pedido — se cambia a `bottom-0` (el borde inferior de la imagen queda SIEMPRE pegado al borde inferior de la tarjeta, creciendo hacia arriba desde ahí, nunca sobresaliendo por debajo). En mobile pasa de "clavada en la esquina superior derecha" a centrada horizontalmente y pegada abajo (`left-1/2 -translate-x-1/2`, la tarjeta gana `pb-32`/`sm:pb-36` de padding extra abajo — vuelve a `p-9` normal desde `md` — para que los botones, que ahora quedan justo arriba de la imagen en vez de al costado, tengan espacio de sobra sin tocarla); desde `md` la imagen vuelve a ir al costado derecho como en el boceto original (`md:left-auto md:right-4 md:translate-x-0`, `lg:right-8`). Verificado en vivo con Playwright en mobile (390px) y desktop (1440px): en ambos, el borde inferior de la imagen coincide EXACTO (mismo valor de `y`) con el borde inferior de la tarjeta, con al menos 16px de separación limpia respecto a los botones más cercanos, y sin scroll horizontal en ningún ancho. Confirmado también visualmente con capturas reales en los 2 anchos. `vite build` limpio en ambos bloques.
- **Nota de paso, ambos bloques**: el usuario reemplazó `visualizacion_telefono.png` (1MB) por `visualizacion_telefono.webp` (90KB) por su cuenta entre un bloque y el otro — se actualizó el import en `Home.jsx` para apuntar al `.webp` nuevo (la build había fallado con "no se pudo resolver" hasta corregirlo). Confirma la sugerencia de compresión que se había dejado como nota en el Bloque 101.
- **Bloque 104 (bug real reportado en vivo, con captura — "la imagen está tapando los productos"):** consecuencia directa de agrandar la imagen en el Bloque 102/103 — desde `lg` (1024px+), la imagen (320×320px) sobresale ~94px por ENCIMA del borde de la tarjeta (tarjeta de solo ~226px de alto ahí), y el `pt-11` (44px) de espacio antes de la sección no alcanzaba para contenerla — se salía de la propia `<section>` y tapaba las tarjetas de producto de la sección de arriba (confirmado en vivo con Playwright: a 1920px, el tope de la imagen quedaba 50px por ENCIMA del tope de la sección). Se sube el padding superior de la sección a `lg:pt-36` (144px, con margen de sobra sobre el sobrante real de 94px) — mobile/sm/md no se tocan (ahí la imagen no sobresale del borde de la tarjeta, no lo necesitaban). Verificado en vivo: a 1920px, el tope de la imagen quedó 50px POR DENTRO del tope de la sección (antes 50px por fuera) — separación limpia confirmada también con captura de pantalla real, ya sin ninguna tarjeta de producto tapada. `vite build` limpio.
- **Bloque 105 (pedido explícito, con captura — "en móviles debe verse más grande la imagen"):** sube de `w-28`/`sm:w-36` a `w-44`/`sm:w-52` (112px→176px en mobile, 144px→208px en sm) — bastante más grande dentro de la tarjeta. El padding inferior reservado para que no choque con los botones sube en la misma proporción (`pb-32`/`sm:pb-36` → `pb-48`/`sm:pb-56`). Verificado en vivo con Playwright en 390px y 700px: en ambos, el borde inferior de la imagen sigue exacto al borde inferior de la tarjeta y queda con 16px limpios de separación del botón "Ver tiendas" más cercano, sin scroll horizontal en ningún ancho. Confirmado también con captura real. `vite build` limpio.
- **Bloque 106 (pedido explícito, con captura — "para dispositivos móviles no es la misma imagen, es otra"):** el usuario subió una segunda imagen a la misma carpeta, `hero-mobile-apps.webp` (3 capturas de una app en formato apaisado, 709×532px — a diferencia del mockup cuadrado de escritorio) y pidió usarla dedicada para mobile en vez de seguir reescalando la de escritorio. Se separan en 2 `<img>` — uno con la imagen nueva (`hidden` desde `md`, visible solo mobile/sm) y otro con la de escritorio (`hidden` por defecto, `md:block`), cada uno con su propio tamaño/posición (mismo criterio "pegada a la base, centrada" del Bloque 103, pero calibrado para el aspect-ratio real de la imagen nueva en vez de forzar el cuadrado de escritorio). Bug real encontrado al probarlo en vivo: la imagen nueva, al ser más ancha que alta, termina siendo más ALTA en píxeles reales que la vieja al mismo ancho (192px vs 176px en la base, 240px vs 208px en sm) — el `pb-48`/`sm:pb-56` calibrado para la imagen vieja casi tocaba el botón "Ver tiendas" (0.08px de diferencia, prácticamente unidos) — se sube a `pb-56`/`sm:pb-72`. Verificado en vivo con Playwright en 390px y 700px: gap real de 32-48px contra el botón más cercano en ambos, borde inferior de la imagen siempre flush con el de la tarjeta, sin scroll horizontal; confirmado con `isVisible()` que a escritorio (1440px) se muestra la imagen vieja y la nueva queda oculta, y viceversa a mobile. Confirmado también visualmente con captura real — coincide con la referencia que mandó el usuario. `vite build` limpio.
- **Bloque 107 (bug real reportado en vivo — "el carrito está vacío y me dice que tiene otra tienda"):** confirmado y reproducido de punta a punta en el navegador real (sin mocks): agregar un producto de una tienda, sacarlo del carrito UNO POR UNO desde `/carrito` (el botón de basura + su confirmación — nunca el botón "Vaciar carrito" entero), y agregar un producto de OTRA tienda disparaba el modal falso de "Tu carrito tiene otra tienda" pese a que el carrito realmente estaba en 0 productos. Causa real en `CartContext.jsx`: `clearCart()` (el botón "Vaciar") sí reseteaba `vendor` a `{}` junto con `items`, pero `removeItem()` (sacar un producto puntual) solo tocaba `items` — si el cliente terminaba en 0 productos sacándolos de a uno, `vendor` se quedaba pegado con la tienda vieja, y `addItem()` decide si hay conflicto mirando SOLO `vendor.vendorId` (nunca si `items` estaba vacío de verdad). Fix en 2 partes: (1) `removeItem` ahora resetea `vendor` y `discount` a su estado vacío cuando sacar ese ítem deja el carrito en 0 — mismo criterio que `clearCart`; (2) refuerzo defensivo en `addItem`: el chequeo de conflicto ahora exige también `items.length > 0`, para que ningún otro camino futuro que dejara un `vendor` viejo pegado sin querer pueda disparar un conflicto falso sobre un carrito vacío de verdad. Verificado en vivo con Playwright reproduciendo el bug real tal cual lo reportó el usuario: agregar "Zapatillas deportivas unisex" (Moda Caribe) → sacarlo con el botón de basura + confirmar → `localStorage` muestra `vendor:{}` correctamente (antes se quedaba con Moda Caribe) → agregar "Shampoo natural de aloe" (Bella Piel, tienda distinta) → se agregó directo, sin ningún modal de conflicto. `vite build` limpio.
- **Bloque 108 (2 pedidos explícitos, mismo turno):** (1) las tiendas cerradas en su ficha pública (`Store.jsx`) pasan del genérico "Cerrado ahora" a decir cuándo abren de verdad — `isVendorOpenNow` (`schedule.service.js`) gana `nextOpenLabel`: si el horario de hoy todavía no empezó, "Abre hoy a las HH a.m./p.m."; si ya cerró por hoy (o hoy es día de descanso), recorre hasta 7 días hacia adelante buscando el próximo día real que abra — "Abre mañana a las..." si es el día siguiente, o "Abre el `<lunes/martes/etc.>` a las..." (nombre real del día) si está más lejos; si ningún día de la semana tiene horario cargado, `nextOpenLabel` es `null` y el badge se cae al genérico de siempre (nunca inventa una hora). Formato de hora siempre 12h con a.m./p.m. (nunca el "09:00" crudo que guarda la DB). (2) "X ventas" se saca del perfil público (franja de estadísticas de `Store.jsx`) — pedido explícito: es un dato interno de la tienda, no para cualquier visitante por ahora; se sigue el mismo patrón ya usado para datos privados de KYC (`ownerName`/`ownerIdNumber`/`companyAddress`) y se excluye `salesCount` también del lado del backend (`getVendorBySlug`), no solo de la vista — nunca viaja en la respuesta a un visitante anónimo, no es solo "no se muestra". Verificado en vivo contra el backend real con 6 escenarios sintéticos (abre más tarde hoy, ya cerró y abre mañana, cerrado varios días seguidos hasta el viernes, abierta ahora mismo, sin horario cargado, los 7 días cerrados) — los 6 dieron el resultado esperado exacto — y contra un vendedor real (`GET /vendors/moda-caribe`): devolvió `"Abre mañana a las 8:00 a.m."` y confirmó que `salesCount` ya no está en la respuesta. Confirmado también visualmente en el navegador real. `node --check` limpio en los 2 archivos backend tocados, `vite build` limpio del frontend.
- **Bloque 109 (2 pedidos explícitos, mismo turno — "solicitar un producto sin stock debe exigir login, y no debe poder solicitarse varias veces"):** "Solicitar este producto" (Bloque 23, el botón que reemplaza "Agregar al carrito" cuando el stock es 0) tenía 2 problemas reales de diseño: aceptaba visitantes anónimos (un `guestId` en localStorage) — el vendedor recibía la solicitud sin ningún dato de contacto real, inútil para "avisarle cuando lo tenga disponible" — y el dedup de "no repetir la misma solicitud" expiraba solo a las 24h, así que el mismo cliente podía terminar mandando la misma solicitud una y otra vez indefinidamente. **Login obligatorio**: `POST /products/:id/request` pasa a exigir `authenticate` (antes público, decodificaba el token a mano si venía pero nunca lo exigía) — sin sesión, ya no es un fallback silencioso a `guestId`, es un 401 real; el frontend (`RequestProductButton.jsx`) saca el `guestId`/`getOrCreateGuestId` por completo y, sin sesión, el clic manda a `/cuenta?next=<esta misma página>` en vez de mandar la solicitud — mismo patrón exacto que ya usaban las reseñas de Product.jsx/Store.jsx para exigir login. **Dedup permanente, no por 24h**: el chequeo de "¿ya lo pidió?" deja de mirar una ventana de tiempo — busca CUALQUIER solicitud previa de ese cliente para ese producto, sin importar cuándo — y se libera sola cuando el producto se repone DE VERDAD: `updateProduct` (`products.controller.js`) detecta la transición real de sin-stock a con-stock (stock pasa de 0 a >0, o pasa a `unlimitedStock`) y borra las `ProductRequest` viejas de ese producto en la misma transacción — así un producto que se agota de nuevo más adelante sí puede volver a recibir solicitudes, pero nunca mientras sigue agotado. **Bonus, aprovechando el cambio**: `getProductBySlug` (pública, con login opcional vía el mismo mecanismo `resolveOptionalUserId` ya usado en chat/assistant/orders.controller.js) ahora devuelve `product.alreadyRequested` — si el cliente logueado ya tiene una solicitud pendiente de ESTE producto, el botón arranca directo mostrando "Enviado"/"¡Listo!" en vez de dejarlo creer que puede mandar otra. Verificado en vivo de punta a punta contra el backend real, sin mocks, con un producto real sin stock ("Ropa vieja con congrí", Sabor Criollo): (1) `POST` sin token → 401; (2) con un cliente real logueado → 201, `duplicate:false`; (3) el mismo cliente de nuevo → 200, `duplicate:true`, sin fila nueva; (4) la ficha del producto con ese cliente → `alreadyRequested:true`; (5) el vendedor repone stock a 10 unidades vía `PATCH /products/:id` → la solicitud pendiente se borró sola; (6) la ficha del producto de nuevo → `alreadyRequested:false`. Confirmado también en el navegador real: sin sesión, clic en "Solicitar este producto" redirige a `/cuenta?next=/producto/sabor-criollo/ropa-vieja-con-congri`. Se restauró el stock del producto de prueba a 0 al terminar. `node --check` limpio en los 2 archivos backend tocados, `vite build` limpio del frontend.
- **Bloque 110 (pedido explícito, follow-up del Bloque 109):** el estado "ya solicitado" de `RequestProductButton.jsx` se veía IGUAL que el botón normal (mismo gris de siempre, solo cambiaba el ícono y el texto) — ahora lleva borde grueso y fondo con tinte amarillo/ámbar (`#8a5100`, el mismo tono que el proyecto ya usa para avisos de "bajo stock" — `STATUS_STYLE.low` en `VendorProducts.jsx` — reusado a propósito en vez de inventar un color nuevo) para que se note de un vistazo, sin tener que leer el texto. Texto también más corto y directo: "Solicitado" (antes "Enviado") en la versión chica, "Solicitado — te avisamos cuando esté disponible" (antes "¡Listo! Le avisamos a la tienda") en la grande. Verificado en vivo en el navegador real (con una solicitud pendiente real creada a mano y una sesión de cliente real inyectada): el botón carga directo en amarillo con el borde y el tinte, confirmado con captura de pantalla. Datos de prueba (solicitud + sesión) borrados al terminar. `vite build` limpio.
- **Bloque 111 (bug real reportado en vivo, con 2 capturas — "Cuba muestra 0 subdivisiones pero 'hab' ya existe"):** encontrada la causa raíz real, bastante más grave de lo que parecía en la captura. `Province.code` es único GLOBAL (nunca por país, `schema.prisma:258`) y `Province.countryId` es nullable — cuando se agregó el modelo `Country` (Bloque 66, creado vía `prisma db push` directo, sin migración real — ver el fix de historial en `20260730015900_fix_missing_country_table`), las 16 provincias reales de Cuba del seed original (con datos reales de usuarios/vendedores/direcciones YA ligados) se quedaron con `countryId: NULL` — nunca se corrió el backfill que el propio comentario del schema decía dar por hecho ("las provincias ya sembradas se backfillean a Cuba"). `AdminLocations.jsx` filtra por `province.country?.id === selectedCountryId` — con `countryId` null, las 16 quedaban invisibles bajo CUALQUIER país (de ahí "0 subdivisiones" en los 3 países de la captura), pero el índice único de `code` seguía viéndolas: al intentar cargar "La Habana" con código "hab", chocaba contra la fila real ya existente (confirmado en la base: `{code:"hab", name:"La Habana", countryId:null}`) sin que el admin tuviera forma de saber que ya existía. Se escribió una migración de datos (`20260909030000_backfill_cuba_provinces_country`, idempotente: crea el país CUBA solo si no existe por `code`, nunca por un id fijo — el admin ya lo había creado a mano antes de esta migración en esta base real — y vincula solo las provincias sin país que coinciden exacto con los 16 códigos reales del seed cubano) y se aplicó en caliente contra la base real (sin reiniciar el backend — es un `UPDATE`/`INSERT` puro, no toca schema.prisma ni el cliente de Prisma generado). **Hallazgo adicional real al verificar**: como las provincias originales eran invisibles, el admin ya había intentado recrear a mano "Santiago de Cuba" y "Ciego de Ávila"/"Ciego de Avila" (sin el acento) con otro código — tras el backfill, esas 2 quedaban duplicadas junto a las originales. Se verificó con `_count` de relaciones reales (vendorLocations/addresses/users/municipalities) que las 2 duplicadas estaban 100% vacías (la original de "Santiago de Cuba" sí tenía 1 vendedor, 1 usuario y 9 municipios reales, incluido uno con el mismo nombre "Segundo Frente" que el único municipio vacío de su duplicada) — se borraron las 2 filas duplicadas vacías (cascada borra también el municipio vacío de una de ellas), dejando exactamente las 16 provincias reales de Cuba, sin huecos ni duplicados. Verificado en vivo contra el endpoint real de admin (`GET /admin/locations/provinces` con un token de admin real): antes 0 bajo CUBA con "hab"/"stg"/"cav" bloqueados en silencio por el unique global; después, exactamente 16, una sola vez cada una, todas con `countryId` real. `node --check` no aplica (SQL puro) — `npx prisma migrate deploy` corrió limpio.
- **Bloque 112 (bug real reportado en vivo, con 2 capturas — "escribo 'users' tal cual lo muestra lucide.dev y me dice que no existe"):** tanto `businessCategories.controller.js` como `AdminCategories.jsx` validaban el ícono con un `Icons[name]` exacto, sensible a mayúsculas — pero lucide.dev muestra el slug del ícono en minúsculas/kebab-case ("users", "shopping-bag"), mientras que el paquete `lucide-react` exporta el nombre real en PascalCase (`Users`, `ShoppingBag`); el admin tecleando exactamente lo que la propia web de la librería le muestra se topaba con un "ese nombre no existe" falso. Se reemplaza el chequeo exacto por un resolver tolerante (`toPascalCase` + `resolveLucideIconName`, duplicado a propósito en frontend y backend — mismo criterio, sin depender de una llamada de red extra) que primero prueba el nombre tal cual, y si falla, lo normaliza (separa por espacios/guiones/guion bajo, capitaliza cada palabra) y reintenta contra los exports reales de `lucide-react`; se excluyen a propósito los alias con sufijo `"...Icon"` que la librería también exporta, para que el admin siempre vea/guarde el nombre canónico sin sufijo. Tanto `createBusinessCategory` como `updateBusinessCategory` ahora guardan el nombre YA resuelto (nunca el texto crudo tecleado), y el preview en vivo del modal usa ese mismo nombre resuelto. Verificado en vivo contra el backend real: `POST /admin/business-categories` con `{"icon":"users"}` y un token de admin real devolvió `"icon":"Users"` — categoría y sesión de prueba borradas al terminar. `node --check` limpio en los 2 archivos backend tocados, `vite build` limpio del frontend.
- **Bloque 113 (pedido explícito, varios turnos — "hay mucha inconsistencia entre países, provincias y municipios... el cliente debe seleccionar solo países agregados en el panel de admin; si es de Cuba debe elegir provincia y municipio antes de vender; si es de otro país, solo país+estado+dirección, sin municipio" + 2 follow-ups sobre el paso 3 del registro de vendedor y un bug de íconos ya cubierto en el Bloque 112):** hasta ahora el registro (cliente y vendedor, en las 2 pantallas que lo ofrecen — `/cuenta` y `/vendedor/ingresar`) no pedía ningún país/provincia real de catálogo — el vendedor elegía una "provincia donde presta servicio" de una lista PLANA sin país ni municipio (`GET /locations/provinces`, sin filtrar), y el cliente no elegía ubicación en absoluto. **Modelo de datos nuevo**: `User` (y su staging `PendingRegistration`) gana `registrationCountryId`/`registrationCountryOther`/`provinceId`/`municipalityId` — explícitamente un concepto NUEVO y separado de: (a) `country` (código ISO de 2 letras del teléfono, `PhoneInput`, nunca tocado), (b) `Vendor.registrationCountryId/legalProvinceId/legalMunicipalityId` (domicilio legal, solo se pide en verificación/KYC opcional) y (c) `VendorLocation` (dónde presta servicio la tienda, ya existía, solo estaba incompleta). **Reglas de validación** (factorizadas en `backend/src/services/registrationLocation.service.js`, nuevo, con 3 funciones separadas a propósito porque el criterio difiere): si el país elegido es un país real del catálogo del admin y es Cuba, hace falta provincia Y municipio reales (uno perteneciendo al otro); si es cualquier otro país real ya cargado, alcanza con provincia/estado (nunca se exige municipio fuera de Cuba); para la ubicación de LA PERSONA (cliente o el vendedor como persona) se admite además "otro país" con el nombre escrito a mano si el suyo no está en la lista (nunca se le obliga a mentir eligiendo uno ajeno) — para la ubicación DE LA TIENDA (`VendorLocation`) esa salida NO existe, siempre tiene que ser un lugar real del catálogo (el marketplace no puede listar una tienda en un país que no tiene cargado), y si la tienda opera fuera de Cuba pasa a exigirse una dirección real (`companyAddress`) ya que no hay municipio que la ubique. `auth.controller.js` (`register`/`verifyRegistration`) y `vendors.controller.js` (`createVendor`) quedan wireados contra este servicio. **Frontend, reescrito en los 2 flujos de registro que existen, arreglados por separado (pedido explícito, no fusionarlos):** `Account.jsx` (`/cuenta` y `/vendedor/ingresar`) suma un paso 3 nuevo compartido por cliente Y vendedor (país → "otro país" con texto libre → provincia/estado → municipio si es Cuba, con selects en cascada que resetean los hijos al cambiar el padre — mismo patrón ya usado en `VendorVerification.jsx`), empujando el paso de datos de tienda del vendedor de 3→4; `VendorOnboarding.jsx` (`/vender`, el flujo independiente para un usuario que ya tiene cuenta y crea su tienda después) recibe el mismo selector en cascada país→provincia/estado→municipio reemplazando su select plano de provincia. **Follow-up explícito, mismo turno** ("el nombre del responsable/correo/WhatsApp de la tienda en el paso 3 están de más, ya se pidieron al principio"): en los 2 flujos se eliminan los campos "Nombre del responsable del negocio", "Correo de la tienda" y "WhatsApp de la tienda" del paso de datos de la tienda — se reusa tal cual el nombre/correo/teléfono ya capturados al inicio del registro (`form.fullName`/`email`/`phone` en Account.jsx; `user.fullName`/`email`/`phone` en VendorOnboarding.jsx, ya que ahí el usuario ya tiene cuenta creada), mostrando en su lugar un aviso de solo lectura ("Responsable del negocio: `<nombre>`... debe coincidir con tu documento de identidad, se puede verificar más adelante"). Verificado en vivo de punta a punta contra el backend real (sin mocks, con datos reales de Cuba de la base — La Habana/Playa, Matanzas/Varadero — y un país+estado temporal creado y borrado para el caso no-Cuba): registro de cliente en Cuba sin municipio → 400 "Selecciona tu municipio"; completo → 201, `User` con `provinceId`/`municipalityId` reales persistidos; registro con "otro país" ("Narnia") sin provincia → 201, `User` con `registrationCountryOther:"Narnia"` y provincia null; sin país ni "otro país" → 400; vendedor con provincia de Cuba sin municipio en `POST /vendors` → 400; completo → 201 con `VendorLocation.municipalityId` real; vendedor en país no-Cuba sin `companyAddress` → 400 "Indica la dirección de tu tienda"; con dirección → 201. Los 8 escenarios dieron el resultado esperado exacto — usuarios, sesiones, tiendas, `PendingRegistration` y el país/categoría de negocio temporales se borraron al terminar. Confirmado también visualmente en el navegador real con Playwright contra los 2 flujos (`/cuenta?tab=registro` y `/vendedor/ingresar?tab=registro`): el paso nuevo país→provincia→municipio carga y encadena bien en los 2, y el paso 4 del vendedor muestra el aviso "Responsable del negocio: `<nombre del paso 1>`" sin los 3 campos eliminados. `node --check` limpio en los 3 archivos backend tocados/nuevos, `vite build` limpio del frontend en los 2 archivos reescritos.
- **Bloque 114 (pedido explícito, con 2 capturas — registro de vendedor mostrando el campo "Nombre completo" de una pieza y el teléfono en 3 cajas separadas, más el panel de admin con 3 países cargados — "si tengo varios países agregados en el panel de admin me sale solo Cuba"):** 4 cambios sobre el registro del Bloque 113, todos del mismo pedido. **(1) Nombre → Nombre + Apellidos**: separados en 2 campos en el paso 1 de `Account.jsx` (se concatenan recién al armar el payload de `register()`, `buildRegisterPayload()` — el backend sigue guardando un solo `fullName`, no hizo falta tocar el schema). **(2) Teléfono en un solo campo**: `PhoneInput.jsx` (componente compartido, usado también en `Checkout.jsx`) pasa de 3 cajas con borde propio cada una a un único contenedor con un solo borde (`flex` + divisores internos en vez de `grid`+`gap`) — se ve y se comporta como un campo, no tres; se suma además un cuadro de aviso debajo ("Este es el número por el que los clientes te contactarán..."). **(3) La causa real de "me sale solo Cuba"**: `GET /locations/countries` (`listActiveCountries`) solo devolvía países con ≥1 provincia/estado YA cargado — España y Estados Unidos, recién agregados por el admin con "0 subdivisión(es)" (ver captura de `/admin/ubicaciones`), quedaban invisibles en el selector de registro aunque estuvieran activos; ese filtro tiene sentido para VendorVerification.jsx/VendorSettings.jsx (ahí SIEMPRE se elige una Province real, un país sin ninguna sería un select vacío, callejón sin salida), pero no para el registro después del punto (4). Se sumó un flag `?all=true` al mismo endpoint (`requireSubdivisions = req.query.all !== "true"`) que se salta ese filtro — Account.jsx/VendorOnboarding.jsx lo usan ahora, VendorVerification.jsx/VendorSettings.jsx siguen con el comportamiento de siempre (verificado en vivo: sin el flag, sigue devolviendo solo CUBA; con `?all=true`, las 3). **(4) Fuera de Cuba, "estado" pasa a ser texto libre** (ya no un select de `Province` type=STATE, que era precisamente lo que hacía necesario el filtro del punto 3): con Cuba, sigue exigiéndose provincia+municipio reales del catálogo (sin cambios); con cualquier otro país real del catálogo, ahora hace falta un "estado" y una "dirección" escritos a mano — el admin ya no necesita cargar provincias/estados de otros países para que el registro funcione, solo el país. Requirió cambios de schema: `User`/`PendingRegistration` suman `stateOther` (y reusan `address`, columna que ya existía sin usarse desde antes — ver `CustomerPanel.jsx`, que sí la edita post-registro); `VendorLocation.provinceId` pasa a ser opcional (antes NOT NULL, FK de `ON DELETE CASCADE` a `ON DELETE SET NULL`) y suma `countryId` (antes se inferría de `provinceId`, ya no se puede sin provincia) + `stateOther` (migración `20260909050000_registration_state_freetext`, con backfill de `countryId=CUBA` para las `VendorLocation` ya existentes). `registrationLocation.service.js` se reescribe: `resolveRealCountryLocation` ahora bifurca Cuba (provincia+municipio) vs. cualquier otro país real (estado+dirección libres); `assertVendorLocationComplete` pasa a requerir `countryId` explícito. **Bonus del mismo pedido ("no quiero preguntar la ubicación dos veces")**: el vendedor deja de tener un paso separado para "tu propia ubicación" — el paso 3 ahora se llama "configuración de la tienda" y su única respuesta de ubicación se manda a la vez como `User.registrationCountryId/...` (la persona) Y como `VendorLocation` (la tienda), bajando el registro de vendedor de 4 pasos a 3 (mismo número que el cliente, que sigue con su propio paso 3 de ubicación, sin cambios de fondo salvo los puntos 3 y 4 de arriba). Verificado en vivo contra el backend real, 5 escenarios sintéticos (España sin estado → 400; España con estado+dirección libres → 201, persiste `stateOther`/`address`, `provinceId` null; Cuba sigue exigiendo provincia+municipio reales sin regresión; registro de vendedor en España con un solo paso de ubicación → `User` Y `VendorLocation` quedan con `stateOther:"Madrid"` y `provinceId` null; `POST /vendors` sin `countryId` → 400 de zod) — los 5 dieron el resultado esperado exacto, datos de prueba borrados al terminar. Confirmado también visualmente con Playwright contra los 2 flujos reales: paso 1 con Nombre/Apellidos separados y el teléfono en una sola caja con el aviso debajo; paso 3 del vendedor en 3 (no 4) mostrando el selector de país con los 3 países reales del admin, cambiando a provincia+municipio con Cuba y a estado+dirección de texto libre con España; paso 3 del cliente con el mismo comportamiento. `node --check` limpio en los 4 archivos backend tocados, `vite build` limpio del frontend en los 3 archivos reescritos (`Account.jsx`, `VendorOnboarding.jsx`, `PhoneInput.jsx`).
- **Bloque 115 (pedido explícito, amplio — "revisa todo mi código, está desorganizado... une secciones en una sola ventana con pestañas... marca los campos obligatorios... prueba todo creando cuentas y productos... el link de una tienda debe mostrar la vista previa de la tienda, no la del marketplace"):** auditoría real (3 sub-agentes de exploración, con grep/lectura de código, no especulación) de los paneles admin/vendedor/cliente + pruebas en vivo de punta a punta (cuenta de vendedor nueva, cuenta de admin nueva, producto con imagen por link externo). Resultado: **1 bug real encontrado y arreglado**, **1 hueco real de producción cerrado**, **1 reorganización real aplicada**, y **varias sospechas del pedido descartadas con evidencia** (documentado así para no repetir la misma investigación después). **(1) Bug real, reproducido 2 veces en vivo:** `lib/toast.jsx` (wrapper propio sobre react-hot-toast, con Bloque histórico que migró toda la API a `toast.success()/.error()/.warning()/.info()` como MÉTODOS de un objeto, ya NO una función invocable) — 2 call sites viejos que nunca se migraron seguían llamando `toast(mensaje, { icon })` como si `toast` todavía fuera la función cruda de react-hot-toast: `VendorProducts.jsx:326` (aviso de "no pudimos confirmar que el link cargue una imagen" al agregar una foto por URL) y `VendorVerification.jsx:115` ("Pago cancelado" al volver de Stripe). Cada vez que corrían, `toast(...)` tiraba `TypeError: toast is not a function`, y ese error terminaba mostrándose como el fallback genérico de un `onError` de mutación ajeno — el resultado real para el vendedor: la imagen SÍ se guardaba (confirmado con `POST /products/:id/images/link` → 201, imagen persistida en la base) pero la pantalla le decía "No se pudo agregar el link.", una falsa alarma que además esconde el aviso real y útil ("revisa que se vea bien en la tienda"). Se cambian los 2 call sites a `toast.warning(...)`/`toast.info(...)`. Reproducido en vivo 2 veces con URLs de Unsplash reales (el chequeo de accesibilidad del backend, `isUrlReachable`, les daba HEAD/GET 404 — CDNs que bloquean fetches no-navegador, comportamiento esperable y ya diseñado como no bloqueante) y confirmado arreglado con una tercera URL tras el fix: el aviso correcto ("No pudimos confirmar...") reemplazó al error falso. **(2) Campos obligatorios, antes invisibles hasta fallar el submit:** `Input.jsx`/`Select.jsx`/`PasswordInput.jsx`/`PhoneInput.jsx` (los 4 componentes compartidos que arman TODOS los formularios del sitio) ahora leen su propio prop `required` y agregan un asterisco rojo junto al label automáticamente — un solo cambio, en un solo lugar cada uno, corrige la inconsistencia en TODO el sitio de una vez (antes solo 2 campos de `VendorProducts.jsx` tenían asterisco puesto a mano; el resto dependía del atributo HTML nativo, invisible). Verificado en vivo: el registro de cliente/vendedor y el login de admin ya muestran "Nombre *", "Correo *", "Contraseña *", etc. **(3) Vista previa al compartir: hueco real de producción, no un bug de hoy.** La inyección dinámica de `<title>`/OG/Twitter por tienda/producto (`vite-plugins/ogMeta.js`, ya existía) solo corre en el servidor de DESARROLLO de Vite — confirmado que NO hay ningún equivalente del lado de Express para un build de producción, y que `index.html` no tenía NINGÚN meta tag estático de respaldo (ni siquiera el genérico del marketplace) — es decir, hoy mismo, en cualquier hosting estático de producción, compartir CUALQUIER link (tienda, producto, o el sitio en sí) no mostraría ninguna vista previa. Se agregaron meta tags estáticas de respaldo a `index.html` (el genérico de ZeuDin, nunca vacío) y se corrigió `ogMeta.js` para que en dev SIGA mandando (con `stripExistingMeta`, saca las estáticas antes de inyectar las resueltas — si no, quedaban duplicadas: 2 `<title>`, 2 `og:title`, etc.). Verificado en vivo contra el dev server: home → genérico; `/tienda/sabor-criollo` → "Sabor Criollo — ZeuDin" con su descripción real; `/producto/moda-caribe/camiseta-algodon-unisex` → nombre real del producto — sin duplicados en ningún caso. **Falta un equivalente de producción real** (un middleware Express que inyecte lo mismo al servir `dist/index.html`) — no se construyó a ciegas porque el proyecto todavía no tiene decidido cómo se va a hospedar el frontend en producción (memoria: "100% local dev"); queda como pendiente concreto para cuando se decida el hosting. **(4) Reorganización real aplicada, la más clara del pedido:** `VendorSettings.jsx` (1037 líneas, 8 tarjetas de configuración apiladas una debajo de otra sin ninguna pestaña — moneda, métodos de pago, cómo recibir pedidos, visibilidad pública/privada, cobertura y países de entrega, datos de facturación, garantías, documento IA) se reagrupa en 3 pestañas ("General", "Cobertura y visibilidad", "Facturación y otros") usando un componente nuevo y compartido, `components/ui/Tabs.jsx` (no existía ninguno en el proyecto — ahora reusable para el resto de la reorganización pendiente, ver más abajo). Cambio puramente de presentación: ninguna query, mutación, ni el guardado en bloque (`handleSaveAll`) se tocó. Verificado en vivo con una sesión real inyectada: las 3 pestañas cambian el contenido visible correctamente, sin mezclar secciones entre sí. **(5) Sospechas del pedido investigadas y descartadas, con evidencia (para no reabrirlas después):** *"Países y provincias" del admin como sección sin usar* — falso; grep confirma uso activo en 8+ lugares (registro Bloque 113/114, VendorSettings, VendorVerification, Checkout, CustomerPanel, AdminCampaigns, LocationContext del storefront). *Tienda privada no funciona* — funciona tal cual se pidió: oculta de listados/buscador/recomendaciones (`isPrivate`, excluida en `listVendors`/`search.controller.js`/etc.) pero 100% accesible por el link directo (`getVendorBySlug` nunca chequea `isPrivate`) — es el diseño correcto, no un bug. *Duplicación entre `VendorProfile.jsx` y `VendorSettings.jsx"* (reportada inicialmente por un sub-agente) — descartada tras grep directo: `VendorSettings.jsx` NO edita companyName/description/category desde el Bloque 47 (comentario propio del código: "se mudó a Mi perfil"), no hay campo repetido. *Falta de anuncios para vendedores* — confirmado real pero es una limitación de diseño conocida, no un bug: los anuncios (banners de imagen/texto en el home) son exclusivos del admin (`AdminAnnouncements.jsx` → `AnnouncementPopup.jsx`, un popup de pantalla completa montado en `PublicLayout.jsx`, NO un contenedor dentro de `Home.jsx` como se describió en el pedido); lo más cercano que tiene un vendedor es "Ofertas" (descuento de un producto puntual, visible en el slider "Ofertas de la semana" del Home) — no un banner libre. *`pg_trgm` sin instalar* (item pendiente de la auditoría 2026-08-06) — ya estaba instalado, confirmado en vivo (`SELECT extname FROM pg_extension`). *92 errores sin resolver en Admin → Errores* — revisados por mensaje agrupado: la gran mayoría son de configuración (API keys de IA/Resend inválidas o de prueba, límites del sandbox de Resend) y 16 eran un `TypeError` real en `reports.controller.js` pero fechados 2026-08-23 con rutas `E:\AGENCIAS\...` (la ubicación VIEJA del proyecto, antes de moverse a `C:\PROJECT\...`) — confirmado en vivo que `prisma.report.findMany` funciona hoy sin error; son logs viejos, no un bug vigente. **Prueba de punta a punta en vivo** (creación real, no simulada): cuenta de vendedor nueva vía el registro real (paso 1→2→3, con verificación de código forzada por script) → tienda "Ferretería Carlos Test" creada en Holguín → producto "Martillo de acero 16oz" publicado → imagen agregada por link externo (acá se encontró y arregló el bug del punto 1) → confirmado visible en `/vendedor/productos` con estado "Activo". Cuenta de administrador nueva (creada directo en la base, ya que el registro de admin es intencionalmente cerrado — "un admin no se autocrea") → login real con 2FA (código forzado por script) → panel `/admin` carga limpio, con la tienda recién creada visible en el feed de actividad y el conteo de "Tiendas por provincia". Toda la data de prueba (cuentas, tienda, producto, sesiones) se borró al terminar. `vite build` limpio en los 6 archivos frontend tocados (`Input.jsx`, `Select.jsx`, `PasswordInput.jsx`, `PhoneInput.jsx`, `Tabs.jsx` nuevo, `VendorSettings.jsx`, `VendorProducts.jsx`, `VendorVerification.jsx`, `index.html`, `vite-plugins/ogMeta.js`). **Pendiente real para un bloque futuro** (alcance demasiado grande para uno solo, documentado para no perderlo): aplicar el mismo patrón de pestañas (`Tabs.jsx`) al resto de candidatos ya identificados con evidencia — admin: páginas estáticas (Términos, FAQ, Contacto, Centro de ayuda → 1 sola sección con pestañas), Marca (ruta huérfana `/admin/marca`, ya casi replegada a Mi perfil), Suscripciones/Ofertas/Ubicaciones (cada una mezcla una lista con una tarjeta de configuración global); vendedor: unir Ofertas/Códigos de descuento/Ofertas de tienda en una sección "Promociones", unir Verificación+Pago manual; y el middleware Express de OG tags para producción, cuando se decida el hosting.
- **Bloque 116 (pedido explícito, con captura — "las tarjetas de cada producto deben mostrar cuántas reseñas y la escala de reseñas, igual que las tiendas, encima del título y debajo de la imagen" + "arregla cómo se ven las cantidades del carrito en la tarjeta, se ve mal con varios productos"):** 2 cambios independientes sobre `ProductCard.jsx`/`AddToCartControl.jsx`. **(1) Estrellas por producto, antes código muerto:** `ProductCard.jsx` ya tenía un `<StarRating value={product.rating} .../>` desde antes, pero NINGÚN endpoint de listado (Home, catálogo, tienda, búsqueda) le mandaba nunca ese campo al producto — a diferencia de `Vendor.rating`, que SÍ es una columna real recalculada en cada reseña (`reviews.controller.js`), `Product` no tenía ninguna columna de calificación propia; el único lugar que la calculaba era la ficha de UN producto (`getProductBySlug`, con un aggregate al vuelo), nunca un listado de varios. Se agregan `Product.rating`/`Product.reviewCount` como columnas denormalizadas (mismo criterio que `Vendor.rating`, migración `20260910010000_product_rating` con backfill real desde las reseñas ya existentes) y una función nueva, `recalculateProductRating(productId)`, cableada en los 4 lugares donde ya se recalculaba el rating de la tienda (`createReview`, `setReviewHidden`, `resolveReviewReport`, `deleteReview`) — así cualquier endpoint que ya devuelve un `Product` (todos usan `include`, no un `select` recortado) trae la calificación gratis, sin tocar cada uno. En `ProductCard.jsx`, el bloque de estrellas se movió de cerca del precio (abajo del todo) a justo debajo de la fila del nombre de la tienda y ARRIBA del título del producto — pedido explícito de "encima del título, debajo de la imagen" — reusando `count` de `StarRating.jsx` (ya soportaba mostrar "(N)", solo nadie se lo pasaba) para mostrar la cantidad de reseñas junto a las estrellas, igual que ya se ve en `StoreCard.jsx`. Solo se muestra si el producto ya tiene ≥1 reseña con estrellas — nunca un falso "0.0 ★ (0)". Verificado en vivo contra `/catalogo` real: 6 productos con reseñas reales de la base (backfill) muestran sus estrellas + cantidad justo debajo de la imagen y antes del título (confirmado por inspección directa del DOM, orden real de los hijos del contenedor). **(2) Cantidad del carrito rota con números de varios dígitos, reproducido con el mismo caso de la captura (512):** el stepper (−/cantidad/+) de `AddToCartControl.jsx` reusaba el mismo ancho CUADRADO fijo (`w-9`/`w-[34px]`) pensado para el botón de un solo ícono de "agregar" — con una cantidad de 3+ dígitos, el centro no tenía espacio real y el número se superponía visualmente con los botones. Se separó: los botones −/+ pasan a un ancho fijo chico (28px/`w-8`, antes ocupaban 1/3 del cuadrado cada uno), y el número del medio pasa a `flex-1` con `min-width` en el contenedor en vez de ancho fijo — así es SIEMPRE el número el que se estira para hacerle lugar a más dígitos, nunca los botones los que se achican. Reproducido el bug exacto de la captura inyectando un carrito con cantidad 512 directo en `localStorage` (mismo mecanismo que usa `CartContext.jsx`) contra un producto real, confirmado roto ANTES del fix (con el `dim` viejo) y arreglado DESPUÉS: los 3 elementos (botón −, "512", botón +) miden 28px/28.2px/28px respectivamente, sin superposición, el contenedor crece de 76px a 85.5px para acomodarlos — verificado con las coordenadas reales del DOM (`getBoundingClientRect`), no solo visualmente. `node --check` limpio en `reviews.controller.js`, `vite build` limpio en `ProductCard.jsx`/`AddToCartControl.jsx`.
- **Bloque 117 (pedido explícito, corrige el criterio del Bloque 69 — "el tope debe ser un comentario por PRODUCTO por día, no por tienda — la misma cuenta puede reseñar 2 productos de la misma tienda el mismo día, cada uno es separado"):** `createReview` (`reviews.controller.js`) dedupaba reseñas contra `{ vendorId, userId, createdAt }` — un cliente que ya había reseñado el producto A de una tienda no podía reseñar el producto B de esa MISMA tienda hasta el otro día, aunque fueran productos totalmente distintos. Se suma `productId: data.productId ?? null` al chequeo: ahora una reseña de un producto puntual dedupa contra `{ vendorId, productId, userId }` (cada producto es su propio balde de "1 por día", sin importar la tienda), y la reseña GENERAL de una tienda (`Store.jsx`, sin producto) sigue dedupando por `{ vendorId, productId: null, userId }` — su propio balde aparte, independiente del de cualquiera de sus productos. Mensaje de error también se bifurca: "Ya reseñaste este producto hoy..." si venía con `productId`, el mensaje viejo ("...en esta tienda...") si era una reseña general. Texto de `Product.jsx` actualizado de "1 comentario por día por tienda" a "1 comentario por día por producto" (`Store.jsx` no necesitó cambios — nunca mencionaba el tope por tienda explícitamente). Verificado en vivo contra el backend real con un cliente y una tienda con 2 productos reales, 5 escenarios: reseñar producto A → 201; reseñar producto B de la MISMA tienda el mismo día → 201 (antes del fix, esto daba 429 — es el bug que se corrige); reseñar producto A de nuevo el mismo día → 429 con el mensaje nuevo; reseña general de la tienda → 201 (balde aparte); reseña general de nuevo el mismo día → 429 con el mensaje viejo. Los 5 dieron el resultado esperado exacto — reseñas de prueba borradas y `Vendor.rating`/`Product.rating`/`reviewCount` recalculados de vuelta a sus valores reales al terminar (no quedaron desactualizados por las reseñas de prueba ya borradas). `node --check` limpio, `vite build` limpio.
- **Bloque 118 (pedido explícito, follow-up del Bloque 117 — "quiero poder cambiar esos valores de comentarios por tienda y por producto y por día desde el panel de admin, y poder habilitar/deshabilitar que los vendedores comenten"):** los 2 topes del Bloque 117 (1 por producto, 1 por tienda, ventana de 24h) eran constantes hardcodeadas (`REVIEW_DEDUP_HOURS`) — se mueven a `SiteSettings` como 3 columnas nuevas (`reviewDedupHours`, `maxReviewsPerProductPerPeriod`, `maxReviewsPerStorePerPeriod`, migración `20260910020000_review_policy_settings`, defaults = los mismos valores que ya estaban hardcodeados, sin cambiar el comportamiento visible al desplegar) más una cuarta, `vendorsCanReview` (booleana, default `true`) para el segundo pedido — "los vendedores no pueden agregar comentarios a ninguna tienda ni producto". `createReview` (`reviews.controller.js`) pasa de un `findFirst` booleano ("¿ya comentó?") a un `count()` comparado contra el máximo configurado (`getReviewPolicy()`, nueva función en `settings.controller.js`, mismo criterio interno que `getOfferPolicy` — nunca una ruta HTTP directa, cualquier controller que la necesite la importa), y gana un chequeo nuevo al principio: si `req.user.role === "VENDOR"` y `vendorsCanReview` está en `false`, rechaza con 403 ANTES de tocar la tienda/producto — bloquea a cualquier cuenta vendedor, incluida la dueña de la tienda que está comentando (sin excepción de "puede reseñarse a sí misma"); las cuentas CUSTOMER nunca se ven afectadas por este interruptor. **UI del admin, en `AdminReviews.jsx` (pedido explícito: "en donde mejor creas en configuración")**: se sumó `ReviewPolicyCard`, una tarjeta embebida arriba del listado de comentarios — mismo patrón exacto que `OfferPolicyCard` en `AdminOffers.jsx` (ya precedente en el código para "lista + su propia tarjeta de política global"), con 3 campos numéricos (ventana en horas, máximo por producto, máximo por tienda, cada uno con su explicación de una línea) y un checkbox con su descripción completa para `vendorsCanReview` — nunca un toggle sin explicar qué hace. `PATCH /admin/settings/review-policy` nuevo, mismo formato de respuesta que `offer-policy`. **Bonus, mismo turno**: el texto que ve el cliente en `Product.jsx`/`Store.jsx` ("1 comentario por día por producto/tienda") pasa de estar escrito a mano a generarse con una función compartida nueva (`lib/reviewPolicy.js`, `describeReviewLimit`) a partir de los valores REALES configurados (expuestos ahora en `GET /settings` y en `usePlatformSettings()`) — si el admin cambia el máximo a 3, el texto que ve el cliente dice "3 comentarios por día" solo, sin tocar código. Verificado en vivo de punta a punta contra el backend y el admin reales: la tarjeta carga los valores reales guardados (24h/1/1); se cambió a máximo 3 por producto + `vendorsCanReview` desactivado, guardado confirmado (toast + `GET /settings` reflejando los 4 valores nuevos); con una cuenta VENDOR real, `POST /reviews` devolvió 403 con el mensaje del bloqueo; con una cuenta CUSTOMER real sobre el mismo producto, 3 intentos seguidos dieron 201 y el 4º dio 429 ("Ya alcanzaste el límite..."), confirmando que el nuevo máximo de 3 se respeta de verdad. Se revirtió la política a sus valores originales (24h/1/1/`true`) y se borraron reseñas/sesiones de prueba al terminar. `node --check` limpio en los 3 archivos backend tocados, `vite build` limpio en los 4 archivos frontend tocados/nuevos (`AdminReviews.jsx`, `usePlatformSettings.js`, `Product.jsx`, `Store.jsx`, `lib/reviewPolicy.js` nuevo).
- **Bloque 119 (pedido explícito — "configurar el preloader de mi sitio web a partir de logo-loader.js"):** `logo-loader.js` es un Web Component propio ya armado por el usuario (no escrito en este bloque) — un Custom Element `<logo-loader>` con el logo (ícono de tiendita: toldo, cuerpo, puerta, letrero) dibujándose en loop vía SVG + CSS keyframes, sin ninguna dependencia (ni de React ni de ninguna librería). **Problema real encontrado antes de conectarlo**: el archivo vivía en `src/assets/images/`, dentro del árbol que Vite empaqueta — un `<script type="module" src="/src/assets/images/logo-loader.js">` en `index.html` hace que Vite lo FUSIONE en el mismo chunk que `main.jsx` (confirmado con un build real: el script tag desaparecía de `dist/index.html`, movido a un único `<script>` en el `<head>` apuntando al bundle de React ya unido). Eso rompe la idea completa de un preloader: el Custom Element recién se registraría cuando TODO React ya terminó de descargar/parsear/ejecutar — exactamente el momento en que ya no hace falta taparle nada al visitante. Se movió el archivo a `frontend/public/logo-loader.js` (fuera del bundler, Vite lo sirve tal cual en dev y en build) y se referencia con un `<script src="/logo-loader.js">` normal, sin `type="module"` — confirmado con un segundo build que el tag ya no desaparece y `dist/logo-loader.js` queda copiado verbatim. **Implementación**: `index.html` suma un `<div id="app-preloader">` de pantalla completa (fondo navy `#0e1a28`, la marca de `tailwind.config.js`) con `<logo-loader ink="#FFFFFF" accent="#FE9800" size="140">` adentro — vive fuera de `#root`, a propósito, para pintarse de una sin esperar a React. `components/layout/AppPreloader.jsx` (nuevo, montado en `App.jsx` junto a `RouteLoader`) es quien lo apaga: en un `useEffect` que corre una sola vez tras el primer render, espera un mínimo (700ms, para que nunca se sienta como un parpadeo "prendido y apagado" en una carga rápida/cacheada) y recién ahí lo desvanece (`transition: opacity 400ms` puesto a mano en el `style` inline del div, ya que Tailwind/index.css todavía no cargó en ese instante) y lo saca del DOM. Verificado en vivo con Playwright: el Custom Element se registra (`customElements.get("logo-loader")` → true), el SVG se pinta con los colores/tamaño correctos dentro de su Shadow DOM (confirmado leyendo las CSS custom properties `--accent`/`--ink` ya resueltas, y con una captura de pantalla real mostrando el ícono navy+blanco centrado en pantalla completa), y el div se remueve solo del DOM después de la ventana de tiempo esperada — probado también alargando la espera a 15s para confirmarlo visualmente sin la carrera contra el tiempo de cada tool call, y revertido a 700ms al terminar. `vite build` limpio, sin errores de consola nuevos.
- **Bloque 120 (pedido explícito, corrige el criterio del Bloque 119 — "el loader debe mostrarse hasta que dé toda su animación completa; si el contenido de la página todavía no cargó, se mantiene hasta que cargue, y ENTONCES desaparece"):** el Bloque 119 apagaba el splash con un mínimo fijo de 700ms (pensado solo para "no parpadear en una carga instantánea"), sin relación real ni con la animación del logo ni con si la página ya cargó de verdad — se rehace el criterio de apagado con las 2 condiciones reales pedidas, cumplidas LAS DOS a la vez (la que tarde más manda). Se saca toda la lógica de `components/layout/AppPreloader.jsx` (archivo borrado — ya no hace falta, ver por qué abajo) y se mueve a un `<script>` plano dentro de `index.html`, justo después del splash: **(1) Vuelta completa de la animación**: `ANIM_MS = 4250` (tiene que coincidir con `duration="4.25"` del `<logo-loader>`, agregado explícito en el markup en vez de dejarlo en el default implícito de antes), medido desde `Date.now()` al momento en que este script corre — que es ANTES de que React exista siquiera, a diferencia del Bloque 119 (ahí el reloj arrancaba recién cuando `AppPreloader.jsx` montaba, que es más tarde: el SVG ya llevaba un rato animándose sin que nada lo contara). **(2) Página cargada de verdad**: `window.load` (no `DOMContentLoaded`, que dispara con el HTML parseado nomás — antes de que el bundle de React o cualquier imagen terminen de bajar; `load` es la señal real y estándar del navegador de "esta página ya cargó por completo"), con un chequeo de `document.readyState === "complete"` primero por si ya pasó (carga desde caché). El splash se apaga recién cuando `pageLoaded` es `true` Y ya pasaron los 4250ms completos desde que arrancó — si la página tarda más que la animación, la animación simplemente sigue en loop hasta que la página esté lista (nunca se corta a la mitad); si la animación tarda más que la carga, se espera el resto de la vuelta igual. Verificado en vivo con Playwright, alargando `ANIM_MS` a 20000 para separar las 2 señales con margen real: navegando a una página que YA reportaba `document.readyState === "complete"` (`window.load` disparado, confirmado), el splash seguía 100% visible (`opacity: "1"`) — probando que NO se apaga solo porque la página cargó, tiene que cumplirse la animación completa también; esperando el resto de los 20s, se confirmó que se apagó solo. Revertido a 4250ms al terminar. `vite build` limpio, sin errores de consola nuevos.
- **Bloque 121 (pedido explícito — "agregar el nombre de la plataforma debajo del loader, el mismo nombre dinámico que se actualiza en todo el sitio desde el admin, con una animación parecida al loader"):** `#app-preloader` (Bloque 119/120) pasa de un solo hijo centrado a `flex-direction:column` con un `<div id="app-preloader-name">` nuevo debajo del `<logo-loader>`. Arranca con el mismo fallback ya establecido en `lib/usePlatformSettings.js` ("ZeuDin" — nunca vacío) y, apenas corre el script del splash, dispara un `fetch("%VITE_API_URL%/settings")` — el MISMO endpoint y el MISMO campo `siteName` que ya usa toda la app vía `usePlatformSettings()` (nunca un valor propio aparte) — y actualiza el texto si responde bien; si falla o tarda, se queda con el fallback sin bloquear nunca el apagado del preloader (esa lógica del Bloque 120 no depende de este fetch). `%VITE_API_URL%` es el reemplazo de HTML nativo de Vite (funciona en dev y en build) — no se podía usar `import.meta.env.VITE_API_URL` porque este es un `<script>` plano, fuera del sistema de módulos. La animación de entrada/salida del nombre (`preloader-name-pulse`, fade + `translateY`) usa los mismos puntos de tiempo (9%/17%/92%/100%) que el keyframe `enter` del logo, para que los dos elementos aparezcan/desaparezcan en el mismo compás — el keyframe del logo vive dentro de su Shadow DOM (inaccesible desde afuera), así que se define un equivalente propio. Verificado en vivo con Playwright (alargando la animación a 20s para separar la carrera contra el tiempo de cada tool call): el nombre real configurado en el admin ("SuperMarket") reemplazó al fallback, confirmado leyendo el DOM y con una captura de pantalla real mostrándolo debajo del logo; forzando el fetch a una URL rota, el nombre se quedó en "ZeuDin" sin romper nada. Revertido todo al terminar. `vite build` limpio.
- **Bloque 122 (pedido explícito, follow-up del Bloque 121 — "el nombre también debe tener una animación parecida al loader, donde la letra se ve pálida/gris y se va rellenando de blanco como escritura de lápiz, y la primera letra siempre en el color accent/naranja del loader"):** se reemplaza el fade simple del nombre por el mismo truco de 2 capas que usa `logo-loader.js` para "dibujar" el logo (una copia PÁLIDA de fondo, siempre visible tal cual, más una copia SÓLIDA encima que se revela) — 2 `<span>` (`#app-preloader-name-pale` y `#app-preloader-name-solid`) con el mismo texto, superpuestos (`position:absolute` la de arriba). La capa sólida usa un nuevo keyframe, `preloader-name-draw`, que anima `clip-path: inset(0 100%→0% 0 0)` — un barrido de izquierda a derecha que revela el texto sólido por encima del pálido, letra por letra, en vez de aparecer de golpe (el mismo efecto visual que el `stroke-dashoffset` que usa el logo para "dibujarse", adaptado a texto plano vía `clip-path` en vez de trazos SVG, que no aplica a caracteres). Cada capa arma su HTML con una función nueva, `nameLayerHTML()`, que separa la PRIMERA letra del resto: primera letra siempre en accent/pale-accent (naranja, `#FE9800`/`rgba(254,152,0,.3)`), el resto en ink/pale-ink (blanco, `#FFFFFF`/`rgba(255,255,255,.28)`) — los 4 valores son copia exacta de los atributos ya puestos en el `<logo-loader>` de arriba ("siempre utilizará los colores que tiene el loader", pedido explícito, nunca una paleta propia). `renderPreloaderName(name)` arma las 2 capas de una — se llama tanto con el fallback "ZeuDin" al arrancar como con el nombre real cuando responde el fetch del Bloque 121. Verificado en vivo con Playwright (misma técnica de alargar la animación a 20s): inspeccionando el DOM se confirmó el HTML de las 2 capas con los colores correctos por letra, y con `getComputedStyle` se confirmó el `clip-path` en pleno barrido (`inset(0px 0% 0px 0px)` = totalmente revelado en ese instante); una captura de pantalla real agarró el momento justo de la escritura en progreso — la "S" de "SuperMarket" ya visible en naranja sólido mientras "uperMarket" todavía se veía gris/pálido detrás, confirmando el efecto de "escritura letra por letra" tal cual se pidió. `vite build` limpio.
- **Bloque 123 (pedido explícito, corrige el criterio del Bloque 122 — "no quiero que sea como que pasa y vuelve para atrás, quiero un relleno letra por letra, todas las letras juntas a la vez, como un hormigueo, una animación moderna — y que la fuente tenga bordes redondeados"):** el Bloque 122 barría el nombre COMPLETO con un único `clip-path` de lado a lado — visualmente se leía como una sola cortina corriéndose (y, al repetirse en loop, "volviendo para atrás"), no como letras rellenándose por separado. Se reescribe entero: se sacan las 2 capas superpuestas (`#app-preloader-name-pale`/`-solid`, el `clip-path` y el keyframe `preloader-name-draw`) y se reemplazan por UN solo contenedor donde cada letra del nombre es su propio `<span>` (`nameLayerHTML()` reescrita: un loop por carácter, ya no 2 llamadas de "primera letra + resto"), cada uno con su propio par de variables CSS `--pale`/`--solid` (primera letra siempre accent/pale-accent naranja, el resto ink/pale-ink blanco — "siempre los colores del loader", sin cambios de fondo) y su propio `animation-delay` escalonado en pasos chicos de 45ms (`LETTER_STAGGER_MS`) sobre un keyframe nuevo, `preloader-letter-fill` (opacity+scale+translateY "pop" + color pálido→sólido→pálido, todo dentro del mismo ciclo de 4.25s). El escalonado de 45ms por letra es justo lo que arma el "hormigueo" pedido: en cualquier instante, las letras más a la izquierda van un poco más adelantadas en su propio ciclo que las de la derecha — no es una letra esperando a que termine la anterior (eso sería una máquina de escribir), es un desfase chico y simultáneo, visualmente una ola rápida en vez de una cortina. **Fuente con bordes redondeados**: se suma Fredoka (Google Fonts, `wght@600;700`) al mismo `<link>` de fuentes que ya carga Montserrat/Inter — SOLO para `#app-preloader-name` (`font-family:'Fredoka','Montserrat',sans-serif`), nunca reemplaza la tipografía del resto del sitio (`tailwind.config.js` intacto); Fredoka tiene terminales redondeadas de fábrica, a juego con los trazos `stroke-linecap:round` que ya usa el logo del `<logo-loader>`. Verificado en vivo con Playwright (mismo truco de alargar `ANIM_MS` para observar con margen): se confirmó que "SuperMarket" se parte en 11 `<span>` individuales, cada uno con su `animation-delay` incremental (0ms, 45ms, 90ms... 450ms) y sus variables `--pale`/`--solid` correctas por letra; leyendo el color computado real de cada letra en un mismo instante se confirmó un DEGRADADO de opacidad de izquierda a derecha (la "S" ya 100% sólida, las últimas letras todavía relleñándose) — la prueba directa de que es un hormigueo escalonado y no una cortina única; una captura de pantalla real confirmó visualmente la fuente redondeada (Fredoka) y la primera letra en naranja. `vite build` limpio.
- **Bloque 124 (pedido explícito, corrige el criterio del Bloque 123 — "no quiero que las letras se vean flotando/moviéndose, quiero que sean fijas — que solo se vayan rellenando de color suavemente como escritura, con la misma animación que tiene el logo"):** el Bloque 123 le sumaba a cada letra, además del cambio de color, un `opacity`+`scale`+`translateY` ("pop" de entrada/salida) — el resultado se leía como que las letras flotaban/rebotaban, no como que se escribían en su lugar. Se reescribe el keyframe `preloader-letter-fill` para que SOLO anime `color` (mismo criterio que `drawAwning`/`drawBody` de `logo-loader.js`: esos tampoco mueven nada, revelan el trazo en el lugar fijo donde ya está dibujado) — se sacan `opacity`/`transform` del keyframe y el `display:inline-block` que solo hacía falta para que el `scale` se viera bien (ya no aplica). Las letras quedan clavadas en su posición siempre, nunca se mueven ni aparecen/desaparecen de golpe — solo pasan de pálidas a sólidas y de vuelta, con el mismo escalonado de 45ms por letra de antes (el "hormigueo" ahora es 100% de color, no de movimiento). El grupo completo (`#app-preloader-name`, `preloader-name-pulse`) sigue con su propio fade+translateY de entrada/salida al principio/final del ciclo — eso es intencional y no cambió (mueve el bloque ENTERO una sola vez por ciclo, en el mismo compás que el logo completo también entra/sale con scale+opacity; no es "una letra flotando", es el mismo tipo de respiración que ya tenía el logo desde el Bloque 119). Verificado en vivo con Playwright: comparando `offsetLeft`/`offsetTop` de cada letra (posición RELATIVA a su contenedor, sin el movimiento del grupo) en 2 instantes separados por 1.5s, las posiciones dieron exactamente iguales (`relativePositionsIdentical: true`) mientras el color sí cambió (`colorsChanged: true`) — confirma que ninguna letra se mueve individualmente, solo cambia de color. `vite build` limpio.
- **Bloque 125 (pedido explícito, escalación técnica precisa del Bloque 124 — "convertido a paths (una `<path>` por letra), animación CSS de 'stroke draw-on': `fill:none`, stroke blanco, `stroke-width` X, `stroke-linecap`/`stroke-linejoin:round`, `pathLength='1'`, `stroke-dashoffset` de 1 a 0 con `stroke-dasharray:1 1`, easing `ease-in-out`, delay escalonado ~0.12s por letra, fill apareciendo con transición de opacidad al terminar el trazo, ~1.5s de trazo dentro de un loop de 4.25s"):** el truco de color-only del Bloque 124 (CSS `color` interpolando sobre texto normal) no es lo mismo que un trazo SVG de verdad — el pedido es literalmente aplicarle al nombre la MISMA técnica que ya usa `logo-loader.js` para dibujarse (`pathLength="1"` + `stroke-dasharray:1 1` + `stroke-dashoffset` animado), pero sobre los contornos reales de cada letra. Como el nombre es 100% dinámico (texto configurado desde el panel de admin, nunca fijo), no alcanza con SVGs pre-dibujados a mano — hace falta convertir CUALQUIER texto a paths de verdad en tiempo real. No hay API nativa del navegador para eso; se resuelve con **opentype.js** (MIT), que parsea un archivo de fuente real y expone `font.getPath(texto, x, y, tamaño).toPathData()` (contorno vectorial exacto de cada carácter) y `font.getAdvanceWidth()` (para ubicar la siguiente letra). Mismo criterio de auto-hosting que ya se usa en todo este preloader (nunca CDN externo, todo servido desde `public/` tal cual, sin pasar por el bundler de Vite — ver Bloque 119): se copió el build UMD de `opentype.js` a `public/opentype.min.js` (instalado con `npm install --no-save`, copiado, y desinstalado con `npm uninstall --no-save` — cero rastro en `package.json`/`package-lock.json`, confirmado con `git status`) y se descargó el archivo real de la fuente Fredoka (variable, ejes weight+width, licencia OFL) directo del repo oficial `google/fonts` en GitHub a `public/fonts/Fredoka.ttf` — NO la versión servida por Google Fonts vía CSS, que viene en WOFF2/Brotli y `opentype.js` no la parsea de forma confiable. **Implementación**: `buildNameSVG(font, name)` arma un `<svg>` con un `<path>` real por letra (sin contar espacios, que solo avanzan el cursor) — cada uno con `pathLength="1"`, `fill-opacity:0` inicial, `stroke-dasharray:1 1`/`stroke-dashoffset:1` inicial, `stroke-linecap`/`stroke-linejoin:round`, color = accent naranja para la primera letra / ink blanco para el resto (mismos 2 colores exactos del logo, sin cambios), y 2 animaciones CSS nuevas en paralelo: `preloader-letter-stroke` (`stroke-dashoffset` 1→0, el trazo dibujándose) y `preloader-letter-fill-in` (`fill-opacity` 0→1, el relleno apareciendo recién cuando el trazo ya terminó) — ambas dentro del mismo ciclo de 4.25s del logo (0%→21% ≈ 0-0.9s el trazo, 21%→35% ≈ 0.9-1.5s el relleno, sostenido hasta 92%, reset a 100% listo para el próximo loop), con `animation-delay` escalonado de `0.12s` por letra (`LETTER_STAGGER_S`) para que se dibujen en secuencia, no todas a la vez. `renderPreloaderName(name)` arma el SVG si la fuente ya cargó; si no (o si `opentype.min.js` no llegó a cargar), cae a texto plano sin animar — el preloader nunca se rompe por esto. Verificado en vivo con Playwright (alargando `ANIM_MS` para observar con margen): se confirmaron los `<path>` reales en el DOM con `pathLength="1"`, `d` con datos de contorno reales, colores correctos por letra y `animation-delay` incremental en pasos de 0.12s; capturas de pantalla confirmaron visualmente el trazo dibujándose letra por letra con terminales redondeadas y el relleno apareciendo después. `vite build` limpio; se confirmó que `dist/fonts/Fredoka.ttf` y `dist/opentype.min.js` quedan copiados verbatim junto al resto de `public/`.
- **Bloque 126 (2 correcciones puntuales, mismo turno del Bloque 125):** **(1) Pedido explícito — "no debería poner ZeuDin al principio y luego flashear rápido y poner el nombre real configurado en el panel":** el Bloque 121 pintaba el fallback "ZeuDin" de una al arrancar el script y recién lo reemplazaba cuando volvía `fetch("%VITE_API_URL%/settings")` — con el nombre real ya configurado ("SuperMarket"), eso se veía como un parpadeo de un nombre incorrecto antes del correcto. Se reestructura para que NUNCA se pinte nada hasta que se sepan las 2 cosas a la vez: el nombre real (resuelto por el `fetch`, con "ZeuDin" como fallback recién ahí, solo si el fetch falla) Y si la fuente Fredoka ya cargó o no (`fontSettled`, marcado por el callback de `opentype.load` — o de una si `window.opentype` ni siquiera existe). `renderIfReady()` es el único punto que llama a `renderPreloaderName()`, y no hace nada hasta que las 2 condiciones estén resueltas — se pinta UNA sola vez, directo con el nombre correcto. **(2) Pedido explícito — "quiero que el logo se vea más responsivo y pequeño en dispositivos móviles, ya que ahora se ve muy grande":** el tamaño del `<logo-loader>` lo controla el atributo `size` (ver `logo-loader.js`), no una propiedad CSS — no hay forma de achicarlo con un media query puro. Se agrega `applyResponsiveLogoSize()` en el mismo `<script>` del splash: `size="92"` cuando `window.innerWidth <= 480`, `size="140"` (el de siempre) en el resto — reactivo a `resize` por si el visitante gira el teléfono o achica la ventana; funciona porque `logo-loader.js` ya tiene `size` en su `observedAttributes` y se re-renderiza solo con `attributeChangedCallback` en cada cambio, sin tocar el archivo del usuario para nada. Verificado en vivo con Playwright: en 375px de ancho, `logoEl.getAttribute("size")` da `"92"` y una captura de pantalla real confirma el logo visiblemente más chico y proporcionado; en 1280px, vuelve a `"140"`. Sobre el punto (1), no se pudo "atrapar" el fallback con un `MutationObserver` porque el fetch local resuelve más rápido que el round-trip del tool de Playwright — la garantía real es por construcción: `renderPreloaderName()` solo se invoca desde `renderIfReady()`, y esa función jamás corre con un valor hardcodeado, solo con lo que devolvió el `fetch` (o su fallback dentro del `.catch`) — inspeccionando el DOM ya cargado se confirmó el SVG del nombre real ("SuperMarket") sin rastro de "ZeuDin" en ningún momento capturable. `vite build` limpio, `dist/` con `opentype.min.js`/`fonts/Fredoka.ttf` copiados.
- **Bloque 127 (2 correcciones puntuales, sin relación con el preloader):** **(1) Pedido explícito — "en Explorá por categoría, las categorías se desplazan lentamente aunque caben perfectamente en el contenedor; solo deberían moverse en bucle infinito si NO caben":** `CategoryMarquee` (Home.jsx) siempre armaba el track duplicado (`[...categories, ...categories]`) y la animación `animate-marquee` sin importar cuántas categorías hubiera — con solo 1-2 (como el "modo de prueba" del dueño, confirmado en vivo: hoy hay una sola categoría de negocio real cargada), el contenido entero ya cabía en una sola línea y el movimiento constante no tenía sentido. Se agrega detección real de desborde: una copia invisible de la lista SIN duplicar (`measureRef`, `visibility:hidden` + `aria-hidden` — nunca `opacity-0`, que dejaría esos enlaces invisibles pero igual navegables por teclado/lector de pantalla, un bug de accesibilidad aparte) se mide con `ResizeObserver` (`measure.scrollWidth` vs `container.clientWidth`, con 1px de margen por redondeo) en un `useLayoutEffect` — corre antes de pintar, así que nunca hay un salto visible entre modos al cargar. Si desborda, se arma el track duplicado + `animate-marquee` de siempre (comportamiento sin cambios cuando de verdad hacen falta muchas categorías); si no, se muestra la lista tal cual, sin duplicar, en un `flex flex-wrap justify-center` estático (puede envolver en más de una línea si hace falta, nunca se corta). Se extrajo `CategoryPill` como componente aparte para no duplicar el markup entre la copia de medición y la visible. Verificado en vivo con Playwright: con la única categoría real del ambiente ("cafetería"), `hasMarqueeTrackClass` da `false` (nunca se arma el track/animación) y solo un `<a>` queda con `visibility:visible`/tabulable (el otro, de la copia de medición, da `visibility:hidden` + ancestro `aria-hidden="true"` — confirmado que no es alcanzable por teclado). **(2) Pedido explícito — "el ícono del chatbot en la pantalla principal muestra una S (inicial de SuperMarket), debería mostrar el ícono del sitio web; el círculo naranja de fondo se queda tal como está":** `PlatformAvatar` (`MarketplaceChatWidget.jsx`, usado en la burbuja proactiva y en el header del panel del bot general) solo tenía la letra inicial de `siteName` como contenido — nunca leía `logoUrl`. Se le suma exactamente el mismo patrón ya establecido en `Logo()` de `Header.jsx` (con quien el propio comentario del código ya decía que debía compartir "el mismo círculo"): si el admin subió un logo (panel Marca → `logoUrl`, vía `usePlatformSettings()`), se muestra esa imagen (`object-cover`, más `overflow-hidden` nuevo en el círculo para que la imagen respete el recorte redondo); si no hay logo configurado, sigue cayendo a la letra inicial de siempre — el círculo en sí (`bg-secondary-container`, que en `tailwind.config.js` resuelve al naranja `#fe9800` de la marca, el "naranja ZeuDin") no se tocó, tal cual pidió explícitamente. Verificado en vivo con Playwright: abriendo el panel del asistente, el círculo ahora muestra la imagen real subida en el admin (`http://192.168.1.79:4000/uploads/site/...png`) en vez de la "S", confirmado leyendo el DOM y con una captura de pantalla real. `vite build` limpio en ambos archivos.
- **Bloque 128 (pedido explícito, con captura — "el ícono del carrito con muchos productos se ve mal, debería mostrar hasta 99 como una notificación, y si son más, '99+', para que se vea más moderno y responsivo"):** la insignia numérica del carrito (`Header.jsx`) era un círculo de ancho FIJO (`w-[17px]`) mostrando `{count}` en crudo, sin ningún tope — con carritos de 4 dígitos (como el `1047` de la captura), el número se salía del círculo. Se adopta exactamente la idea que propuso el dueño (estándar ya usado en notificaciones de iOS/Android, y coincide con el patrón que YA existe en este mismo código para la campanita de notificaciones del vendedor — `VendorNotificationBell.jsx`, `min-w-[16px] px-1`, aunque esa sin el tope de 99 todavía): `{count > 99 ? "99+" : count}` para el tope, y `w-[17px]` fijo pasa a `min-w-[17px]` + `px-1` — con 1 dígito se ve un círculo perfecto de siempre (mismo tamaño, cero cambio visual), con "99+" el mismo `rounded-full` se estira solo a una píldora que abraza el texto en vez de recortarlo o deformar el círculo. Verificado en vivo con Playwright: se inyectó un carrito de prueba con 1047 unidades vía `localStorage` (mismo `STORAGE_KEY` real, `zeudin_cart`, mismo shape que usa `CartContext.jsx`), recargando la insignia mostró "99+" en una píldora prolija (antes, "1047" se desbordaba del círculo, igual a la captura del reporte) — confirmado con una captura de pantalla real; carrito de prueba borrado de `localStorage` al terminar. `vite build` limpio.
- **Bloque 129 (pedido explícito, con captura, follow-up del Bloque 126/127 — "el ícono del chatbot se ve muy pegado a los bordes del círculo, debería estar separado y un poco más chico"):** el logo agregado en el Bloque 126 (`PlatformAvatar`, `MarketplaceChatWidget.jsx`) usaba `h-full w-full` sin ningún margen, así que la imagen pintaba borde a borde contra el círculo naranja, sin aire. Se le suma `p-1` directo al `<img>` — con `object-cover` puesto, el padding en un elemento reemplazado como `<img>` sí reduce el área real donde se dibuja la imagen (dentro del padding queda el fondo naranja del círculo de siempre visible como margen), en vez de recortar la imagen o requerir un wrapper extra. La letra de fallback (cuando no hay logo subido) no se tocó — ya tenía aire propio por el tamaño de fuente relativo al círculo. Verificado en vivo con Playwright: se abrió el panel del asistente y se confirmó visualmente con una captura de pantalla real que el logo ahora queda separado del borde del círculo en las 2 instancias donde vive `PlatformAvatar` (burbuja proactiva y header del panel). `vite build` limpio.
- **Bloque 130 (pedido explícito, con captura del sitio + mockup de Photoshop — "la sección 'Para dueños de negocio' debería verse así en pantallas grandes"):** en el mockup la tarjeta es UNA sola fila compacta — texto a la izquierda, los 2 botones en una columna al lado, el teléfono a la derecha sobresaliendo por arriba del borde de la tarjeta (para eso existe el `lg:pt-36` de la `<section>`, Bloque 103). En el sitio los botones caían a una 2ª fila debajo del texto, la tarjeta se estiraba y el teléfono quedaba encerrado adentro. **Causa real, medida en vivo a 1280px**: el ancho útil de la tarjeta era 797px (1217 − `p-9` 36 − `lg:pr-96` 384) y la fila necesitaba 841px (texto `max-w-[560px]` + `gap-6` 24 + columna de botones 257) — con `flex-wrap`, no entraba, y envolvía. Además el ancho de los botones depende del nombre del sitio ("Ver tiendas en SuperMarket" es ~30px más ancho que con "ZeuDin"), así que el nombre configurado en el admin podía romper o no la fila — frágil. **Fix (`Home.jsx`)**: desde `xl` (≥1280px) `flex-nowrap`, el bloque de texto pasa a ser el que cede ancho (`xl:min-w-0 xl:flex-1`, con el mismo tope de 560) y la columna de botones nunca (`flex-shrink-0`); `lg:pr-96` (384) pasa a `lg:pr-[352px]`, que es EXACTAMENTE lo que ocupa la imagen (`lg:right-8` 32 + `lg:w-80` 320) — antes sobraban 32px de aire muerto. Valor arbitrario a propósito: la escala de Tailwind salta de `pr-80` a `pr-96`, no existe `pr-88` (se probó primero y no generó nada, dejando el `md:pr-72` y los botones 64px por debajo del teléfono — detectado midiendo, corregido antes de dar por hecho). **Por qué `xl` y no `lg`**: se midió con la fila única a 1024px y el texto quedaba en 292px (título en 2 líneas, párrafo en 6) — apretado; entre 1024 y 1279 se conserva el wrap de siempre, que a ese ancho se ve bien. Verificado en vivo con Playwright (`getBoundingClientRect` de tarjeta, texto, botones e imagen): a 1280px, fila única, tarjeta de 226px de alto (antes 361), texto encogido a 548px con el título todavía en 1 línea, botones pegados al borde de la imagen (gap 0, sin superposición) y el teléfono asomando 94px por encima de la tarjeta; a 1920px, idéntico con el texto a su tope de 560 — captura de pantalla real comparada con el mockup, misma composición; a 1024px, wrap como antes (tarjeta 361px, botones debajo). `vite build` limpio.
- **Bloque 131 (pedido explícito — "el ícono del loader se ve un poco muy grande en pantallas grandes, un poquito menos, no tanto"):** el `size="140"` del Bloque 126 (aplicado a pantallas por encima de 480px) baja a `118` — una reducción moderada (~16%) a propósito, el pedido fue explícito en que NO quería un cambio drástico. El tamaño de 92 en móvil no se tocó (no fue parte de este pedido). Verificado en vivo con Playwright (alargando `ANIM_MS` para observar con margen): a 1920px, `logoEl.getAttribute("size")` da `"118"`. Revertido `ANIM_MS` a 4250 al terminar. `vite build` limpio.
- **Bloque 132 (pedido explícito — "quiero que modifiques otras cosas en la base de datos, pero no que la pongas fija, sino como que el usuario o el vendedor las editó; a cada producto agrégale imagen, buen título y descripción; a cada tienda una imagen de portada; nada estático, todo editable/borrable; las imágenes con links externos, que está permitido en el código"):** se completaron los datos de prueba REALES en la base de datos — nunca hardcodeado en el frontend — usando el MISMO mecanismo real que ya usa el sitio: el array `images` de `Product` (poblado normalmente vía `addProductImageLink`, `products.controller.js`, que acepta un link externo desde que el vendedor lo pega en su panel — confirmado que `SiteSettings.allowProductImageLinks` está en `true`) y el campo `Vendor.coverUrl` (mismo campo que ya edita el vendedor desde "Marca" en su panel, `vendors.controller.js`, un `String` simple sin restricción de que sea solo upload). Se armó un script de un solo uso (corrido a mano con Prisma, borrado después — no quedó en el repo) que replica EXACTAMENTE la regla de negocio real: si el producto no tenía ninguna foto, al agregarle la primera se marca `activatedAt` (si todavía no la tenía) — el mismo ancla que usa la etiqueta "Nuevo" automática (Bloque anterior, `addProductImageLink`) — para que el dato quede indistinguible de si lo hubiera cargado un vendedor real hoy. Los 2 productos que YA tenían una foto subida a mano en pruebas anteriores (Lámpara de mesa decorativa, Champú para el pelo) no se tocaron en `images` — se les completó solo la descripción, que les faltaba. **34 productos** recibieron 1 imagen cada uno (`https://picsum.photos/seed/<id-del-producto>/800/600` — semilla fija por id, siempre la MISMA foto para ese producto, nunca random en cada visita; se confirmó el servicio alcanzable con `curl` antes de usarlo), **35 productos** recibieron una descripción nueva escrita a mano en español, específica de cada producto/rubro (ropa, comida criolla, tecnología, belleza, agro, hogar — nunca un texto genérico repetido), y las **6 tiendas** recibieron su portada (`https://picsum.photos/seed/<slug>-cover/1200/400`). Los títulos de los productos ya eran buenos (específicos, en español, sin placeholders tipo "Producto 1") — no se tocaron. Verificado en vivo con Playwright: el listado de "Destacados en toda Cuba" y la página de una tienda (`/tienda/moda-caribe`) muestran fotos reales y distintas por producto (antes "Sin foto" en gris) con su descripción truncada + "...leer más", y el banner de la tienda ahora pinta la imagen de portada detrás del nombre — todo sigue siendo filas normales de `Product`/`Vendor`, editable y borrable desde el panel de vendedor/admin de siempre (agregar/quitar imágenes, cambiar portada, editar descripción), nada vive en código.
- **Bloque 133 (pedido explícito, con 2 capturas del store real — "aquí debe mostrarse el ícono que el cliente sube para su tienda, no la portada; agregas ícono con enlaces externos también, ya que lo podrían subir desde su dispositivo o pegar un link"):** hasta este bloque, `Vendor.logoUrl`/`coverUrl` existían en el schema y `updateMyVendor` ya los aceptaba como link plano, pero NINGUNA pantalla de vendedor los exponía, y `Store.jsx` (banner público) tampoco leía `logoUrl` — el círculo de avatar mostraba SIEMPRE la inicial, sin importar si había logo cargado. Se resuelve en 3 partes: **(1)** `Store.jsx` ahora muestra `v.logoUrl` (nunca `coverUrl`, a propósito, tal como se pidió) dentro del círculo si existe — el recorte circular se movió a un `<span>` interno con `overflow-hidden` en vez de ponerlo en el contenedor de afuera, porque si no el punto de estado (abierto/cerrado, pegado al borde inferior-derecho) quedaba parcialmente cortado por el propio recorte redondo. **(2)** Nueva tarjeta "Logo y portada de tu tienda" en `VendorProfile.jsx` (`StoreBrandingCard`, junto a "Datos de la tienda" — mismo lugar donde ya vive el resto de la marca de la tienda), con el MISMO patrón exacto que ya usa `AdminBranding.jsx` para el logo de la plataforma entera: subir archivo O pegar un link externo, para el logo Y para la portada por separado, cada uno con su propia vista previa y botón "Quitar". **(3)** Backend nuevo para la mitad "subir archivo" (la mitad "pegar link" ya funcionaba, `PATCH /vendors/me`): `POST /vendors/me/logo` y `POST /vendors/me/cover` (`uploadVendorLogo`/`uploadVendorCover`, `vendors.controller.js`), con su propio middleware `vendorBrandingUpload.js` — copia exacta de `siteUpload.js` (mismas extensiones permitidas, mismo límite de 8MB, mismo nombre de archivo aleatorio) — guardando en `uploads/vendor-branding/`, servido públicamente vía `express.static` en `app.js` (mismo criterio que `/uploads/products`). De paso, se sacó el corte a 3 métodos de pago en la franja inferior del banner (`v.acceptedPaymentMethods.slice(0, 3)` + "+N", con captura del reporte: "Zelle Visa Mastercard +3") — ahora se muestran TODOS, `flex-wrap` los pasa a una 2ª línea si no entran en una, nunca se cortan (la sección completa más abajo, "Formas de pago que acepta esta tienda", ya los mostraba todos desde antes — esta era la única vista truncada). Verificado en vivo con Playwright: se seteó un `logoUrl` de prueba por Prisma directo (sin credenciales de vendedor a mano en este momento) y se confirmó visualmente que el círculo de `/tienda/moda-caribe` pinta la imagen real, recortada en círculo, con el punto de estado intacto encima (revertido después); en `/tienda/casa-verde` (6 métodos de pago cargados) se confirmaron los 6 visibles, sin "+3". `vite build` limpio, `node --check` limpio en los 5 archivos backend nuevos/tocados.
- **Bloque 134 (pedido explícito — "quiero que bloquees el zoom o zoom por completo de la página, así el cliente por error no perderá el contexto"):** el `<meta name="viewport">` de `index.html` ya traía `maximum-scale=1.0`/`user-scalable=no` desde antes, pero Safari de iOS los IGNORA a propósito desde iOS 10 (decisión de accesibilidad de Apple, ningún meta tag lo puede evitar) — dado que las 2 capturas de este mismo pedido muestran justo un navegador con look de Safari/iOS, ese meta tag solo nunca iba a alcanzar. Se agregan 2 capas reales: **(1)** `touch-action: manipulation` en `html, body` (`<style>` de `index.html`) — la vía estándar que si funciona en iOS/Android modernos contra el doble-tap-para-zoom. **(2)** Un `<script>` plano, apenas arranca el `<body>` (antes de React, mismo criterio que el resto de este `index.html`), que bloquea las otras 2 formas reales de hacer zoom con `preventDefault()`: pellizco de 2 dedos (`touchmove` con más de 1 dedo — el equivalente universal a los eventos `gesturestart`/`gesturechange`, que son propietarios de WebKit/Safari) y Ctrl+rueda del mouse en desktop (el atajo de zoom del navegador). Nunca toca scroll/pan normal (1 dedo sin Ctrl). `vite build` limpio.
- **Bloque 135 (pedido explícito — "quiero un loader normal para cuando se cambia entre páginas, en caso de que alguna se quede atascada"):** la barra superior de progreso (`RouteLoader.jsx`, Bloque 17) es puramente cosmética — un timer fijo de ~710ms que SIEMPRE termina y desaparece, sin ninguna relación real con si los datos de la página de destino ya cargaron; si una página se queda de verdad atascada (backend lento/caído, red mala), la barra igual desaparece y el visitante se queda sin ningún indicio de que algo sigue pasando. Se suma `StuckPageHelper`, un componente chico nuevo en el mismo archivo que usa `useIsFetching()` de React Query (cuenta las queries en vuelo en TODA la app en cualquier momento — señal real, a diferencia del timer de la barra) — si se mantiene activa 4 segundos SEGUIDOS, aparece un spinner no bloqueante, abajo al centro, con el mensaje "Esto está tardando más de lo normal, seguimos intentando..."; desaparece solo apenas la query en cuestión termina. Verificado en vivo con Playwright usando `page.route()` para demorar a mano la respuesta de `/vendors` 3 segundos (con el umbral bajado a 50ms para la prueba): el spinner aparece mientras la petición está demorada y desaparece apenas se resuelve — confirmado con ambos estados leídos del DOM. Revertido el umbral a 4000ms al terminar. `vite build` limpio.
- **Bloque 136 (pedido explícito — "quiero que agregues un aviso de que el sitio va a alojar caché, y quiero que el sitio empiece a alojar el caché, así cuando se sale de la pantalla y se vuelve a entrar, no se estaría recargando la página nuevamente"):** React Query ya cacheaba en MEMORIA (`staleTime: 30s` de antes), pero eso se perdía entero en cualquier recarga completa (F5, cerrar/reabrir la pestaña) — nunca quedaba "alojado" de verdad, tal como se pidió. Se instalan `@tanstack/query-sync-storage-persister` + `@tanstack/react-query-persist-client` (oficiales de TanStack, mismo mayor que `@tanstack/react-query` ya instalado) y `main.jsx` pasa de `QueryClientProvider` a `PersistQueryClientProvider`, con un persister que guarda el caché entero en `localStorage` (`queryClient.js`, clave `zeudin_query_cache`, `maxAge` 24h) — una pantalla ya visitada, incluso después de una recarga completa, pinta de una con los datos guardados, sin esperar al servidor. `staleTime` general sube de 30s a 5 minutos (mismo valor puntual que ya usaban `usePlatformSettings.js`/`MarketplaceChatWidget.jsx`/`AdminIntegrations.jsx`, ahora es el default general) para que volver dentro de esa ventana ni siquiera dispare un refetch de fondo silencioso. **Aviso**: `CacheNotice.jsx` nuevo, aparece UNA sola vez por navegador (recordado en `localStorage`, nunca por sesión), abajo a la izquierda (no choca con el chat, siempre abajo a la derecha). **Seguridad**: como el caché ahora sobrevive a una recarga Y queda en `localStorage` (mismo lugar donde ya viven los tokens de sesión, mismo criterio de exposición que ya tenía este código), se limpia ENTERO — en memoria (`queryClient.clear()`) y en `localStorage` (`queryPersister.removeClient()`) — en el único punto real de cierre de sesión (`clearLocalSession`, `AuthContext.jsx`, cubre logout explícito, inactividad, refresh fallido y sync entre pestañas), para que el caché de una cuenta nunca sobreviva a su logout ni se filtre a la siguiente cuenta que entre en el mismo navegador. Verificado en vivo con Playwright: tras cargar el Home, `localStorage.getItem("zeudin_query_cache")` da un JSON real de ~63KB; el aviso aparece la primera vez y, tras cerrarlo, `zeudin_cache_notice_seen` queda en `"1"` y ya no vuelve a aparecer en una recarga siguiente. `vite build` limpio.
- **Bloque 137 (pedido explícito — "recuerda siempre que cuando se comparte el enlace de una tienda, debe previsualizarse en ese link los datos e imágenes de ESA tienda, y no lo del sitio web en principal"):** el frontend es una SPA pura (Vite+React, sin SSR) servida como archivos estáticos — un bot de previsualización (WhatsApp, Facebook, Twitter/X, Telegram, Discord...) NUNCA ejecuta el JS de React, así que sin importar qué mostrara `Store.jsx` ya montado, esos bots solo leían el `<head>` ESTÁTICO de `index.html`, con los meta tags fijos del sitio entero — nunca los datos reales de la tienda compartida (bug real, no cosmético: esto es lo que WhatsApp/Facebook muestran al pegar el link). Se agrega `GET /og/tienda/:slug` (`og.controller.js`, nuevo) — arma un HTML aparte con los meta tags REALES de esa tienda (`og:title`/`og:description`/`og:image` con `companyName`/`description`/`coverUrl` o `logoUrl` de respaldo, resueltos a absolutos con `env.backendUrl` igual que ya hace `getBrandSettings()`) y redirige de una (meta refresh + script) a cualquier humano que lo abra al link real de la SPA de siempre — un bot nunca sigue esa redirección, solo lee el `<head>` una vez. El botón "Compartir tienda" (`handleShare`, `Store.jsx`) ahora copia ESTE link (`${api.defaults.baseURL}/og/tienda/:slug`) en vez del `window.location.href` de la SPA — funciona YA, sin depender de ninguna configuración de hosting nueva, porque es el propio botón del sitio el que decide qué URL se comparte. **Pieza pendiente documentada para cuando se decida el hosting de producción** (hoy no hay `vercel.json`/`netlify.toml`/nginx en el repo — el proyecto todavía no está desplegado en ningún lado): si además se quiere que un link copiado a mano DESDE LA BARRA DE DIRECCIONES (no desde el botón "Compartir") también se previsualice bien, hace falta una regla de rewrite en la capa de hosting que reescriba `/tienda/:slug` hacia `${BACKEND_URL}/og/tienda/:slug` — un párrafo completo con el ejemplo queda en el comentario de cabecera de `og.controller.js`. Manejo de tienda no encontrada/bloqueada/privada: sin datos reales que mostrar, la ruta redirige 302 directo al link real (nunca un preview con datos inventados) — verificado en vivo con `curl` contra un slug inexistente. Verificado en vivo, extremo a extremo: `curl` contra `/og/tienda/moda-caribe` devuelve el HTML con título/descripción/imagen reales de esa tienda puntual y la URL de redirect correcta (se detectó y corrigió en el camino un bug real: `env.frontendUrl` es una lista separada por comas en este ambiente — igual que ya usa `app.js` para CORS — y se estaba usando tal cual sin tomar el primer origen, armando una URL rota); con Playwright, interceptando `navigator.clipboard.writeText`, se confirmó que tocar "Compartir tienda" copia exactamente `http://192.168.1.79:4000/og/tienda/moda-caribe`. `vite build` limpio, `node --check` limpio.
- **Bloque 138 (pedido explícito, con captura de referencia — el aviso de caché debe verse como un aviso de cookies típico: ícono + texto + "Learn more" + 2 botones lado a lado, "Allow"/"Decline"):** se rehace `CacheNotice.jsx` entero para calzar con la referencia — ícono `Cookie` de lucide-react (antes un ícono de base de datos genérico), texto + link "Más información" (a `/privacidad`, la página real que ya existe) arriba, 2 botones de igual ancho ("Permitir"/"Rechazar") abajo, mismo estilo de tarjeta blanca redondeada que ya usa el resto del sitio. El texto en sí NO es una copia literal de la referencia — esa habla de cookies para "redes sociales" y "publicidad", que este sitio no hace (no hay tracking de terceros ni anuncios); dice literalmente lo que el sitio hace de verdad: guardar respuestas en el dispositivo para cargar más rápido. **"Rechazar" con efecto real, no decorativo**: `queryPersister` (`queryClient.js`) pasa a ser un wrapper alrededor del persister real de `createSyncStoragePersister` — `persistClient()` revisa `CACHE_CONSENT_KEY` en cada llamada y se vuelve un no-op si el visitante rechazó, así que ninguna escritura nueva llega a `localStorage` de ahí en más (el caché en MEMORIA de React Query sigue funcionando normal durante esa sesión — eso es SPA común, sin ninguna implicación de privacidad, "rechazar" solo corta lo que se ESCRIBE EN DISCO); al tocar "Rechazar" también se borra de una lo que ya hubiera quedado guardado (`queryPersister.removeClient()`). Verificado en vivo con Playwright: con ~53KB ya guardados en `localStorage`, tocar "Rechazar" los borra al instante (`zeudin_query_cache` pasa a `null`); navegando a otra página después (disparando queries nuevas) se confirmó que sigue en `null` — no es un borrado de una sola vez, la escritura queda apagada de verdad mientras dure el rechazo. `vite build` limpio.
- **Bloque 139 (2 correcciones puntuales al Bloque 138, mismo turno):** **(1) Pedido explícito — "el mensaje debe ser algo así o parecido, más profesional: 'Almacenamos datos temporalmente para mejorar su experiencia de navegación y recomendarle contenido de interés', y arriba un título que diga 'Este sitio web usa cookies'":** se suma el título en negrita arriba del texto y se reemplaza la copia por la dictada (registro formal "usted", a propósito distinto del resto del sitio que usa "tú" — es texto de aviso/cumplimiento, no copy de producto). **(2) Pedido explícito, inmediato follow-up — "no habrá botón de permitir y rechazar, solo uno de 'Estoy de acuerdo'":** se saca el botón "Rechazar" y su lógica real del Bloque 138 (el wrapper de `queryPersister` que revisaba un flag de consentimiento antes de escribir en `localStorage`) — con un solo botón de confirmación, ese camino queda inalcanzable desde la UI, así que `queryClient.js` vuelve a la versión simple (persiste siempre, sin gate) para no dejar código muerto. Queda un solo botón, "Estoy de acuerdo" con el mismo naranja de marca (`bg-secondary-container`, antes 2 botones grises parejos) — cerrar el aviso solo marca que ya se vio (`localStorage`, una vez por navegador), igual que antes. Verificado en vivo con Playwright: el aviso muestra el título + el texto nuevo tal cual se pidió, un solo botón "Estoy de acuerdo" full-width, y tocarlo lo cierra y marca `zeudin_cache_notice_seen` en `"1"`. `vite build` limpio.
- **Bloque 140 (bug real reportado en vivo, con captura — una línea del carrito con cantidad ~2147483671 y total en cientos de miles de millones de CUP, todo superpuesto/roto; + "el nombre del producto debe mostrarse más completo llenando el espacio de la línea, que quede moderno"):** **Causa real**: `addItem`/`updateQuantity` (`CartContext.jsx`) y `atStockLimit` (`AddToCartControl.jsx`) usaban `stock ?? Infinity` como tope — cualquier producto SIN `stock` cargado en el objeto que le llega al carrito (`unlimitedStock:true`, o simplemente una pantalla que no trae ese campo en su fetch) quedaba SIN NINGÚN techo real; tocar "+" muchas veces terminaba en una cantidad absurda, y con eso el total (`unitPrice * quantity`) también. Se agrega `MAX_CART_QUANTITY = 99` (`CartContext.jsx`, exportado), un techo DURO que se aplica siempre — tenga o no stock cargado el producto — en los 3 puntos donde antes existía el `?? Infinity` (`addItem`, `updateQuantity`, `atStockLimit`). **Auto-cura carritos ya rotos**: `readStored()` ahora sanea cada `quantity` guardado en `localStorage` al leerlo (mismo criterio de tope), así que un carrito que YA tenía un número absurdo de antes de este fix se corrige solo en la próxima carga — no hace falta que el cliente lo vacíe a mano ni perder lo demás que tenía cargado. **Layout del carrito (`CartDrawer.jsx`), pedido explícito de "nombre más completo"**: el nombre pasa de `truncate` (1 línea, corta rápido con "…") a `line-clamp-2` (usa 2 líneas completas antes de cortar) — y el precio total de la línea, que antes vivía en una columna aparte comprimiendo el ancho disponible para el nombre, se movió a la fila de abajo, al lado del selector de cantidad (mismo patrón que ya usa la página completa `/carrito`) — el nombre ahora tiene el ancho ENTERO de la tarjeta para sí. El número de cantidad pasa de `w-6` fijo a `min-w-[18px]` (con el tope de 99, nunca pasa de 2 dígitos, pero así tampoco se rompe si ese tope cambia a futuro). Verificado en vivo con Playwright: se inyectó un carrito de prueba con una cantidad de `2147483671` en un producto de nombre largo (sin `stock` cargado, mismo escenario real) — al cargar la página, `localStorage` ya mostraba la cantidad corregida a `99` (el `readStored()` saneado se reescribe solo al persistir); el drawer mostró el nombre completo en 2 líneas prolijas, cantidad `99` legible junto al ícono de borrar sin superponerse, y un total real (`188,100 CUP`) en vez del número astronómico de la captura — confirmado con una captura de pantalla real. `vite build` limpio.
- **Bloque 141 (pedido explícito, muy extenso — "revisar toda la parte de mesas/QR de restaurantes, uno de los fuertes del proyecto"):** revisión de punta a punta del sistema de mesas ya existente (`Table`/`TableOrder` en el schema, `tables.controller.js`, `VendorTables.jsx`, `TableOrder.jsx`) — el backend ya era sólido (token único no adivinable por mesa, chequeo de tienda bloqueada/suspendida ya arreglado en el Bloque 62, `availableForTableMenu` ya separaba qué productos entran al menú QR), pero **el QR del panel de vendedor era decorativo, no uno real** — el propio código ya lo documentaba como deuda ("`DecorativeQr`... un patrón decorativo, no un QR real escaneable", comentario que además señalaba lo mismo desde `VendorSettings.jsx`). Se implementa todo lo pedido: **(1) QR real y descargable**: `qrcode.react` (ya usado en otro lado del código) reemplaza el patrón falso — `fgColor` negro, `imageSettings` con el logo de la tienda compuesto en un círculo con borde negro (armado en un canvas aparte, `buildCircularLogoDataUrl`, recorte circular + anillo — nunca rompe si el logo falla, cae a QR sin logo), tamaño proporcional al QR (~22%, "responsivo" como se pidió). "Descargar" (`downloadTableQr`) compone en un canvas nuevo el QR + una línea vertical + una etiqueta redondeada con "Mesa N" (o el nombre propio si el vendedor le puso uno) y descarga un PNG listo para imprimir — exactamente el layout pedido. **(2) Colores distintos por mesa**: paleta fija de 8 colores, determinística por `tableNumber` (mismo color siempre para la misma mesa, sin campo nuevo en la base) — se ve como borde superior de la tarjeta en `VendorTables.jsx`. **(3) Gestión completa de mesas** (antes solo "agregar" existía): 3 endpoints nuevos — `PATCH /tables/me/:id` (renombrar, campo `label` nuevo en `Table`, opcional — si no se carga, sigue siendo "Mesa N"), `POST /tables/me/:id/regenerate-qr`, `DELETE /tables/me/:id` (bloqueado si la mesa tiene un pedido sin liberar, para no perder un pedido en curso). **(4) Alternativa real contra "le sacan una foto al QR y piden desde afuera"** (pedido explícito, sin alternativa fija clara): la defensa accionable es la regeneración — el token es un valor random no adivinable (ya lo era); "Nuevo QR" lo cambia al instante, cualquier copia/foto del QR anterior pasa a dar 404 en el momento mismo en que se regenera, sin tocar `tableId` ni pedidos ya existentes — explicado con un aviso fijo arriba de la grilla de mesas en vez de repetirlo por tarjeta. Además, el propio flujo de cocina YA daba la segunda mitad del pedido explícito ("mientras no haya alternativa, que el vendedor vaya y verifique con el cliente antes de preparar") — RECEIVED->PREPARING siempre fue ese gate manual (recién ahí se descuenta stock, Bloque 29); se lo hace explícito con un botón renombrado "Verificar y aceptar" + tooltip en `VendorOrders.jsx`. **(5) El cliente solo pone su nombre** (pedido explícito — "nada más es necesario, es un pedido a la mesa"): `TableOrder.customerName` nuevo (migración `20260910150000_table_customer_name_label`, columna nullable por compatibilidad con pedidos viejos, obligatorio en el zod de `createTableOrderSchema` para cualquiera nuevo) reemplaza el correo obligatorio de antes en `TableOrder.jsx` (`customerEmail` pasa a 100% opcional — sigue aceptándose si en algún momento se vuelve a pedir, pero ya no se muestra en el formulario público). El nombre real ahora se ve en `VendorOrders.jsx` (antes decía literal "Mesa N", ahora "Mesa N · María") y en el panel admin (`AdminVendors.jsx`, sección "Pedidos de mesa recientes"). **(6) Secciones separadas para tiendas mixtas** (pedido explícito — "productos externos fijos" vs. "lo que tienen en la cafetería/restaurante", "todo en secciones diferentes"): `Store.jsx` (la tienda pública normal, DISTINTA del menú QR) splitea la grilla de productos en "Menú de {tienda}" / "Otros productos de {tienda}" usando el mismo flag `availableForTableMenu` que ya filtra el menú QR — pero SOLO si la tienda es restaurante Y tiene de verdad una mezcla real de las 2 cosas (si solo tiene una, sigue con la grilla única de siempre, sin secciones vacías). **(7) Visibilidad del admin** (pedido explícito — "todo lo que hacen los vendedores, todas las actividades relevantes"): antes NINGÚN cambio de estado de un pedido de mesa quedaba en `ActivityLog` (a diferencia de un pedido normal) — se agrega `logActivity` en `updateKitchenStatus` (cubre tanto la confirmación RECEIVED->PREPARING como PREPARING->READY) y en las 3 acciones nuevas de mesa (creada ya lo tenía, se suma renombrada/regenerada/eliminada) — Admin > Actividad ahora refleja todo esto; la sección "Pedidos de mesa recientes" por tienda (`AdminVendors.jsx`) YA existía de antes (no hacía falta construirla) y ahora suma el nombre del cliente. Verificado en vivo de punta a punta: se habilitaron 2 productos reales de "Sabor Criollo" para el menú QR (`availableForTableMenu`+stock, revertido después), se escaneó un QR real (`/mesa/:qrToken` con un token real de la base), se completó un pedido con SOLO el nombre ("María Test") — confirmado en la base que se creó con `customerName:"María Test"`, `customerEmail:null` (pedido de prueba borrado después); se confirmaron las 3 rutas nuevas registradas y protegidas (401 sin sesión); se armó un catálogo mixto temporal (2 productos de menú + 2 de catálogo normal) y se confirmó con una captura real que `Store.jsx` arma las 2 secciones separadas correctamente (revertido después). **Lo único que no se pudo probar en vivo con clics reales**: la interacción del panel de vendedor (`VendorTables.jsx` — renombrar/regenerar/eliminar/descargar QR) y `VendorOrders.jsx`, porque no había credenciales de vendedor a mano en este momento y no se intentó saltear el login (mismo criterio que en un bloque anterior de esta sesión) — la implementación se apoya en un patrón ya probado en producción (`QRCodeSVG`/`QRCodeCanvas` de `qrcode.react` ya usado en `VendorSettings.jsx`, `ConfirmModal` ya usado en todo el resto del panel) y en revisión de código cuidadosa; falta la verificación visual de esa pantalla puntual. `vite build` limpio, `node --check` limpio en los archivos backend tocados, migración aplicada sin errores.
- **Bloque 142 (bug real reportado en vivo, con captura — filtrar "Tiendas" por tipo de negocio, ej. "Tecnología", daba "0 tiendas en Cuba" con "Todas las provincias" seleccionado):** no era un bug de código — `listVendors` (`vendors.controller.js`) filtra por `businessCategoryId` exacto, lógica correcta y ya probada; el problema era de DATOS: los 6 vendedores de prueba nunca tuvieron `businessCategoryId` asignado (`null` en los 6, confirmado consultando la base directo) — las 10 categorías reales creadas en el Bloque 132 (Restaurantes, Moda y ropa, Tecnología, etc.) existían, pero NINGUNA tienda apuntaba a ninguna, así que cualquier filtro por categoría daba 0 resultados siempre, sin importar la provincia. Se asignó a cada tienda la categoría real que le corresponde por lo que vende — TecnoHabana → Tecnología, Moda Caribe → Moda y ropa, Bella Piel → Belleza y cuidado personal, Sabor Criollo → Restaurantes (ya tenía `isRestaurant:true`, coincide), Casa Verde → Hogar y decoración, AgroHolguín → Supermercado y abarrotes — mismo mecanismo real que ya usa el registro/edición de tienda (`Vendor.businessCategoryId`, sin tocar código). Verificado en vivo: `/tiendas?businessCategoryId=<Tecnología>` pasó de "0 tiendas en Cuba" a "1 tiendas en Cuba" (TecnoHabana, con su categoría real mostrada en la tarjeta) — confirmado con una captura de pantalla real.
- **Bloque 143 (pedido explícito, corrige el criterio del Bloque 140 — "a los productos se le pueda agregar más de 99 unidades, el monto a agregar es ilimitado dependiendo del stock del producto; el stock siempre debe ponerse al crear un producto, y si es ilimitado debe ponerse como ilimitado al crear un producto"):** el Bloque 140 diagnosticó mal el bug real de esa captura — asumió que un `stock` faltante en el objeto del carrito era un descuido y agregó un techo DURO de 99 para TODO producto. En los hechos, el producto de esa captura (Champú, `unlimitedStock:true`) SIEMPRE estuvo pensado para no tener techo: `AddToCartControl.jsx`/`Product.jsx`/`StoreChatWidget.jsx` ya mandaban `stock:null` a propósito como sentinel ("`null` le dice a CartContext que no hay techo real que respetar", comentario que ya estaba ahí desde antes de este bloque) — el Bloque 140 reemplazó el `?? Infinity` original (que SÍ respetaba ese sentinel, porque `??` también dispara con `null`) por `?? MAX_CART_QUANTITY`, rompiendo "disponible siempre" para cualquier producto ilimitado, no solo para el caso roto. **Se revierte ese criterio y se corrige de verdad**: nuevo `resolveStockCap(stock)` en `CartContext.jsx` — `null` (el sentinel real, nunca un `undefined` accidental) es sin techo; cualquier otro valor se interpreta como el stock real del producto (puede ser mayor a 99 sin problema — una tienda que vende 500 unidades de algo no tiene por qué toparse en 99). Mismo criterio restaurado en `AddToCartControl.jsx` (`atStockLimit`, vuelve a `existing.stock ?? Infinity`) y en el saneo de `readStored()` (sigue auto-curando una cantidad guardada que no sea un número válido — NaN, negativa — pero ya no contra un techo arbitrario). `MAX_CART_QUANTITY` se elimina del todo (ya no se exporta ni se usa en ningún lado). El layout del Bloque 140 (nombre en 2 líneas, cantidad con `min-w` en vez de ancho fijo, total en su propia fila) sigue siendo necesario y se queda igual — ahora sí puede volver a aparecer una cantidad de varios dígitos (de verdad, no por un bug) y el layout ya no se rompe; se aplicó el mismo ajuste (`min-w` en vez de `w` fijo) a los 2 lugares de `/carrito` (Cart.jsx) que todavía tenían ancho fijo. **Segunda parte del pedido — el stock deja de poder quedar indefinido al crear un producto**: `VendorProducts.jsx` YA forzaba esta elección en la UI (input de stock `required`, o el checkbox "Disponible siempre" que lo reemplaza) — pero el backend (`productSchema`, con `stock`/`unlimitedStock` con `.default(...)`) todavía aceptaría en silencio un request que no mandara ninguno de los 2, creando el producto con "stock 0, no ilimitado" sin que nadie lo haya decidido de verdad. `createProductSchema` nuevo (solo para CREAR, `productSchema.partial()` de `updateProduct` sigue igual — una edición parcial sigue sin tener que reenviar el stock entero) saca esos defaults y agrega un `superRefine`: exige stock explícito, `unlimitedStock:true`, o tallas con su propio `sizeStock` (que también define un stock real, vía `reconcileStock`) — cualquier otra combinación tira 400 con un mensaje claro. Verificado en vivo con Playwright: se guardó un carrito de prueba con un producto `stock:null` (ilimitado) en cantidad 250 — sobrevivió intacto al pase de saneo al cargar la página (antes se hubiera recortado a 99), y el drawer lo mostró prolijo (`(250)` en el badge, `250` en el selector, `25,000 CUP` de total, sin overlap). Se probó `createProductSchema` directo con 4 casos reales (exportándola temporalmente, revertido después): sin stock ni ilimitado → rechaza con el mensaje claro; con stock número → pasa; `unlimitedStock:true` sin stock → pasa; con tallas y `sizeStock`, sin stock explícito → pasa. `vite build` limpio, `node --check` limpio.
- **Bloque 144 (bug real reportado en vivo — "envié un registro, el administrador lo recibió, recibió el correo y todo funcionó perfecto, pero al aprobar la cuenta, cuando fui al panel del vendedor y actualicé esa cuenta, aún mostraba en revisión, cuando ya debería mostrar como que fue aprobada"):** no era un bug de la máquina de estados de verificación — `transitionVendorVerification()`, `updateVerification` (admin) y toda la lógica de `VendorVerification.jsx` (las secciones de elegir método de pago, `showChooseMethod`/`showCardCheckout`/`inCupCycle`) ya estaban completas y correctas, confirmado leyendo el flujo entero de punta a punta y comparándolo contra la descripción exacta del reporte (el texto del correo que el cliente dijo haber recibido coincide literal con el body real de la notificación `VERIFICATION_DOCS_APPROVED`). **Causa real: una regresión propia, del Bloque 136 de esta misma sesión** — el caché persistido en `localStorage` con `staleTime` general de 5 minutos hace que `refetchOnMount` (que por default de React Query solo refetchea si la query está "stale") NO dispare un fetch nuevo si la pantalla se había visitado hacía menos de 5 minutos, incluso después de una recarga completa de página — porque `PersistQueryClientProvider` rehidrata desde `localStorage` ANTES que cualquier fetch corra. Resultado real: un vendedor que ya había abierto su panel de verificación, y volvía a los pocos minutos (justo el caso típico de "mandé la solicitud, actualicé para ver si ya la aprobaron"), seguía viendo el estado viejo cacheado aunque el admin ya hubiera aprobado del otro lado. **Fix**: se agrega `refetchOnMount: "always"` (opción válida y tipada de TanStack Query v5, confirmada en los `.d.ts` de `node_modules`) a las 3 queries donde un dato desactualizado importa de verdad — `["my-vendor"]` (`VendorLayout.jsx`), `["my-verification"]` (`VendorVerification.jsx`) y `["admin-verifications", tab]` (`AdminVerifications.jsx`) — fuerza un refetch real cada vez que el componente se monta, sin importar qué tan "fresco" sea el caché; el resto del sitio (donde SÍ tiene sentido seguir sirviendo de caché para que se sienta instantáneo, tal como se pidió en el Bloque 136) no se tocó. `node --check` limpio, `vite build` limpio.
- **Bloque 145 (pedido explícito, misma solicitud extensa del Bloque 144 — vencimiento de 24 horas tras la aprobación + más información visible para el administrador):** **(1) Vencimiento de 24h, pedido explícito — "las solicitudes demorarán veinticuatro horas luego de ser aprobada... si no se selecciona la forma de pago adecuada y no se realiza el pago y se confirma, automáticamente se deberán enviar nuevos datos al sistema":** en vez de sumar una columna nueva mutable tipo `approvedAt` (riesgo real de desincronizarse del estado real con el tiempo), el "cuándo se aprobó" se deriva siempre del propio historial inmutable que ya existía (`VerificationStatusLog.findFirst({ toStatus: "PENDING_PAYMENT" }, orderBy: "desc" )`) — única fuente de verdad, nunca 2 relojes que puedan discrepar. Se agrega `expireStalePendingPaymentApprovals()` (`verificationPayment.job.js`) — recorre vendedores en `PENDING_PAYMENT`, calcula ese mismo log y, pasadas 24h reales sin pago confirmado, transiciona a `REJECTED` vía `transitionVendorVerification()` (la única vía sancionada para tocar `Vendor.verificationStatus`, nunca un `update` directo) con un motivo claro — el propio flujo de reenviar documentos para una nueva solicitud ya existía de antes para cualquier `REJECTED`, así que "se deberán enviar nuevos datos" queda cubierto sin construir nada aparte. Nuevo tipo de notificación `VERIFICATION_APPROVAL_EXPIRED` (enum de Prisma + mapa de eventos en `verificationNotify.service.js`, título "Venció el plazo para completar el pago"); el body de `VERIFICATION_DOCS_APPROVED` (la notificación de aprobación en sí) se actualiza para mencionar la ventana de 24h desde ese mismo momento, tal como se pidió. **Cron aparte, no mezclado con el existente**: ya había un cron diario (8:30am hora Cuba, `startVerificationPaymentJob`) para vencimientos de renovación mensual — meter ahí también el chequeo de 24h hubiera significado que, en el peor caso, una aprobación pudiera tardar hasta ~48h en vencerse de verdad en vez de 24h como se prometió al cliente; se agrega `startVerificationApprovalExpiryJob()` como cron HORARIO independiente (`"0 * * * *"`), registrado en `server.js` junto al existente, así el plazo real nunca se aleja más de ~1h de lo prometido. `paymentDeadlineAt` (mismo cálculo de log, nunca un campo aparte) se suma a `getMyVerification` y a `listVerifications` (admin) y se muestra en pantalla — banner con la fecha/hora límite en `VendorVerification.jsx` antes de las secciones de pago, y en la tarjeta de cada solicitud en `AdminVerifications.jsx`. **(2) Más información para el admin, pedido explícito — "necesito que se recopilen más información para que los administradores lo reciban y que se vea todo más responsivo":** se agrega el email y WhatsApp del vendedor al encabezado de cada tarjeta en `AdminVerifications.jsx` (antes solo visible yendo a otra sección del panel admin) y el timestamp real de `paymentClaimedAt` (hora y fecha de cuándo el vendedor marcó que ya pagó) antes del comprobante — pedido explícito del punto "con registro de hora y fecha de cuándo se hizo todo este movimiento de dinero", que ya se guardaba en la base (`paymentClaimedAt`/`paymentConfirmedAt`/`paymentProofUrl`) pero no se mostraba en pantalla. **(3) Confirmado, no hizo falta construir nada nuevo** — la configuración de pago manual en CUP que se pidió agregar ("número de cuenta, teléfono de verificación... aparte de la integración con Stripe que ya tengo") YA EXISTE completa: `AdminSubscriptions.jsx` tiene un formulario real (`cupBankAccountNumber`/`cupBankAccountHolder`/`cupBankInstructions`/`cupSubscriptionPriceCup`, `PATCH /admin/settings/cup-payment`) y `PagoManual.jsx` (vista del vendedor) ya la muestra y deja reclamar el pago con comprobante opcional — se lo hace notar al cliente en el reporte final en vez de reconstruirlo. **Bug real encontrado probando `expireStalePendingPaymentApprovals()` por primera vez** (con un vendedor de prueba aislado, nunca el vendedor real "Casa Verde" que el cliente ya tenía genuinamente en `PENDING_PAYMENT` de sus propias pruebas de hoy): `PrismaClientValidationError: Invalid value for argument type. Expected VendorNotificationType` — el nuevo tipo se había sumado al mapa JS de `verificationNotify.service.js` pero no al enum real de `schema.prisma`; la transacción de `transitionVendorVerification` (cambio de estado + log) YA había committeado antes de fallar el envío de notificación — comportamiento preexistente de esa función (la notificación se manda DESPUÉS de la transacción, sin volver atrás si falla), no algo introducido acá, dejado tal cual por "no romper nada". Se corrigió agregando el valor al enum (migración nueva), regenerando el cliente de Prisma (mismo procedimiento ya establecido en esta sesión para el EPERM de Windows: matar el proceso del backend por PID, `prisma generate`, reiniciar) y volviendo a correr la prueba completa con éxito (`status:"REJECTED"` real + notificación real con título/body correctos) — vendedor de prueba revertido a su estado original después. `node --check` limpio en los 6 archivos backend tocados, `npx prisma validate` y `migrate status` limpios, `vite build` limpio.
- **Bloque 146 (pedido explícito, misma solicitud extensa — captura real de rostro/documento en el KYC):** el `CameraCapture.jsx` existente era 100% decorativo pese a las apariencias — la "línea láser verde" sobre el óvalo de la selfie y el marco punteado del documento eran una animación CSS en loop sin ninguna detección real detrás; la única forma de tomar la foto era el botón manual "Capturar" (bug real, no cosmético — el pedido explícito fue "quiero que ese láser detecte cuándo la persona tenga la cara puesta en el círculo y automáticamente va a ser el scanner"). Reescrito de punta a punta: **(1) Selfie**: `FaceDetector` nativo del navegador (Shape Detection API) corre un loop (`requestAnimationFrame`, throttled a ~180ms) mientras la cámara está activa, verificando que la cara detectada esté centrada en el óvalo y a una distancia razonable (`isFaceWellFramed`) — 9 frames buenos seguidos (~1.6s) disparan la cuenta regresiva y la foto sola, sin tocar nada; el óvalo se pone verde y se rellena como feedback visual mientras se cumple la condición (antes la línea se movía siempre, sin importar si había o no una cara bien puesta). **(2) Documento**: heurístico real que no depende de ninguna API de detección (funciona en cualquier navegador) — un canvas chico de muestreo (64×48) compara frame a frame (poco movimiento = cámara quieta) y mide el rango de tonos (contraste real = hay algo adentro del marco, no una superficie lisa/pared) — mismo criterio de 9 frames estables antes de disparar la foto sola, tal como se pidió ("cuando el documento esté bien enmarcado... la foto se va a tomar automáticamente"). **(3) Botón manual "Capturar" se mantiene SIEMPRE disponible** en los 2 casos — decisión explícita de diseño: la detección automática es un plus aditivo, nunca la única vía, así ningún navegador sin soporte (o una condición de luz/cámara mala) deja a alguien trabado sin poder terminar el trámite. **(4) Video de liveness, pedido explícito — "le va a pedir a la persona que se mueva la cabeza a la izquierda... y a la derecha... un video grabado con la foto cuando mueve la cabeza"**: si el navegador soporta `FaceDetector` Y `MediaRecorder` a la vez, después de la foto frontal arranca una grabación real (`MediaRecorder` sobre el mismo stream de la cámara) con 2 fases guiadas en pantalla ("Girá la cabeza hacia la IZQUIERDA/DERECHA" con ícono direccional + badge "REC" pulsante), cada una detectando un corrimiento horizontal real del centro de la cara (`LIVENESS_SHIFT_THRESHOLD`, con el signo corregido porque el `<video>` NUNCA se espeja en este archivo — se analiza el frame tal cual lo ve la cámara), con un tope de ~4.5s por lado y un botón manual "Ya giré, continuar" para nunca dejar a nadie esperando indefinidamente; el video resultante sale por un `onVideoCaptured` nuevo. Si el navegador no soporta esta combinación, el flujo sigue exactamente como antes (foto y listo) — cero cambio de comportamiento para esos casos. **(5) Pedido explícito — "el recuadro de la selfie debe indicar que debe coincidir con la foto del documento":** se agregaron textos explicativos arriba de cada `CameraCapture` en `VendorVerification.jsx` aclarando esto para la selfie y pidiendo que el documento esté bien enfocado y legible para el otro. **Backend**: `selfieVideoUrl` nuevo en `VerificationRequest` y `VerificationArchive` (migración), multer (`kycUpload.js`) extendido con `.webm`/`.mp4` y el límite subido de 8MB a 20MB (un video pesa más que una foto), campo opcional `selfieVideo` sumado al `.fields()` existente (backward-compatible, una solicitud sin video sigue funcionando igual), servido tanto en vivo (`getVerificationFile`) como archivado (`getVerificationArchiveFile`) con un nuevo branch `type:"video"`, copiado también al archivar (`archiveVerificationDocuments`) y expuesto al admin (`listVerifications`). **Frontend**: `PrivateDocument.jsx` gana un prop `kind` — `"video"` pinta un `<video controls>` en vez de `<img>`, mismo blob autenticado de siempre; se usa en `AdminVerifications.jsx` y en `VendorProfile.jsx` (vista del propio vendedor de sus documentos enviados). **Verificación honesta, con las limitaciones reales del ambiente**: `vite build` limpio y `node --check` limpio en los 9 archivos backend tocados; se confirmó con Playwright, evaluando en el navegador real de prueba (Chrome 151), que **`FaceDetector` NO está disponible en este ambiente puntual** (`'FaceDetector' in window` → `false`, `MediaRecorder` sí está) — es decir, en este navegador de prueba específico la detección de selfie y el video de liveness caerían al camino manual de siempre (foto con el botón, sin auto-detección ni video), mientras que la detección de documento (que no depende de ninguna API especial) sí sería funcional. No se pudo probar con clics reales contra una cámara física ni con una sesión de vendedor logueada (sin credenciales a mano en este momento, mismo criterio de no saltear el login que ya se usó en bloques anteriores de esta sesión) — la implementación se apoya en revisión de código cuidadosa y en las 2 APIs siendo estándares documentados (Shape Detection API, MediaRecorder API), con el botón manual como red de seguridad universal en cualquier escenario no cubierto. Queda documentado como caveat real para el cliente: el soporte de `FaceDetector` es históricamente Chrome/Edge-only y ni siquiera garantizado ahí (confirmado en este mismo ambiente) — nunca en Safari/Firefox — así que la selfie auto-detectada y el video de giro de cabeza son una mejora que aparece cuando el navegador la soporta, no algo garantizado en el 100% de los casos; el trámite en sí (foto + documento) SIEMPRE se puede completar en cualquier navegador via el botón manual, que nunca se deshabilita ni se oculta.
- **Bloque 147 (3 bugs/pedidos reales reportados en vivo, con captura — "las imágenes de los productos no se ven en el carrito, si hago clic en un producto del carrito no me dirige a ver ese producto, y quiero agregar un botón para vaciarlo completamente"):** **(1) Fotos ausentes, bug real de origen:** el recuadro de cada línea del carrito (`CartDrawer.jsx` y la página completa `Cart.jsx`) SIEMPRE fue un `<div>` vacío con fondo gris (`bg-surface-container`, sin ninguna `<img>` adentro) — nunca fue un problema de que la imagen no cargara, el ítem del carrito nunca tuvo ningún campo de imagen para empezar. Causa real: los 3 puntos donde un producto entra al carrito (`AddToCartControl.jsx`, `Product.jsx`, `StoreChatWidget.jsx`) arman el objeto que recibe `CartContext.addItem` con exactamente los campos que hacían falta para precio/stock (id, nombre, precio, stock...) pero NUNCA copiaban `product.images`/`product.slug` — y `CartContext.jsx` tampoco los guardaba aunque hubieran llegado. **(2) Clic sin navegación, mismo origen:** sin `slug` guardado en el ítem, no había forma de construir la URL de la ficha del producto (`/producto/:vendorSlug/:slug`) — el bug de "no me lleva al producto" es consecuencia directa del mismo campo faltante, no un problema de manejo de clics aparte. **Fix**: se suma `image` (mismo nombre de campo que ya usa `SharedCart.jsx`/`sharedCart.controller.js` para el mismo propósito, un solo string, no el array `images` completo) y `slug` en los 3 puntos de entrada y en los 2 lugares donde `CartContext.jsx` arma el ítem (`addItem`, tanto ítem nuevo como cantidad actualizada de uno existente, y `resolveConflict`) — se guardan tal cual llegan, mismo criterio que el resto de los campos de ese objeto. `sharedCart.controller.js` (resuelve un carrito compartido contra el catálogo real) ya mandaba `image` desde antes pero nunca `slug` — se agrega ahí también, por la misma razón. En la UI, la imagen (con `imgUrl()`, mismo helper que ya usa `ProductCard.jsx`/`SharedCart.jsx` — antepone el origin del backend salvo que ya sea un link externo) y el nombre del producto pasan a ser un `<Link>` real a la ficha del producto — SOLO cuando el ítem tiene `slug` (un ítem viejo, agregado antes de este bloque y todavía en el `localStorage` de algún cliente, se degrada a texto/imagen sin clic en vez de armar un link roto a `/producto/undefined/undefined`); un ícono `ImageOff` (lucide-react) reemplaza el cuadro gris vacío cuando de verdad no hay foto (producto sin ninguna imagen cargada). Los controles de cantidad/eliminar quedan FUERA del `<Link>` (nunca envolviendo toda la fila) — así no hace falta ningún `stopPropagation`, tocar "+"/"-"/la papelera nunca dispara una navegación accidental. **(3) Vaciar carrito completo, pedido explícito:** `CartContext.clearCart()` ya existía de antes (lo usa el bot de tienda cuando el cliente pide "vaciá el carrito" por chat, ver Bloque 40) pero nunca tuvo un botón real y visible en la UI del carrito. Se agrega en los 2 lugares — "Vaciar" junto al nombre de la tienda en `CartDrawer.jsx`, "Vaciar carrito" junto a "Compartir carrito" en `Cart.jsx` — ambos con confirmación (`ConfirmDeleteModal`, el mismo componente ya usado para eliminar un ítem individual) antes de ejecutar, para que no se pierda el carrito entero por un toque accidental. `ConfirmDeleteModal` ganó un prop opcional `zIndex` (default `"z-50"`, el de siempre — los otros 12 usos existentes no lo pasan y no cambian) porque el mini-carrito vive en `z-[61]`, por encima del `z-50` de siempre; sin esto la confirmación hubiera quedado tapada DETRÁS del panel en vez de encima. Verificado en vivo con Playwright, con productos reales de TecnoHabana (fotos reales `picsum.photos` cargadas en el Bloque 132): se agregó "Audífonos inalámbricos Pro" al carrito desde su ficha real — el drawer y la página `/carrito` mostraron la foto real del producto (antes un cuadro gris vacío, confirmado con capturas de pantalla reales); se hizo clic real en el nombre del ítem dentro del drawer estando en la página de Inicio y navegó correctamente a `/producto/tecnohabana/audifonos-inalambricos-pro` (drawer cerrado de una, vía el mismo `closeCart` que ya usan el resto de los links del drawer); se tocó "Vaciar" en el drawer — apareció la confirmación correctamente por ENCIMA del panel (no tapada), y confirmarla vació el carrito de una (pantalla de "Tu carrito está vacío"). Carrito de prueba limpiado de `localStorage` al terminar. `vite build` limpio, `node --check` limpio en `sharedCart.controller.js`.
- **Bloque 148 (bug real reportado en vivo, con captura, follow-up inmediato del Bloque 147 — "por qué el ícono de verificación de la tienda está al lado del botón de vaciar carrito, y quiero que el botón de vaciar carrito esté encerrado en un contenedor transparente, con borde fino de un color redondeado, y que todo esté bien estructurado y responsivo"):** **(1) El check de verificado pegado al botón de vaciar, bug real de layout:** la fila del vendedor en `CartDrawer.jsx` era un solo `flex` con `gap-2.5` parejo entre CUATRO elementos (avatar, nombre en un `flex-1`, badge, botón) — el `flex-1` del nombre empuja el resto del contenido hacia la derecha, pero el badge y el botón, al compartir el mismo gap fijo entre sí, terminaban pegados el uno al otro en el extremo derecho, sin ninguna relación visual real entre ellos (el check es de la TIENDA, no de la acción de vaciar). El propio componente `VerifiedBadge.jsx` ya documenta su uso correcto en un comentario ("usar siempre junto al nombre de la tienda") — la implementación del Bloque 147 se apartó de esa convención sin querer al ubicar el botón nuevo en la misma fila. **Fix**: se reagrupa la fila en 2 mitades reales con `justify-between` — del lado izquierdo, un único grupo `min-w-0` con avatar + nombre truncable + badge (el badge ahora vive pegado al nombre, como corresponde); del lado derecho, solo el botón "Vaciar", ya sin compartir gap con nada del vendedor. **(2) Contenedor transparente con borde fino, pedido explícito:** "Vaciar" pasa de texto suelto subrayado al hover a una píldora real (`rounded-full`, fondo transparente, `border border-error/35` — un borde fino en el mismo rojo que ya usa el texto, no un gris neutro) con `hover:bg-error/10` como único cambio visual al pasar el mouse — mismo tratamiento aplicado también al botón "Vaciar carrito" de la página completa `/carrito` (`Cart.jsx`), que antes tenía un borde gris neutro (`border-outline-variant`, igual al de "Compartir", sin distinguirse como acción destructiva) — ahora ambos comparten el mismo lenguaje visual. **(3) Responsivo, pedido explícito:** en `CartDrawer.jsx`, el nombre de la tienda trunca primero (`min-w-0`/`truncate`) dejando SIEMPRE visible tanto el badge como la píldora completa de "Vaciar" (`flex-shrink-0` en ambos, `whitespace-nowrap` en el botón para que el texto nunca se parta en 2 líneas) — probado con un nombre de tienda artificialmente largo ("Súper Tienda de Electrónicos y Accesorios de La Habana") a 375px de ancho: el nombre cortó con "…", el check y la píldora se mantuvieron completos y sin superposición. En `Cart.jsx`, el grupo "Compartir carrito"/"Vaciar carrito" ganó `flex-wrap` (antes solo el contenedor exterior del título envolvía; el grupo interno de 2 botones podía quedarse sin espacio en pantallas muy angostas) para que, si no entran ambos en una fila a un ancho mínimo, el segundo baje en vez de recortarse. Verificado en vivo con Playwright a 1280px y a 375px (con Playwright real, agregando "Audífonos inalámbricos Pro" al carrito de TecnoHabana): capturas de pantalla confirmaron el check junto al nombre en ambos anchos, la píldora de "Vaciar" con borde fino visible y sin overlap con nada, y el mismo comportamiento sosteniéndose con el nombre de tienda largo de prueba. Carrito de prueba limpiado de `localStorage` al terminar. `vite build` limpio.
- **Bloque 149 (pedido explícito — "elimina la animación del botón de compartir"):** `ShareCartButton.jsx` tenía un anillo pulsante (`animate-ping`) detrás del ícono, activado por una prop `animated` que solo pasaba el botón ícono-solo del mini-carrito (`CartDrawer.jsx`, header) — la página completa `/carrito` nunca la usaba. Se saca el anillo y la prop `animated` por completo (no solo se deja de pasar desde `CartDrawer.jsx` — se elimina del componente entero, ya que ningún llamador la necesitaba después de esto, para no dejar código muerto). El ícono `Share2` queda estático, sin ninguna animación. Verificado en vivo con Playwright: el botón "Compartir carrito" del mini-carrito ya no tiene el `<span>` con `animate-ping` en el DOM. `vite build` limpio.
- **Bloque 150 (pedido explícito, muy extenso — reescribe de fondo el archivo de verificación y el cobro de la suscripción):** dos pedidos grandes en un mismo turno. **(1) Archivo consultable desde el envío, pedido explícito — "una vez una tienda envíe una verificación, debe quedar archivado todo el historial... desde que el cliente envía la solicitud, y siempre se pueda consultar":** el Bloque 72 ya archivaba documentación, pero recién al llegar a `VERIFIED` — un ciclo que quedaba en curso, o que terminaba rechazado, no dejaba NINGÚN registro permanente aparte del estado actual (que se pisa en el próximo reenvío). Se mueve la creación de la rama de archivo (`VerificationArchive`) del final (`transitionVendorVerification` al llegar a `VERIFIED`) al INICIO (`archiveVerificationSubmission`, llamada desde `submitVerification` apenas se suben los documentos) — nuevo campo `VerificationRequest.archiveId` (FK única) apunta a la rama del ciclo VIGENTE; un `syncVerificationArchive(vendorId, patch)` nuevo (único punto que la actualiza) se llama en cada paso real del ciclo — revisión de documentos (`updateVerification`), método de pago + meses elegidos (`chooseMyPaymentMethod`), pago reclamado con todos sus datos (`uploadMyPaymentProof`), Stripe confirmando el cobro (`handleCheckoutCompleted`), pago confirmado por un admin (`confirmSubscriptionPayment`), y el cierre final (`verifiedAt`/`rejectedAt`, centralizado en `transitionVendorVerification` para los 2 desenlaces posibles) — así la rama queda completa y consultable en CUALQUIER momento del trámite, no solo al final. Un reenvío tras un rechazo crea una rama NUEVA (mismo criterio de siempre, verificado en vivo — ver más abajo) — la vieja nunca se pisa ni se borra sola. **Gate de borrado corregido de paso**: el criterio viejo (`vendor.verificationStatus !== "VERIFIED"`) dejó de alcanzar — con ramas creadas desde el envío, ese criterio hubiera dejado borrable la rama de un ciclo EN CURSO (todavía sin verificar) o recién rechazado (esperando reenvío), perdiendo evidencia de un trámite activo. El gate real ahora es "¿es esta la rama que `VerificationRequest.archiveId` apunta ahora mismo?" (`deleteVerificationArchive`, backend) — esa nunca se puede borrar sin importar en qué haya terminado; el endpoint de listado (`listVerificationArchive`) devuelve `currentArchiveId` para que el admin (`AdminVerifications.jsx`) refleje el mismo gate por rama, no un flag único para toda la tienda como antes. La rama también gana los datos del ciclo de pago completo (método, meses, quién declaró haber pagado, cuándo se reclamó/confirmó Stripe/admin, y el desenlace) — mostrados en `ArchiveBranch` junto a los documentos de siempre, sumando también el video de liveness (Bloque 146, no se mostraba en el archivo hasta ahora) y el comprobante de pago archivado (nuevo branch `type:"proof"` en `getVerificationArchiveFile`). **(2) Pago de suscripción rediseñado de punta a punta, pedido explícito — meses a elegir (1-24), captura de pago obligatoria para los 2 métodos, datos de quién pagó, Stripe deja de activar solo:** **Meses**: `chooseMyPaymentMethod` ahora recibe `{method, months}` juntos (un solo paso, `MIN_PAYMENT_MONTHS=1`/`MAX_PAYMENT_MONTHS=24`) — el monto real es precio mensual × meses, calculado siempre server-side (`getSubscriptionPricing()`, nuevo en `settings.controller.js`, lee `SiteSettings.cupSubscriptionPriceCup`/`cardSubscriptionPriceUsd` — este último es NUEVO, antes una constante `SUBSCRIPTION_PRICE_USD=25` hardcodeada en `lib/stripe.js`, ahora editable desde el mismo formulario de admin que ya editaba el precio CUP, "Datos de pago de la suscripción" en `AdminSubscriptions.jsx`). **Stripe deja de ser una suscripción recurrente, pedido explícito — "mejor utilizaremos Stripe, para generar el pago correspondiente al monto con la cantidad de meses... el cliente será redirigido a la página de confirmación de pago... donde podrá hacer la captura y subirla"**: `createVerificationCheckoutSession` pasa de `mode:"subscription"` (Bloque 64 — Stripe cobraba cada mes solo, vía `invoice.payment_succeeded/failed`) a `mode:"payment"` — un cobro ÚNICO por el bloque completo de N meses, sin ningún cobro automático futuro; renovar es volver a elegir método+meses (mismo gesto que ya usaba CUP desde el Bloque 64). El webhook `checkout.session.completed` (`handleCheckoutCompleted`) YA NO activa la verificación solo — dejaba de ser cierto lo que decía el comentario del Bloque 25 ("el único paso 100% automático de todo el ciclo"): ahora solo deja una marca (`stripePaidAt`, `stripePaymentIntentId`) y notifica al vendedor que falta subir su comprobante; el resto del ciclo es EXACTAMENTE el mismo que CUP. Los webhooks recurrentes viejos (`invoice.payment_succeeded/failed`, `customer.subscription.deleted`) se dejan INTACTOS a propósito — nunca se tocan ni se eliminan — porque siguen gobernando cualquier suscripción de Stripe REAL creada antes de este bloque (`mode:"subscription"`); los checkouts nuevos nunca generan una Subscription real, así que esos 3 webhooks simplemente no encuentran ningún vendor para sus eventos y no hacen nada — cero riesgo de romper el cobro de quien ya estaba pagando así. El cron de vencimiento (`verificationPayment.job.js`, antes filtraba `verification.paymentMethod === "CUP_TRANSFER"` a secas) se generaliza a `stripeSubscriptionId: null` — separa correctamente "suscripción real de Stripe, legado, gobernada por webhooks" de "bloque prepago de N meses (CUP o CARD nuevo estilo), gobernado por este cron", sin importar el método. **Comprobante obligatorio para los 2 métodos, pedido explícito — "para ambas cosas... el cliente siempre deberá subir una captura de pantalla del pago":** `uploadMyPaymentProof` reescrito por completo — `payerName` (nombre y apellidos) y `paymentAmount` (monto que el vendedor declara haber pagado) obligatorios SIEMPRE, para los 2 métodos; CUP exige además `payerAccountNumber`+`payerPhone` (mismo pedido: "número de cuenta y número de teléfono"); CARD exige la captura O (si no se pudo) `proofUnavailable:true` + datos de respaldo (`payerPhone`/`payerAddress`/`payerCountry`, `payerCardLast4` opcional) — **nunca el número completo de la tarjeta**, corrigiendo la propia corrección que hizo el usuario en el pedido ("el número de la tarjeta no se pedirá, solo el nombre... debe coincidir con el nombre de la tarjeta"), y limitando el respaldo a los últimos 4 dígitos únicamente en el caso sin-captura (fuera de alcance PCI pedir más — este sistema no está certificado para guardar números de tarjeta completos, Stripe ya los tiene). Bug de zod evitado a propósito (documentado ya en `announcements.controller.js`, mismo patrón reusado acá vía `boolish`): `z.coerce.boolean()` convierte CUALQUIER string no vacío en `true`, incluido `"false"` — con FormData mandando todo como string, eso hubiera hecho que "no marcar la casilla" (`proofUnavailable` nunca ausente, sino `"false"` literal) se leyera como `true` igual; se usa el mismo `z.preprocess` explícito ya establecido. Confirmar el pago (`confirmCupPayment` → renombrado `confirmSubscriptionPayment`, mismo endpoint HTTP) ahora acepta los 2 métodos por igual, exige `paymentClaimedAt` seteado (el vendedor ya reclamó el pago) antes de poder confirmar, y calcula `nextPaymentDueDate` con los meses reales elegidos (`paymentMonths ?? 1` como piso de compatibilidad) en vez de un mes fijo siempre. **Frontend**: `VendorVerification.jsx` suma un selector de meses (stepper 1-24) con el monto recalculado en vivo por método/moneda antes de los 2 botones; el redirect de Stripe (`?stripe=success`) ya no se queda esperando una activación automática — manda derecho a `/vendedor/pago-manual` a subir el comprobante. `PagoManual.jsx` (antes 100% CUP) se generaliza para los 2 métodos — mismo formulario base (nombre, monto) con los campos específicos de cada uno, y el checkbox "No pude hacer la captura" solo para CARD. `AdminVerifications.jsx` fusiona el viejo bloque informativo-nada-más de CARD (`showCardInfo`, "la activación es 100% automática, no hay nada para hacer acá" — ya no es cierto) con el bloque de confirmar de CUP (`showCupConfirm`) en un único `showPaymentConfirm` que muestra todos los datos declarados (quién pagó, cuánto, cuenta/teléfono o respaldo sin captura) + el monto ESPERADO al lado para comparar, con el mismo botón "Confirmar pago recibido y activar" para los 2 métodos. **Verificado en vivo**: simulación completa de un ciclo real contra la base de datos (vendedor de prueba aislado, "Moda Caribe", nunca tocado para pruebas reales) — envío de documentos → rama de archivo creada y vinculada (`archiveId`) → aprobación → elegir CUP + 3 meses → reclamar pago (datos completos) → confirmar → `VERIFIED` con `nextPaymentDueDate` a ~90 días exactos (3 meses × 30 días) → rama con `verifiedAt` cerrado → gate de borrado bloqueando la rama vigente → reenvío tras un rechazo simulado creando una SEGUNDA rama nueva, la primera intacta con su `verifiedAt` preservado (2 ramas totales al final, confirmado) — los 7 pasos dieron el resultado esperado, datos de prueba revertidos al terminar. Los 2 schemas de zod nuevos (`paymentMethodSchema`, `claimPaymentSchema`) se probaron por separado (exportados temporalmente, revertido después): límites de meses (0 rechaza, 1 y 24 pasan, 25 rechaza, no-entero rechaza), método inválido rechaza, y el `boolish` de `proofUnavailable` confirmado con `"false"`→`false`/`"true"`→`true`/ausente→`false` (el bug de `z.coerce.boolean()` que se quería evitar). `vite build` limpio, `node --check` limpio en los 11 archivos backend tocados, `npx prisma validate`/`migrate deploy` limpios (2 migraciones nuevas). **No se pudo verificar con clics reales en el panel de admin ni en el panel de vendedor** — las credenciales de desarrollo documentadas en `cuba-seed-data.js` (`admin@zeudin.com`) ya no coinciden con la contraseña real de esta base (mucho más adelante que el seed original, tras ~150 bloques de cambios manuales en esta misma sesión) y, siguiendo el mismo criterio ya establecido en bloques anteriores de esta sesión, no se intentó resetear ninguna contraseña ni saltear el login para conseguirlo — la verificación se apoyó en la simulación real contra la base de datos (no un mock, son las mismas funciones de servicio que usan los controllers) más revisión de código cuidadosa de los 2 controllers HTTP (`verification.controller.js`/`admin.controller.js`) y los 6 componentes de frontend tocados.
- **Bloque 151 (2 pedidos, mismo turno — follow-up inmediato del Bloque 150): (1) bug real reportado en vivo, con captura — el archivo de verificación de Casa Verde salía vacío pese a que la tienda ya había enviado su solicitud; (2) pedido explícito — poder cambiar el método de pago ya elegido, tanto del lado del vendedor como del admin, "restableciéndolo" para que se pueda elegir otro distinto.** **(1) Archivo vacío para solicitudes YA en curso antes del Bloque 150:** `archiveVerificationSubmission()` solo se llama desde dentro de `submitVerification` — cualquier `VerificationRequest` que ya existiera ANTES de que el Bloque 150 se desplegara (como Casa Verde, real, en `PENDING_PAYMENT` genuino de pruebas anteriores del usuario) nunca pasó por ese código, así que se quedó con `archiveId: null` para siempre — el modal de archivo no mentía, literalmente no había ninguna rama creada. Se agrega `ensureVerificationArchive(vendorId)` (backfill perezoso, `vendorVerification.service.js`) — si la solicitud vigente de una tienda no tiene `archiveId`, arma una rama AHORA MISMO con el snapshot completo de cómo está hoy (documentos + todo lo que ya se haya avanzado del ciclo de pago: método, meses, revisión, reclamo, confirmación), llamada desde `listVerificationArchive` justo cuando un admin abre el archivo de esa tienda — nunca queda vacío para siempre por haberse creado antes de este bloque. Se refactorizó `createArchiveBranch()` como helper compartido entre `archiveVerificationSubmission` (rama nueva, en blanco) y `ensureVerificationArchive` (rama de backfill, con los campos ya avanzados). **Límite honesto documentado**: esto NO reconstruye ciclos que ya se REEMPLAZARON por un reenvío posterior (un rechazo-y-reenvío de antes de este bloque perdió esos datos viejos con el upsert de `VerificationRequest`, no hay forma de recuperarlos) — solo cubre la solicitud VIGENTE de cada tienda, que es exactamente el caso reportado. Aplicado en vivo sobre la Casa Verde real (no una tienda de prueba aislada — acá SÍ correspondía tocar sus datos reales, porque es la corrección real del bug, no una simulación): confirmado que ahora tiene 1 rama archivada con sus datos reales (`fullName: "Mariulkis Ge..."`, `paymentMethod: CUP_TRANSFER`, `submittedAt`/`reviewedAt` reales) — nunca se tocó ningún otro dato suyo (`verificationStatus`, documentos, nada), la rama se agrega, no se modifica nada existente. **(2) Cambiar/restablecer el método de pago, pedido explícito — "el vendedor... puede cambiar si luego se arrepiente de esa forma de pago... también se podrá cambiar desde el panel de administrador... o restablecerlo para que él pueda seleccionar otro diferente":** el backend (`chooseMyPaymentMethod`) YA aceptaba elegir un método de nuevo en pleno `PENDING_PAYMENT` sin ningún cambio (limpia el rastro del anterior, mismo código que ya usaba para SUSPENDED/PAYMENT_FAILED) — el bloqueo real era 100% de UI: una vez elegido un método, `VendorVerification.jsx` nunca volvía a mostrar el selector mientras siguiera en `PENDING_PAYMENT`. Se agrega un estado local `changingMethod` — un link "Cambiar método de pago" en la sección de tarjeta (esperando pagar en Stripe) y en la de "avisar que ya pagué" (CUP o CARD ya cobrado por Stripe) reabre el mismo selector de método+meses de siempre, con un "Cancelar" para volver atrás sin elegir nada; elegir un método ahí (aunque sea el mismo de antes) limpia cualquier reclamo/comprobante viejo de forma automática, ya que así funcionaba `chooseMyPaymentMethod` desde su diseño original. **Lado del admin, nuevo por completo**: `PATCH /admin/verifications/:id/reset-payment-method` (`resetVerificationPaymentMethod`, `admin.controller.js`) — limpia `paymentMethod` y todo rastro de ese ciclo de pago (meses, moneda, reclamo, confirmación, checkout de Stripe, datos del pagador) SIN tocar `verificationStatus` (la tienda sigue en `PENDING_PAYMENT`/`PAYMENT_FAILED`/`SUSPENDED` como antes) — el vendedor simplemente vuelve a ver el selector en su panel. Nuevo tipo de notificación `VERIFICATION_PAYMENT_METHOD_RESET` (bell + email) avisándole que tiene que elegir de nuevo; `logActivity` deja registro real de la acción del admin (con el método anterior, para auditoría). Botón "Restablecer método de pago" agregado en `AdminVerifications.jsx`, en las 2 secciones donde ya hay un método elegido (esperando el pago, y esperando confirmar un pago ya reclamado — por si el vendedor reclamó mal y conviene que empiece de cero en vez de rechazar la solicitud entera). **Bug real encontrado y corregido durante la prueba en vivo (antes de dar el código por terminado)**: el primer intento de `resetVerificationPaymentMethod` armaba un único objeto `reset` con TODOS los campos a limpiar y lo pasaba tal cual tanto a `prisma.verificationRequest.update` como a `syncVerificationArchive` (que termina en `prisma.verificationArchive.update`) — pero `VerificationArchive` nunca tuvo columnas `stripeCheckoutSessionId`/`stripeCheckoutUrl`/`stripeCheckoutExpiresAt`/`stripePaymentIntentId` (esos son estado operativo efímero del link de checkout, nunca se pensaron como "archivables" — solo `stripePaidAt`, el HECHO de que Stripe confirmó, si se archiva). `PrismaClientValidationError: Unknown argument` reventó en la primera corrida de la prueba real contra la base — se corrigió desestructurando esos 4 campos fuera del objeto antes de mandarlo a `syncVerificationArchive` (`archivePatch`), dejándolos solo en el update de `VerificationRequest`, donde sí existen. Vuelto a correr la prueba completa con éxito. **Verificado en vivo**: (a) backfill de archivo aplicado de verdad sobre la Casa Verde real, confirmado con una consulta directa a la base — ver arriba; (b) simulación completa del reset contra un vendedor de prueba aislado ("Moda Caribe", nunca tocado para pruebas reales) — método CUP con 6 meses ya reclamado (`paymentClaimedAt` seteado) → reset → `paymentMethod` vuelve a `null` en la solicitud Y en la rama de archivo, `vendor.verificationStatus` intacto (`PENDING_PAYMENT`, sin tocar) → se pudo elegir CARD con 12 meses después sin ningún error, confirmando que el "cambio real" de método funciona de punta a punta — datos de prueba revertidos al terminar. `vite build` limpio, `node --check` limpio en los 6 archivos backend tocados, `npx prisma validate`/`migrate deploy` limpios (1 migración nueva, el enum de notificación). No se pudo probar con clics reales en el panel (misma limitación de credenciales ya documentada en el Bloque 150).
- **Bloque 152 (2 correcciones, con capturas, follow-up inmediato del Bloque 151): (1) bug real — "el responsable debe ser la misma persona a la que está registrada la cuenta" (el archivo de Casa Verde mostraba "Responsable: —" con el KYC ya completo al lado); (2) pedido explícito — "en los datos para transferir deben aparecer más detalles: cuántos meses está pagando el cliente, el nombre de la tienda que está registrado y demás".** **(1) "Responsable" vacío pese a tener el nombre real disponible:** `Vendor.ownerName` ("persona física responsable del negocio", cargada en Configuración) y `VerificationRequest.fullName` ("nombre completo del responsable", capturado en la verificación KYC) son la MISMA persona por definición — el propio comentario del schema de `ownerName` ya lo decía. `createArchiveBranch()` (`vendorVerification.service.js`) copiaba `ownerName` tal cual, sin fallback — una tienda que nunca cargó ese campo en Configuración (como Casa Verde, real) archivaba "Responsable" vacío aunque el nombre real YA estuviera disponible desde el KYC de la misma solicitud. Se agrega el respaldo: `ownerName: vendor.ownerName || v.fullName || null`. **Corregido también en la rama YA archivada de Casa Verde** (creada por el backfill del Bloque 151, con el bug todavía sin arreglar) — se le actualizó `ownerName` a `"Mariulkis Ge"` (el nombre real de su KYC) con una consulta directa, confirmado antes/después. **(2) Más detalle en "Datos para transferir"** (`PagoManual.jsx`): antes solo mostraba el monto total con los meses metidos entre paréntesis al lado, poco visible. Se agregan como líneas propias: **Tienda** (`vendor.companyName`, vía `useOutletContext()` — el mismo patrón que ya usa `VendorVerification.jsx` para leer el vendor desde `VendorLayout.jsx`), **Duración** (meses, con el precio por mes al lado cuando son más de 1, para que se entienda de dónde sale el total), y **Monto total a transferir** como su propia línea — mismo tratamiento para CARD (Tienda/Duración/Monto pagado) y CUP (que además suma Cuenta/A nombre de/Instrucciones, ya existentes). De paso, y en línea con el punto (1) — "el responsable debe ser la misma persona registrada" — el campo "Nombre y apellidos de quien pagó" del formulario de reclamo ahora se prellena con `vendor.ownerName` si ya está cargado (sigue editable, por si de verdad pagó alguien distinto a nombre de la tienda) en vez de arrancar vacío. Verificado: `vite build` limpio; la corrección del archivo de Casa Verde se aplicó y confirmó de verdad contra la base de datos real (antes `ownerName: null`, después `"Mariulkis Ge"`) — el resto (la UI de `PagoManual.jsx`) se revisó con cuidado por código, misma limitación de credenciales de los bloques 150/151 para clickear la pantalla en vivo.
- **Bloque 153 (pedido explícito, el más extenso de esta serie — 5 subsistemas distintos en un solo pedido): (1) montos estrictos y sin editar; (2) renovación de la suscripción mientras la tienda YA está VERIFIED, con historial real; (3) candado sobre los datos de identidad de una tienda verificada + solicitud de cambio al admin; (4) UX del ciclo de pago (botón, "en revisión", paso nuevo del stepper); (5) el selector de orientación de ofertas, sin relación con lo anterior.** **(1) "El monto pagado no se puede editar... quiero que esas cuentas sean bien estrictas":** `paymentAmount` deja de ser un campo que el vendedor tipeaba (aunque prellenado) — se saca del todo de `claimPaymentSchema` y `PagoManual.jsx` ya no tiene ese input; el monto se calcula SIEMPRE server-side (`getSubscriptionPricing()` × meses elegidos) en el momento exacto de reclamar el pago, tanto para el ciclo inicial (`uploadMyPaymentProof`) como para una renovación (`claimRenewalPayment`, ver punto 2) — nunca se vuelve a confiar en un número que mande el cliente. **(2) Renovar mientras VERIFIED, con historial real, pedido explícito — "una vez el cliente esté verificado, en la sección de suscripción podrá ver cuántos días vence, fecha de inicio y fin, y podrá activar un nuevo mes o varios... el proceso será casi igual... se guardará todo ese historial... el admin debe aprobar los demás meses pagos":** nuevo modelo `SubscriptionPayment` — a propósito SEPARADO de `VerificationRequest` (que se REUTILIZA en cada ciclo, nunca guarda historial de por sí) — cada renovación es una fila propia e inmutable una vez confirmada. Mismo patrón end-to-end que el ciclo inicial pero apuntando a esta tabla: `chooseMyRenewalPayment` (elige método+meses, Stripe genera un Checkout de pago único si es CARD — `createRenewalCheckoutSession`, extraído de una base compartida con `createVerificationCheckoutSession` en `lib/stripe.js` para no duplicar la llamada al SDK), `claimRenewalPayment` ("Ya pagué", mismos campos/reglas que el ciclo inicial), `confirmSubscriptionRenewal` (admin) y `getMySubscription` (período vigente + renovación en curso + historial). **Cálculo ESTRICTO del nuevo vencimiento** — pedido explícito de que las cuentas sean precisas: si el ciclo actual todavía no venció, el nuevo período se cuenta desde ESE vencimiento real (nunca desde "ahora", que le regalaría días de más a quien ya tenía tiempo pagado por delante); si ya venció, se cuenta desde ahora — verificado en vivo con una llamada real: vencimiento en 10 días → renovación de 3 meses confirmada → nuevo vencimiento exactamente 3 meses después del vencimiento ORIGINAL, no de la fecha de confirmación. El webhook de Stripe (`handleCheckoutCompleted`) ahora distingue `metadata.verificationId` (ciclo inicial) de `metadata.subscriptionPaymentId` (renovación) y enruta a `handleRenewalCheckoutCompleted` — mismo criterio del Bloque 150 de que Stripe deja una marca (`stripePaidAt`) pero nunca activa nada solo, un admin siempre confirma. **Frontend**: `SubscriptionCard` nuevo en `VendorVerification.jsx`, visible solo con la tienda VERIFIED — inicio/vencimiento/días restantes del ciclo actual, botón "Renovar" que abre el mismo selector de método+meses de siempre, y un desplegable con el historial real de renovaciones confirmadas. `PagoManual.jsx` se generaliza para reclamar TANTO el ciclo inicial como una renovación (detecta el modo según si la tienda ya está VERIFIED y tiene una renovación pendiente, apunta al endpoint que corresponda) — "el proceso es casi igual", tal como se pidió. Lado del admin: pestaña nueva "Renovaciones" en `AdminVerifications.jsx` (`RenewalsList`, lista + confirmar, con el mismo detalle de quién pagó/comprobante que ya tenía el flujo de pago inicial). **(3) Candado sobre datos de identidad + solicitud de cambio, pedido explícito — "todos los datos de la tienda no se pueden modificar después de estar verificadas sin aprobación del admin... si se desea cambiar el nombre o el responsable se debe enviar la solicitud al admin para prevenir fraudes, y en caso dado subir fotos del nuevo responsable y foto del ID":** `updateMyVendor` (backend) rechaza con 409 cualquier intento real de cambiar `companyName`/`ownerName`/`ownerIdNumber`/`companyAddress` una vez `verificationStatus === VERIFIED` — "intento real" es la parte importante: compara contra el valor YA guardado, así que un formulario que reenvía estos campos sin tocarlos (patrón normal de esta base, ver comentario del código) nunca se rompe, solo se bloquea un cambio de verdad. El resto de los datos (descripción, WhatsApp, logo, métodos de pago, moneda, categoría, garantía...) sigue 100% libre — nunca fueron el problema. Nuevo modelo `VendorChangeRequest` — el vendedor propone nuevos valores (solo los campos que quiere cambiar, el resto null) + un motivo opcional + 2 fotos opcionales (nuevo responsable + su ID), obligatorias SOLO si de verdad está proponiendo cambiar QUIÉN es el responsable (corregir, por ejemplo, solo la dirección no las pide). Nuevos endpoints: `POST /vendors/me/change-request` (una sola solicitud PENDING a la vez), `GET /vendors/me/change-request` (estado de la propia), y del lado admin `GET /admin/change-requests` + `PATCH /admin/change-requests/:id` (aprobar APLICA los cambios de verdad a `Vendor`; rechazar solo cierra con un motivo, la tienda queda igual) — ambos desenlaces notifican al vendedor (bell + email, 2 tipos nuevos). **Frontend**: los inputs bloqueados (`OwnerNameCard`/`StoreInfoCard` en `VendorProfile.jsx`, los 2 de facturación en `VendorSettings.jsx`) se deshabilitan y explican por qué cuando la tienda está VERIFIED — el backend ya lo hubiera rechazado igual, esto solo evita el viaje al servidor para nada. Tarjeta nueva "Solicitar cambio de datos" en `VendorProfile.jsx` (`ChangeRequestCard`) con el formulario completo (campos opcionales + motivo + subida de fotos condicional) y el estado de la última solicitud (pendiente/aprobada/rechazada). Lado del admin: pestaña nueva "Cambios de datos" en `AdminVerifications.jsx` (`ChangeRequestsList`) con las fotos del nuevo responsable si aplica y aprobar/rechazar. **(4) UX del ciclo de pago, 3 pedidos puntuales:** el botón que antes decía "Ir a avisar que ya pagué" (para CUP, ANTES de haber transferido nada) pasa a decir **"Empezar con el proceso de pago"** — sonaba a que el pago ya se había hecho. Una vez que el vendedor SÍ reclama el pago, esa misma sección dejó de ser una invitación a hacer algo — pasa a mostrar un estado **"En revisión — el equipo confirma en unas horas"**, sin el botón ni la opción de "Cambiar método de pago" (que sigue disponible SOLO mientras todavía no se reclamó nada, pedido explícito de "no se debe poder cambiar el método... ya hay una en proceso"). El stepper de `VendorVerification.jsx` suma un paso: `["Documentos", "Revisión", "Pago", "Revisión de pago", "Verificado"]` (antes "Pago" cubría las 2 cosas sin distinguir si el vendedor ya había hecho su parte) — `computeStepStates` calcula el paso activo dinámicamente para `PENDING_PAYMENT`/`PAYMENT_FAILED`/`SUSPENDED` según si `paymentClaimedAt` ya está seteado. Panel de admin: cada tarjeta de `AdminVerifications.jsx` ahora muestra "Registrada: fecha" (`Vendor.createdAt`, siempre real) y "Verificada desde: fecha" (la transición MÁS RECIENTE a `VERIFIED` del historial real — `null` si nunca lo estuvo o si es una tienda sembrada que nunca pasó por el código real, honesto en vez de inventar una fecha) — pedido explícito de "que se vea desde cuándo está activa o verificada esa tienda... y si pierde suscripción y la vuelve a retomar, todo eso es historial de la tienda". **(5) Selector de orientación de ofertas, pedido explícito, sin relación con lo anterior — "ya las imágenes vienen con un tamaño así, se debe publicar de ese mismo tamaño y acomodarse en forma de ladrillos como está ya configurado, sin dimensiones específicas":** `VendorOffers.jsx` obligaba a elegir Horizontal/Vertical a mano Y forzaba un recorte a un aspect ratio fijo (`ImageCropUploader`, 900×1200 o 1200×675) antes de poder publicar una oferta personalizada — el propio sistema de "ladrillos" que ya arma el grid público (`OffersSlider.jsx`, `VERTICAL_SPANS`/`HORIZONTAL_SPANS` con `object-cover`) ya está pensado para acomodar cajas de forma distinta sin necesitar un recorte exacto de antemano. Se saca el selector y el recorte — la imagen se sube tal cual, sin tocarla, y `orientation` (que el grid público sigue necesitando para elegir el set de spans correcto) se AUTO-DETECTA de las dimensiones reales del archivo (ancho ≥ alto → horizontal, si no vertical) — mismo criterio para una oferta de PRODUCTO (se lee la imagen ya subida del producto para detectar su forma real). **Verificado en vivo, contra la base de datos real, llamando las funciones EXPORTADAS de los controllers directamente (no una reimplementación aparte de la lógica, para no perderse un bug real de tipeo/wiring)**: ciclo completo de renovación — elegir CUP+3 meses → `SubscriptionPayment` creado con el monto esperado correcto (7500 = 2500×3) → reclamar sin archivo → monto ESTRICTO recalculado server-side (7500, ignora cualquier valor que hubiera mandado el cliente) → admin confirma → `nextPaymentDueDate` extendido EXACTAMENTE 3 meses desde el vencimiento real anterior (no desde "ahora") — confirmado con fechas reales de la respuesta. Solicitud de cambio completa: crear con `companyAddress` nuevo → aprobar → `Vendor.companyAddress` actualizado de verdad. Candado de identidad: intento real de cambiar `companyName` con la tienda VERIFIED devuelve 409 con el mensaje correcto; guardar el formulario completo con el MISMO `companyName` (sin cambiarlo) + una descripción nueva SÍ pasa, sin romper el guardado normal. **Bug real encontrado y corregido durante la prueba (antes de dar el código por terminado)**: el primer intento de la consulta "verificada desde" en `listVerifications` intentaba incluir la MISMA relación (`verificationStatusLogs`) dos veces con filtros distintos dentro de un solo `include` de Prisma (una vez para `PENDING_PAYMENT`, otra para `VERIFIED`) — Prisma no lo permite; se corrigió con una segunda consulta aparte (`distinct: ["vendorId"]` + `orderBy: at desc`, un viaje extra a la base pero simple y correcto) en vez de forzarlo en una sola. Todos los datos de prueba revertidos al terminar cada corrida. `vite build` limpio, `node --check` limpio en los 13 archivos backend tocados, `npx prisma validate`/`migrate status` limpios (2 migraciones de tablas nuevas + 4 de enums de notificación, todas aplicadas). No se pudo probar con clics reales en el panel — misma limitación de credenciales ya documentada en los bloques 150-152; la verificación se apoyó en llamadas reales a las funciones exportadas de los controllers (no una simulación aparte) contra la base de datos real, más revisión de código cuidadosa de los ~15 archivos de frontend tocados.
